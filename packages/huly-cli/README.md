# Huly CLI

[![npm](https://img.shields.io/npm/v/@firfi/huly-cli)](https://www.npmjs.com/package/@firfi/huly-cli)
[![npm downloads](https://img.shields.io/npm/dm/@firfi/huly-cli)](https://www.npmjs.com/package/@firfi/huly-cli)
[![CI](https://github.com/dearlordylord/huly-mcp/actions/workflows/ci.yml/badge.svg)](https://github.com/dearlordylord/huly-mcp/actions/workflows/ci.yml)
[![Package smoke](https://github.com/dearlordylord/huly-mcp/actions/workflows/package-smoke.yml/badge.svg)](https://github.com/dearlordylord/huly-mcp/actions/workflows/package-smoke.yml)
[![Node.js 22.19+](https://img.shields.io/badge/Node.js-22.19%2B-339933?logo=node.js&logoColor=white)](https://nodejs.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

**Huly CLI** is a feature-complete command-line interface for [Huly](https://huly.io/), with full Huly operation parity with [`@firfi/huly-mcp`](https://www.npmjs.com/package/@firfi/huly-mcp). Every shared Huly operation is available as a native, schema-aware command.

Use readable terminal output interactively or `--json` in scripts and coding-agent workflows. The CLI invokes the shared operation registry directly: it does not start an MCP server or translate commands through JSON-RPC.

Visit the [Huly CLI website](https://huly-mcp.dearlordylord.com/cli/) for focused [examples](https://huly-mcp.dearlordylord.com/cli/examples/), [automation patterns](https://huly-mcp.dearlordylord.com/cli/automation/), and a [Huly MCP vs CLI comparison](https://huly-mcp.dearlordylord.com/cli/huly-mcp-vs-cli/).

> [!IMPORTANT]
> **Hosted Huly is being discontinued.** If you use `https://huly.app`, [export and migrate your data](https://github.com/hcengineering/platform/blob/develop/README.md). See Huly's [backup and restore guide](https://github.com/hcengineering/platform/blob/develop/docs/guides/backup-restore.en.md) and [self-hosting repository](https://github.com/hcengineering/huly-selfhost). Self-hosted deployments are not affected.

## Highlights

- Full operation parity with Huly MCP, enforced against the shared operation registry.
- Native commands for projects, issues, documents, chat, calendar, time tracking, workspace administration, and more.
- Generated, schema-aware command help with clear flags and named positionals.
- Human-readable output for interactive use and structured JSON output for automation.
- JSON, file-backed text, upload, download, and binary/image workflows.
- Typed errors, visible warnings, and explicit `--yes` confirmation for consequential operations.

## Installation

Requires Node.js 22.19.0 or later.

Run without installing:

```bash
pnpm dlx @firfi/huly-cli@latest --help
# or, with the npm package runner
npx -y @firfi/huly-cli@latest --help
```

Or install the `huly` command globally:

```bash
pnpm add --global @firfi/huly-cli

huly --help
```

## Quick start

Log in once and run a command:

```bash
huly auth login
huly auth status --json
huly projects list
```

Login prompts for the Huly URL, workspace, email, and password. Only the returned workspace token is stored; the password is never written to disk.

Environment-only configuration remains available for CI and ephemeral execution:

```bash
export HULY_URL=https://your-huly-instance.example.com
export HULY_WORKSPACE=yourworkspace
export HULY_EMAIL=your@email.com
export HULY_PASSWORD=yourpassword

huly projects list
```

Use `HULY_TOKEN` instead of `HULY_EMAIL` and `HULY_PASSWORD` when your deployment provides an API token.

## Configuration

The CLI supports named profiles plus the same Huly connection settings as the MCP server. Each environment variable overrides the corresponding active-profile value, so CI can remain fully environment-driven without modifying user configuration.

| Variable | Required | Description |
| --- | --- | --- |
| `HULY_URL` | Yes | Reachable self-hosted or replacement hosted Huly instance URL. |
| `HULY_WORKSPACE` | Yes | Workspace identifier. |
| `HULY_EMAIL` | Auth* | Account email. |
| `HULY_PASSWORD` | Auth* | Account password. |
| `HULY_TOKEN` | Auth* | API token; use instead of email/password. |
| `HULY_CONNECTION_TIMEOUT` | No | Connection timeout in milliseconds. |

*Auth: provide either `HULY_EMAIL` and `HULY_PASSWORD`, or `HULY_TOKEN`.

### Authentication and profiles

```bash
# Interactive login, sanitized inspection, and logout.
huly auth login --profile work
huly auth status --json
huly auth logout --profile work

# Named contexts with an optional default project.
huly profile create work --url https://huly.example.com --workspace main --default-project HULY
huly profile list --json
huly profile select work
huly profile update work --default-project CLI
```

Configuration precedence is:

1. `HULY_*` environment variables, independently by field;
2. the selected profile's URL, workspace, stored token, and optional default project; and
3. no implicit fallback.

Profiles are stored in the operating system's user configuration directory: `$XDG_CONFIG_HOME/huly` (or `~/.config/huly`) on Linux, `~/Library/Application Support/huly` on macOS, and `%APPDATA%\huly` on Windows. Profile metadata and credentials are separate schema-validated JSON files. On systems that support POSIX permissions, directories use mode `0700` and files use `0600`.

## Usage

Help is progressive: `huly --help` lists top-level command domains, `huly <domain> --help` lists its immediate commands, and leaf help shows the complete schema-derived inputs. Generate shell completion with `huly --completions sh`, `huly --completions bash`, `huly --completions fish`, or `huly --completions zsh`.

```bash
huly projects list
huly issues get PROJ 123 --json
huly issues create --project PROJ --title "Fix login flow" --description-file ./body.md
huly comments delete --project PROJ --issue-identifier 123 --comment-id 0123456789abcdef --yes
huly attachments download 0123456789abcdef --output ./attachment.bin
huly search "customer import"
```

### JSON and automation

Every command supports `--json`, `--input-json '<object>'`, and `--input-file path/to/input.json`. JSON sources make nested objects, arrays, unions, and explicit `null` available without awkward shell syntax.

```bash
# Inspect structured output.
huly projects list --json | jq '.projects[] | {identifier, name}'

# Supply a complete operation input from a file.
huly issues create --input-file ./new-issue.json --json

# Override fields from JSON with explicit flags.
huly issues create \
  --input-json '{"project":"PROJ","title":"Draft title"}' \
  --title "Final title" \
  --json
```

Inputs are merged from lowest to highest precedence: JSON sources in command-line order, named positionals, explicit field flags, then file-backed field flags.

On failure, stdout remains empty. With `--json`, stderr contains one JSON document:

```json
{
  "code": "NOT_FOUND",
  "message": "Issue HULY-404 was not found.",
  "retryable": false,
  "hint": "Optional next action",
  "details": { "tag": "IssueNotFoundError" }
}
```

`hint` and `details` are optional. The stable exit taxonomy is:

| Failure code | Exit status | Meaning |
| --- | ---: | --- |
| `INTEGRATION_FAILED` | 1 | Huly or a required integration failed; inspect `retryable`. |
| `INVALID_INPUT` | 2 | CLI input or confirmation is invalid or missing. |
| `AUTHENTICATION_FAILED` | 3 | Credentials are absent, invalid, or expired. |
| `AUTHORIZATION_DENIED` | 4 | The authenticated account lacks permission. |
| `NOT_FOUND`, `AMBIGUOUS_RESULT`, `CONFLICT` | 5 | A stable domain lookup or state conflict prevented the operation. |
| `INTERNAL_ERROR` | 70 | An unexpected CLI defect, distinct from expected failures. |

Never blindly retry a consequential write. Retry only when `retryable` is true and the operation is known to be safe to repeat.

### Files and binary output

Text inputs can be read from files with command-specific flags such as `--description-file`. Upload commands accept their documented path, URL, or base64 sources. Commands that return supported attachment or image bytes can write them with `--output`.

```bash
huly issues create --project PROJ --title "Release notes" --description-file ./release-notes.md
huly attachments download 0123456789abcdef --output ./attachment.bin
```

### Consequential operations

Commands that delete data, change access, or perform other consequential actions require `--yes`. The CLI refuses them before creating a Huly client when confirmation is absent.

```bash
huly comments delete --project PROJ --issue-identifier 123 --comment-id 0123456789abcdef --yes
```

## Capability overview

| Area | Examples |
| --- | --- |
| Planning and delivery | Projects, issues, milestones, components, time tracking, test management, processes. |
| Documents and storage | Teamspaces, documents, snapshots, Drive, attachments, drawings, templates. |
| Collaboration | Channels, direct messages, comments, activity, notifications, approvals, virtual office. |
| Business workflows | Boards, cards, inventory, leads, recruiting, support, mail, calendar. |
| People and workspace | Contacts, collaborators, workspace members, profiles, preferences, user statuses. |
| Administration | Spaces, labels, tags, custom fields, workflows, permissions, sequences, SDK discovery. |

## Full operation parity

The CLI exposes exactly one native command for every operation in the shared Huly operation registry. Inputs, outputs, warnings, and typed failures therefore stay aligned with Huly MCP, and parity drift fails the project quality gate.

“Full operation parity” describes Huly capabilities rather than MCP protocol mechanics. The CLI provides native command-line equivalents where presentation differs:

- generated root and command help instead of MCP tool discovery;
- flags, named positionals, and JSON/file inputs instead of JSON-RPC arguments;
- terminal or JSON output instead of MCP content envelopes;
- file output and safe metadata for supported attachment and image bytes; and
- terminal warning sections or JSON `warnings` for agent-visible diagnostics.

MCP resources, prompts, proxy discovery, toolset filtering, and transport negotiation remain server-only protocol features; they are not missing Huly operations.

## Output, warnings, and exit status

Successful commands write their result to stdout and exit with status 0. Without `--json`, catalog-owned rendering metadata selects useful table columns. Reusable identifiers are never ellipsized; narrow terminals omit lower-priority presentation fields. ANSI styling is emitted only to a compatible TTY and is disabled by `NO_COLOR`. With `--json`, the CLI writes the same lossless structured value regardless of terminal width or styling.

Operation warnings are part of a successful result. Human-readable output appends a visible `Warnings:` section to stdout; empty result sets remain explicit. JSON output includes `warnings` alongside `result`. Errors—including invalid input, missing `--yes` confirmation, authentication failures, connection failures, and failed operations—are written to stderr and produce a documented non-zero exit status.

## Agent Skill

The published package includes an installable `huly-cli` Agent Skill at `skills/huly-cli`. After a global pnpm installation, compatible coding agents can copy it into their skill directory:

```bash
cp -R "$(pnpm root --global)/@firfi/huly-cli/skills/huly-cli" "${CODEX_HOME:-$HOME/.codex}/skills/huly-cli"
```

The compact skill teaches authentication, progressive command discovery, structured automation, safe confirmation, and representative workflows. It deliberately does not duplicate all 500+ commands. Repository contributors can run `pnpm verify-cli-skill` to reject drift from the command tree and failure contract, and `pnpm verify-cli-skill-package` to smoke-test the published tarball contents.

## Telemetry

Aggregate CLI analytics are enabled by default. Disable them with:

```bash
export HULY_CLI_TELEMETRY=0
```

The CLI uses the same PostHog project as `@firfi/huly-mcp`, with `surface=cli` and `package_name=@firfi/huly-cli` tags so the two surfaces can be measured separately. Set `HULY_CLI_TELEMETRY_DEBUG=1` to print telemetry diagnostics to stderr.

## Updating

`@latest` asks the package runner for the newest published release:

```bash
pnpm dlx @firfi/huly-cli@latest --help
```

For a global installation, rerun the install command with `@latest`:

```bash
pnpm add --global @firfi/huly-cli@latest
```

## Troubleshooting

- Run `huly <command> --help` to inspect the exact accepted flags, positionals, alternatives, and defaults.
- Use `--json` when another program consumes stdout; successful operation warnings remain part of that structured output, while errors are written to stderr.
- If authentication fails, verify that `HULY_URL` is reachable and provide either token auth or the complete email/password pair.
- Quote shell-sensitive environment values, especially passwords containing characters such as `*`, `%`, `!`, or `#`.
- Increase `HULY_CONNECTION_TIMEOUT` when a self-hosted deployment needs more time to connect.

## Project

- [Huly CLI website](https://huly-mcp.dearlordylord.com/cli/)
- [Source code](https://github.com/dearlordylord/huly-mcp/tree/master/packages/huly-cli)
- [Issue tracker](https://github.com/dearlordylord/huly-mcp/issues)
- [Changelog](https://github.com/dearlordylord/huly-mcp/blob/master/packages/huly-cli/CHANGELOG.md)
- [MIT license](https://github.com/dearlordylord/huly-mcp/blob/master/LICENSE)

<details>
<summary><strong>Complete generated command reference</strong></summary>

<!-- CLI_COMMAND_REFERENCE_START -->
<!-- Generated from cliCommandCatalog and shared operation schemas. Run `pnpm update-cli-readme`. -->
## Complete command reference

This release provides 522 native commands for 522 shared Huly operations.

All commands also accept `--json`, `--input-json <object>`, and `--input-file <path>`. Explicit field flags override JSON sources. Structured fields accept JSON. Named positionals are required and are not duplicated as flags. Required non-positional inputs may instead be supplied through either JSON source.

| Command | Purpose and behavior | Required positionals | Required inputs (flag or JSON) | Optional flags and alternatives |
| --- | --- | --- | --- | --- |
| `huly activity filters list` | List Activity Filters | — | — | `--limit <value>` — Maximum number of activity filters to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly activity list` | List Activity | — | — | `--project <value>` — Project identifier for issue activity, e.g. 'HULY'. Type: string. Choose one input alternative: { project, issueIdentifier } \| { teamspace, document } \| { channel } \| { objectId, objectClass }.<br>`--issue-identifier <value>` — Issue identifier for issue activity, e.g. 'HULY-123' or '123'. Type: string. Choose one input alternative: { project, issueIdentifier } \| { teamspace, document } \| { channel } \| { objectId, objectClass }.<br>`--limit <value>` — Maximum number of activity messages to return (default: 50) Type: integer.<br>`--teamspace <value>` — Teamspace name or ID for document activity. Type: string. Choose one input alternative: { project, issueIdentifier } \| { teamspace, document } \| { channel } \| { objectId, objectClass }.<br>`--document <value>` — Document title or ID for document activity. Type: string. Choose one input alternative: { project, issueIdentifier } \| { teamspace, document } \| { channel } \| { objectId, objectClass }.<br>`--channel <value>` — Channel name or ID for channel activity. Type: string. Choose one input alternative: { project, issueIdentifier } \| { teamspace, document } \| { channel } \| { objectId, objectClass }.<br>`--object-id <value>` — Internal Huly object ID to get activity for. Type: string. Choose one input alternative: { project, issueIdentifier } \| { teamspace, document } \| { channel } \| { objectId, objectClass }.<br>`--object-class <value>` — Internal Huly object class for objectId, such as 'tracker:class:Issue'. Type: string. Choose one input alternative: { project, issueIdentifier } \| { teamspace, document } \| { channel } \| { objectId, objectClass }. |
| `huly activity mentions list` | List Mentions | — | — | `--limit <value>` — Maximum number of mentions to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly activity messages get <messageId>` | Get Activity Message | `<messageId>` — ID of the activity message to retrieve. Type: string. | — | — |
| `huly activity messages pin <messageId> <pinned>` | Pin Activity Message | `<messageId>` — ID of the activity message to pin or unpin. Type: string.<br>`<pinned>` — Whether the activity message should be pinned. Type: boolean. | — | — |
| `huly activity messages save <messageId>` | Save Message | `<messageId>` — ID of the activity message to save/bookmark Type: string. | — | — |
| `huly activity messages unsave <messageId>` | Unsave Message | `<messageId>` — ID of the saved activity message to remove from bookmarks Type: string. | — | — |
| `huly activity reactions add <messageId> <emoji>` | Add Reaction | `<messageId>` — ID of the activity message to react to Type: string.<br>`<emoji>` — Emoji to add (e.g., ':thumbsup:', ':heart:', or unicode emoji) Type: string. | — | — |
| `huly activity reactions list <messageId>` | List Reactions | `<messageId>` — ID of the activity message to list reactions for Type: string. | — | `--limit <value>` — Maximum number of reactions to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly activity reactions remove <messageId> <emoji>` | Remove Reaction Requires `--yes`. | `<messageId>` — ID of the activity message Type: string.<br>`<emoji>` — Emoji to remove Type: string. | — | — |
| `huly activity references list <objectId> <objectClass>` | List Activity References | `<objectId>` — Internal Huly object ID to list activity references for. Type: string.<br>`<objectClass>` — Internal Huly object class to list activity references for. Type: string. | — | `--direction <value>` — Reference direction. 'from' lists references created by this object, 'to' lists references pointing at this object, 'both' lists either direction (default: both). Allowed values: "from" \| "to" \| "both". Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of activity references to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly activity replies add <messageId> <body>` | Add Activity Reply | `<messageId>` — ID of the activity message to reply to. Type: string.<br>`<body>` — Reply body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--body-file <path>` — read body as text |
| `huly activity replies delete <replyId>` | Delete Activity Reply Requires `--yes`. | `<replyId>` — ID of the reply activity message to delete. Type: string. | — | — |
| `huly activity replies list <messageId>` | List Activity Replies | `<messageId>` — ID of the activity message whose replies should be listed. Type: string. | — | `--limit <value>` — Maximum number of replies to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly activity replies update <replyId> <body>` | Update Activity Reply | `<replyId>` — ID of the reply activity message to update. Type: string.<br>`<body>` — New reply body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--body-file <path>` — read body as text |
| `huly activity saved list` | List Saved Messages | — | — | `--limit <value>` — Maximum number of saved messages to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly approvals add <attachedTo> <attachedToClass>` | Add an approval request; pass --requested and --tx as JSON Requires `--yes`. | `<attachedTo>` — Raw Huly target document _id that the approval request attaches to. Type: string.<br>`<attachedToClass>` — Raw Huly target document class id. Type: string. | `--requested <value>` — People who must decide the approval. Type: array. Pass arrays or objects as JSON.<br>`--tx <value>` — Opaque Huly SDK transaction applied when the request completes. | `--space <value>` — Raw Huly target space id; omit to resolve it from the target. Type: string. Pass null to clear the field.<br>`--collection <value>` — Parent collection name; defaults to requests. Type: string. Pass null to clear the field.<br>`--required-approves-count <value>` — Number of approvals required; defaults to the unique requested person count. Type: integer. Pass null to clear the field.<br>`--rejected-tx <value>` — Optional opaque Huly SDK transaction applied when the request is rejected. Pass null to clear the field. |
| `huly approvals approve <request>` | Approve a request as the current Huly actor Requires `--yes`. | `<request>` — Approval Request document _id. Type: string. | — | `--comment <value>` — Optional decision comment to attach before approving. Type: string. Pass null to clear the field. |
| `huly approvals cancel <request>` | Cancel Approval Request Requires `--yes`. | `<request>` — Approval Request document _id. Type: string. | — | — |
| `huly approvals comments add <request> <body>` | Add Approval Request Comment | `<request>` — Approval Request document _id. Type: string.<br>`<body>` — Comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--body-file <path>` — read body as text |
| `huly approvals get <request>` | Get Approval Request | `<request>` — Approval Request document _id. Type: string. | — | — |
| `huly approvals list` | List Approval Requests | — | — | `--status <value>` — Optional approval request status filter. Allowed values: "Active" \| "Completed" \| "Rejected" \| "Cancelled". Type: string. Pass null to clear the field.<br>`--attached-to <value>` — Optional raw Huly target document _id. Type: string. Pass null to clear the field.<br>`--attached-to-class <value>` — Optional raw Huly target document class id. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of approval requests to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly approvals reject <request> <comment>` | Reject a request as the current Huly actor Requires `--yes`. | `<request>` — Approval Request document _id. Type: string.<br>`<comment>` — Required rejection decision comment. Type: string. | — | — |
| `huly associations create <sourceClass> <targetClass> <sourceRole> <targetRole> <cardinality>` | Create a generic association definition | `<sourceClass>` — Source Huly class ID. core:class:* system classes are rejected. Type: string.<br>`<targetClass>` — Target Huly class ID. core:class:* system classes are rejected. Type: string.<br>`<sourceRole>` — Role name stored on the source side of the association. Type: string.<br>`<targetRole>` — Role name stored on the target side of the association. Type: string.<br>`<cardinality>` — Association cardinality: one-to-one, one-to-many, or many-to-many. Allowed values: "one-to-one" \| "one-to-many" \| "many-to-many". Type: string. | — | `--automation-only` / `--no-automation-only` — Whether Huly automation owns relation writes. Defaults to false. Type: boolean. Pass null to clear the field.<br>`--if-exists <value>` — return_existing returns an identical association; fail reports it as an error. Allowed values: "return_existing" \| "fail". Type: string. Pass null to clear the field. |
| `huly associations delete <association>` | Delete an unused association definition Requires `--yes`. | `<association>` — Association ID or unambiguous name. A missing association is a successful no-op. Type: string. | — | — |
| `huly attachments add <objectId> <objectClass> <space> <filename> <contentType>` | Add an attachment to a raw Huly object. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<objectId>` — ID of the parent object (issue, document, etc.) Type: string.<br>`<objectClass>` — Class of the parent object (e.g., 'tracker:class:Issue', 'document:class:Document') Type: string.<br>`<space>` — Space ID where the parent object resides Type: string.<br>`<filename>` — Name of the file Type: string.<br>`<contentType>` — MIME type of the file (e.g., 'image/png', 'application/pdf') Type: string. | — | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Attachment description Type: string. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether to pin the attachment (default: false) Type: boolean. Pass null to clear the field.<br>`--kind <value>` — Attachment subclass to create: attachment, embedding, or photo (default: attachment). Allowed values: "attachment" \| "embedding" \| "photo". Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly attachments add-to-document` | Add an attachment to a document. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | — | `--teamspace <value>` — Teamspace name or ID Type: string.<br>`--document <value>` — Document title or ID Type: string.<br>`--filename <value>` — Name of the file Type: string.<br>`--content-type <value>` — MIME type of the file (e.g., 'image/png', 'application/pdf') Type: string. | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Attachment description Type: string. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether to pin the attachment (default: false) Type: boolean. Pass null to clear the field.<br>`--kind <value>` — Attachment subclass to create: attachment, embedding, or photo (default: attachment). Allowed values: "attachment" \| "embedding" \| "photo". Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly attachments add-to-issue` | Add an attachment to an issue. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string.<br>`--identifier <value>` — Issue identifier (e.g., 'HULY-123') Type: string.<br>`--filename <value>` — Name of the file Type: string.<br>`--content-type <value>` — MIME type of the file (e.g., 'image/png', 'application/pdf') Type: string. | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Attachment description Type: string. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether to pin the attachment (default: false) Type: boolean. Pass null to clear the field.<br>`--kind <value>` — Attachment subclass to create: attachment, embedding, or photo (default: attachment). Allowed values: "attachment" \| "embedding" \| "photo". Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly attachments delete <attachmentId>` | Delete Attachment Requires `--yes`. | `<attachmentId>` — Attachment ID to delete Type: string. | — | — |
| `huly attachments download <attachmentId>` | Resolve or download an attachment Supports `--output <path>`. | `<attachmentId>` — Attachment ID Type: string. | — | — |
| `huly attachments get <attachmentId>` | Get attachment metadata | `<attachmentId>` — Attachment ID Type: string. | — | — |
| `huly attachments list` | List attachments | — | `--object-id <value>` — ID of the parent object (issue, document, etc.) Type: string.<br>`--object-class <value>` — Class of the parent object (e.g., 'tracker:class:Issue', 'document:class:Document') Type: string. | `--limit <value>` — Maximum number of attachments to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly attachments pin <attachmentId> <pinned>` | Pin Attachment | `<attachmentId>` — Attachment ID Type: string.<br>`<pinned>` — Whether to pin (true) or unpin (false) Type: boolean. | — | — |
| `huly attachments read-image <attachmentId>` | Read a supported image attachment; use --output to write its decoded bytes Supports `--output <path>`. | `<attachmentId>` — Attachment ID whose image content should be returned. Type: string. | — | — |
| `huly attachments save <attachmentId>` | Save Attachment | `<attachmentId>` — Attachment ID to save/bookmark. Type: string. | — | — |
| `huly attachments saved list` | List Saved Attachments | — | — | `--limit <value>` — Maximum number of saved attachments to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly attachments unsave <attachmentId>` | Unsave Attachment | `<attachmentId>` — Attachment ID to save/bookmark. Type: string. | — | — |
| `huly attachments update <attachmentId>` | Update Attachment | `<attachmentId>` — Attachment ID Type: string. | — | `--description <value>` — New description (null to clear) Type: string. Choose one input alternative: { description } \| { pinned }. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Pin or unpin the attachment Type: boolean. Choose one input alternative: { description } \| { pinned }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly boards archive <board>` | Archive Board | `<board>` — Board locator: board _id or exact board name. Names must match exactly; use list_boards to discover IDs when names are ambiguous. Type: string. | — | — |
| `huly boards cards archive <board> <card>` | Archive Board Card | `<board>` — Board locator: board _id or exact board name. Names must match exactly; use list_boards to discover IDs when names are ambiguous. Type: string.<br>`<card>` — Board card locator scoped to the board: card _id, CARD-123 identifier, bare number 123, or exact card title. Type: string. | — | — |
| `huly boards cards create <board> <title>` | Create Board Card | `<board>` — Board locator: board _id or exact board name. Names must match exactly; use list_boards to discover IDs when names are ambiguous. Type: string.<br>`<title>` — Board card title. Type: string. | — | `--description <value>` — Board card description in markdown. Stored as inline Huly Markup. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--kind <value>` — Board card task type _id or exact task type name. Omit to use the unambiguous board card task type for the board project type. Type: string. Pass null to clear the field.<br>`--status <value>` — Workflow status _id or exact status name. Omit to use the first status configured on the board project type. Type: string. Pass null to clear the field.<br>`--assignee <value>` — Assignee Employee _id, exact email address, or exact person display name. Type: string. Pass null to clear the field.<br>`--members <value>` — Initial card members. Each entry accepts Employee _id, exact email, or exact person display name. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--location <value>` — Optional card location text. Type: string. Pass null to clear the field.<br>`--cover <value>` — Cover settings for a board card. Type: object. JSON shape alternatives: { color, size }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--start-date <value>` — Start date timestamp in milliseconds. Type: integer. Pass null to clear the field.<br>`--due-date <value>` — Due date timestamp in milliseconds. Type: integer. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly boards cards delete <board> <card>` | Delete Board Card Requires `--yes`. | `<board>` — Board locator: board _id or exact board name. Names must match exactly; use list_boards to discover IDs when names are ambiguous. Type: string.<br>`<card>` — Board card locator scoped to the board: card _id, CARD-123 identifier, bare number 123, or exact card title. Type: string. | — | — |
| `huly boards cards get <board> <card>` | Get Board Card | `<board>` — Board locator: board _id or exact board name. Names must match exactly; use list_boards to discover IDs when names are ambiguous. Type: string.<br>`<card>` — Board card locator scoped to the board: card _id, CARD-123 identifier, bare number 123, or exact card title. Type: string. | — | — |
| `huly boards cards labels add <board> <card> <label>` | Add Board Card Label | `<board>` — Board name or ID. Type: string.<br>`<card>` — Board card title or ID. Type: string.<br>`<label>` — Board label ID or exact title. Type: string. | — | `--color <value>` — Color for the board label definition when label is a new title (default: 0). Existing labels are not recolored. Type: integer. Pass null to clear the field.<br>`--category <value>` — Category for the board label definition when label is a new title. Existing labels are not recategorized. Type: string. Pass null to clear the field. |
| `huly boards cards labels list <board> <card>` | List Board Card Labels | `<board>` — Board name or ID. Type: string.<br>`<card>` — Board card title or ID. Type: string. | — | — |
| `huly boards cards labels remove <board> <card> <label>` | Remove Board Card Label Requires `--yes`. | `<board>` — Board name or ID. Type: string.<br>`<card>` — Board card title or ID. Type: string.<br>`<label>` — Board label ID or exact title. Type: string. | — | — |
| `huly boards cards list <board>` | List Board Cards | `<board>` — Board locator: board _id or exact board name. Names must match exactly; use list_boards to discover IDs when names are ambiguous. Type: string. | — | `--include-archived` / `--no-include-archived` — Include archived board cards in results (default: false, showing only active cards). Type: boolean. Pass null to clear the field.<br>`--title-search <value>` — Search board cards by title substring (case-insensitive SQL LIKE). Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of board cards to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly boards cards unarchive <board> <card>` | Unarchive Board Card Requires `--yes`. | `<board>` — Board locator: board _id or exact board name. Names must match exactly; use list_boards to discover IDs when names are ambiguous. Type: string.<br>`<card>` — Board card locator scoped to the board: card _id, CARD-123 identifier, bare number 123, or exact card title. Type: string. | — | — |
| `huly boards cards update <board> <card>` | Update Board Card | `<board>` — Board locator: board _id or exact board name. Names must match exactly; use list_boards to discover IDs when names are ambiguous. Type: string.<br>`<card>` — Board card locator scoped to the board: card _id, CARD-123 identifier, bare number 123, or exact card title. Type: string. | — | `--title <value>` — New board card title. Type: string. Choose one input alternative: { title } \| { description } \| { status } \| { assignee } \| { members } \| { addMembers } \| { removeMembers } \| { location } \| { cover } \| { startDate } \| { dueDate }. Pass null to clear the field.<br>`--description <value>` — New board card description in markdown; null clears it. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Choose one input alternative: { title } \| { description } \| { status } \| { assignee } \| { members } \| { addMembers } \| { removeMembers } \| { location } \| { cover } \| { startDate } \| { dueDate }. Pass null to clear the field.<br>`--status <value>` — New workflow status _id or exact status name in the board project type. Type: string. Choose one input alternative: { title } \| { description } \| { status } \| { assignee } \| { members } \| { addMembers } \| { removeMembers } \| { location } \| { cover } \| { startDate } \| { dueDate }. Pass null to clear the field.<br>`--assignee <value>` — New assignee Employee _id, exact email, or exact person display name; null unassigns. Type: string. Choose one input alternative: { title } \| { description } \| { status } \| { assignee } \| { members } \| { addMembers } \| { removeMembers } \| { location } \| { cover } \| { startDate } \| { dueDate }. Pass null to clear the field.<br>`--members <value>` — Replace card members with this exact list. Cannot be combined with addMembers or removeMembers. Type: array. Choose one input alternative: { title } \| { description } \| { status } \| { assignee } \| { members } \| { addMembers } \| { removeMembers } \| { location } \| { cover } \| { startDate } \| { dueDate }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--add-members <value>` — Members to add without replacing existing members. Type: array. Choose one input alternative: { title } \| { description } \| { status } \| { assignee } \| { members } \| { addMembers } \| { removeMembers } \| { location } \| { cover } \| { startDate } \| { dueDate }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--remove-members <value>` — Members to remove without replacing existing members. Type: array. Choose one input alternative: { title } \| { description } \| { status } \| { assignee } \| { members } \| { addMembers } \| { removeMembers } \| { location } \| { cover } \| { startDate } \| { dueDate }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--location <value>` — New card location; null clears it. Type: string. Choose one input alternative: { title } \| { description } \| { status } \| { assignee } \| { members } \| { addMembers } \| { removeMembers } \| { location } \| { cover } \| { startDate } \| { dueDate }. Pass null to clear the field.<br>`--cover <value>` — New card cover; null clears the cover. Type: object. JSON shape alternatives: { color, size }. Choose one input alternative: { title } \| { description } \| { status } \| { assignee } \| { members } \| { addMembers } \| { removeMembers } \| { location } \| { cover } \| { startDate } \| { dueDate }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--start-date <value>` — New start date timestamp in milliseconds; null clears it. Type: integer. Choose one input alternative: { title } \| { description } \| { status } \| { assignee } \| { members } \| { addMembers } \| { removeMembers } \| { location } \| { cover } \| { startDate } \| { dueDate }. Pass null to clear the field.<br>`--due-date <value>` — New due date timestamp in milliseconds; null clears it. Type: integer. Choose one input alternative: { title } \| { description } \| { status } \| { assignee } \| { members } \| { addMembers } \| { removeMembers } \| { location } \| { cover } \| { startDate } \| { dueDate }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly boards common-preference get` | Get Board Common Preference | — | — | — |
| `huly boards create <name>` | Create Board | `<name>` — Board name. Creation is idempotent by exact active board name. Type: string. | — | `--description <value>` — Plain text board description. Type: string. Pass null to clear the field.<br>`--private` / `--no-private` — Whether the board is private (default: false). Type: boolean. Pass null to clear the field.<br>`--project-type <value>` — Optional board project type _id or exact name. Omit to use the unambiguous project type whose descriptor is board.descriptors.BoardType. Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly boards get <board>` | Get Board | `<board>` — Board locator: board _id or exact board name. Names must match exactly; use list_boards to discover IDs when names are ambiguous. Type: string. | — | — |
| `huly boards labels create <title>` | Create Board Label | `<title>` — Board label title. Creation is idempotent by exact board label title when there is one match. Type: string. | — | `--color <value>` — Huly platform color palette index from 0 through 23 (default: 0). Type: integer. Pass null to clear the field.<br>`--description <value>` — Board label description. Type: string. Pass null to clear the field.<br>`--category <value>` — Board label category _id or exact label. If omitted, uses the board card tag default category or board.category.Other. Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly boards labels delete <label>` | Delete Board Label Requires `--yes`. | `<label>` — Board label ID or exact title. Type: string. | — | — |
| `huly boards labels list` | List Board Labels | — | — | `--category <value>` — Board label category ID or exact label. Type: string. Pass null to clear the field.<br>`--title-search <value>` — Case-insensitive label title search. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of labels to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly boards labels update <label>` | Update Board Label | `<label>` — Board label TagElement _id or exact title. Type: string. | — | `--title <value>` — New board label title. Type: string. Choose one input alternative: { title } \| { color } \| { description } \| { category }. Pass null to clear the field.<br>`--color <value>` — New Huly platform color palette index from 0 through 23. Type: integer. Choose one input alternative: { title } \| { color } \| { description } \| { category }. Pass null to clear the field.<br>`--description <value>` — New board label description; null clears it. Type: string. Choose one input alternative: { title } \| { color } \| { description } \| { category }. Pass null to clear the field.<br>`--category <value>` — New board label category _id or exact label. Type: string. Choose one input alternative: { title } \| { color } \| { description } \| { category }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly boards list` | List Boards | — | — | `--include-archived` / `--no-include-archived` — Include archived boards in results (default: false, showing only active boards). Type: boolean. Pass null to clear the field.<br>`--limit <value>` — Maximum number of boards to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly boards menu-pages list` | List Board Menu Pages | — | — | `--page <value>` — Board menu page ID, exact name, or label. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of rows to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly boards saved-views get <savedView>` | Get Board Saved View | `<savedView>` — FilteredView _id or exact saved view name scoped to attachedTo = board.app.Board. Type: string. | — | — |
| `huly boards saved-views list` | List Board Saved Views | — | — | `--visibility <value>` — Saved-view visibility filter. Allowed values: "own" \| "shared" \| "all". Type: string. Pass null to clear the field.<br>`--name-search <value>` — Case-insensitive saved-view name search. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of rows to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly boards unarchive <board>` | Unarchive Board Requires `--yes`. | `<board>` — Board locator: board _id or exact board name. Names must match exactly; use list_boards to discover IDs when names are ambiguous. Type: string. | — | — |
| `huly boards update <board>` | Update Board | `<board>` — Board locator: board _id or exact board name. Names must match exactly; use list_boards to discover IDs when names are ambiguous. Type: string. | — | `--name <value>` — New exact board name. Type: string. Choose one input alternative: { name } \| { description } \| { private }. Pass null to clear the field.<br>`--description <value>` — New plain text board description; null clears it. Type: string. Choose one input alternative: { name } \| { description } \| { private }. Pass null to clear the field.<br>`--private` / `--no-private` — Whether the board is private. Type: boolean. Choose one input alternative: { name } \| { description } \| { private }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly boards viewlets list` | List Board Viewlets | — | — | `--viewlet <value>` — Board viewlet ID, exact title, variant, or descriptor ID. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of rows to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly calendar calendars list` | List Calendars | — | — | — |
| `huly calendar events create <title> <date>` | Create a calendar event | `<title>` — Event title Type: string.<br>`<date>` — Start date/time (timestamp) Type: integer. | — | `--description <value>` — Event description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--due-date <value>` — End date/time (timestamp). If omitted, Huly MCP uses date + 1 hour. Type: integer. Pass null to clear the field.<br>`--all-day` / `--no-all-day` — All-day event (default: false) Type: boolean. Pass null to clear the field.<br>`--location <value>` — Event location Type: string. Pass null to clear the field.<br>`--participants <value>` — Participants to invite. Each entry may be a plain email string or an object with email, exact name, or personId. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--external-participants <value>` — External participant email addresses that are not resolvable workspace contacts. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--reminders <value>` — Reminder timestamps in milliseconds. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--access <value>` — Event access level. Allowed values: "freeBusyReader" \| "reader" \| "writer" \| "owner". Type: string. Pass null to clear the field.<br>`--time-zone <value>` — IANA time zone for the event, for example 'America/New_York'. Type: string. Pass null to clear the field.<br>`--block-time` / `--no-block-time` — Whether this event blocks the user's time on the calendar. Type: boolean. Pass null to clear the field.<br>`--visibility <value>` — Event visibility (public, freeBusy, private) Allowed values: "public" \| "freeBusy" \| "private". Type: string. Pass null to clear the field.<br>`--calendar-id <value>` — Target writable calendar ID. If omitted, uses the authenticated user's primary personal calendar. Use list_calendars to discover valid calendar IDs. Type: string. Pass null to clear the field.<br>`--calendar-name <value>` — Target writable calendar name. Use when you know the calendar's displayed name but not its ID. Do not provide with calendarId. Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly calendar events delete <eventId>` | Delete Event Requires `--yes`. | `<eventId>` — Event ID Type: string. | — | — |
| `huly calendar events get <eventId>` | Get Event | `<eventId>` — Event ID Type: string. | — | — |
| `huly calendar events instances list <recurringEventId>` | List Event Instances | `<recurringEventId>` — Recurring event ID Type: string. | — | `--from <value>` — Start date filter (timestamp) Type: integer. Pass null to clear the field.<br>`--to <value>` — End date filter (timestamp) Type: integer. Pass null to clear the field.<br>`--limit <value>` — Maximum number of instances to return (default: 50) Type: integer. Pass null to clear the field.<br>`--include-participants` / `--no-include-participants` — Include full participant info (requires extra lookups, default: false) Type: boolean. Pass null to clear the field. |
| `huly calendar events list` | List Events | — | — | `--from <value>` — Start date filter (timestamp) Type: integer. Pass null to clear the field.<br>`--to <value>` — End date filter (timestamp) Type: integer. Pass null to clear the field.<br>`--limit <value>` — Maximum number of events to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly calendar events recurring create <title> <startDate>` | Create a recurring event; pass --rules as JSON | `<title>` — Event title Type: string.<br>`<startDate>` — First occurrence start date/time (timestamp) Type: integer. | `--rules <value>` — Recurring rules (RFC5545 RRULE format) Type: array. Pass arrays or objects as JSON. | `--description <value>` — Event description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--due-date <value>` — First occurrence end date/time (timestamp). If omitted, Huly MCP uses startDate + 1 hour. Type: integer. Pass null to clear the field.<br>`--all-day` / `--no-all-day` — All-day event (default: false) Type: boolean. Pass null to clear the field.<br>`--location <value>` — Event location Type: string. Pass null to clear the field.<br>`--participants <value>` — Participants to invite. Each entry may be a plain email string or an object with email, exact name, or personId. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--external-participants <value>` — External participant email addresses that are not resolvable workspace contacts. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--reminders <value>` — Reminder timestamps in milliseconds. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--time-zone <value>` — Time zone (e.g., 'America/New_York') Type: string. Pass null to clear the field.<br>`--access <value>` — Event access level. Allowed values: "freeBusyReader" \| "reader" \| "writer" \| "owner". Type: string. Pass null to clear the field.<br>`--block-time` / `--no-block-time` — Whether this event blocks the user's time on the calendar. Type: boolean. Pass null to clear the field.<br>`--visibility <value>` — Event visibility (public, freeBusy, private) Allowed values: "public" \| "freeBusy" \| "private". Type: string. Pass null to clear the field.<br>`--calendar-id <value>` — Target writable calendar ID. If omitted, uses the authenticated user's primary personal calendar. Use list_calendars to discover valid calendar IDs. Type: string. Pass null to clear the field.<br>`--calendar-name <value>` — Target writable calendar name. Use when you know the calendar's displayed name but not its ID. Do not provide with calendarId. Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly calendar events recurring list` | List Recurring Events | — | — | `--limit <value>` — Maximum number of recurring events to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly calendar events update <eventId>` | Update a calendar event | `<eventId>` — Event ID Type: string. | — | `--title <value>` — New event title Type: string. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--description <value>` — New event description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Type: string. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--date <value>` — New start date/time (timestamp) Type: integer. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--due-date <value>` — New end date/time (timestamp) Type: integer. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--all-day` / `--no-all-day` — All-day event Type: boolean. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--location <value>` — New event location. Pass null to clear; empty string is also accepted. Type: string. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--visibility <value>` — New event visibility Allowed values: "public" \| "freeBusy" \| "private". Type: string. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--participants <value>` — Replace all workspace participants with these resolved participants. Type: array. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--add-participants <value>` — Resolve and add these workspace participants, preserving existing participants. Type: array. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--remove-participants <value>` — Resolve and remove these workspace participants, preserving other participants. Type: array. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--external-participants <value>` — Replace all external participant email addresses. Type: array. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--add-external-participants <value>` — Add external participant email addresses, preserving existing external participants. Type: array. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--remove-external-participants <value>` — Remove external participant email addresses, preserving other external participants. Type: array. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--reminders <value>` — Replace event reminders with these reminder timestamps. Type: array. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--access <value>` — New event access level. Allowed values: "freeBusyReader" \| "reader" \| "writer" \| "owner". Type: string. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--time-zone <value>` — New IANA time zone for the event. Type: string. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--block-time` / `--no-block-time` — Whether this event blocks time. Type: boolean. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--calendar-id <value>` — Move the event to this writable calendar ID. Do not provide with calendarName. Type: string. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--calendar-name <value>` — Move the event to this writable calendar name. Do not provide with calendarId. Type: string. Choose one input alternative: { title } \| { description } \| { date } \| { dueDate } \| { allDay } \| { location } \| { visibility } \| { participants } \| { addParticipants } \| { removeParticipants } \| { externalParticipants } \| { addExternalParticipants } \| { removeExternalParticipants } \| { reminders } \| { access } \| { timeZone } \| { blockTime } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly calendar schedules create <title>` | Create a calendar schedule; pass --availability as JSON | `<title>` — Schedule title. Type: string. | `--meeting-duration <value>` — Default meeting duration in minutes. Type: integer.<br>`--meeting-interval <value>` — Minimum interval between meetings in minutes. Type: integer.<br>`--availability <value>` — Weekly schedule availability. Type: object. Pass arrays or objects as JSON.<br>`--time-zone <value>` — IANA time zone for this schedule. Type: string. | `--owner <value>` — Schedule owner locator: employee/person ID, exact display name, or email. Omit to use the authenticated user. Type: string. Pass null to clear the field.<br>`--description <value>` — Schedule description. Type: string. Pass null to clear the field.<br>`--calendar-id <value>` — Optional target calendar ID for booked events. Do not provide with calendarName. Type: string. Pass null to clear the field.<br>`--calendar-name <value>` — Optional target calendar name for booked events. Do not provide with calendarId. Type: string. Pass null to clear the field. |
| `huly calendar schedules delete <scheduleId>` | Delete Schedule Requires `--yes`. | `<scheduleId>` — Schedule ID. Type: string. | — | — |
| `huly calendar schedules get <scheduleId>` | Get Schedule | `<scheduleId>` — Schedule ID. Type: string. | — | — |
| `huly calendar schedules list` | List Schedules | — | — | `--owner <value>` — Optional schedule owner locator: employee/person ID, exact display name, or email. Omit to list schedules for all readable owners. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of schedules to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly calendar schedules update <scheduleId>` | Update a calendar schedule; pass --availability as JSON | `<scheduleId>` — Schedule ID. Type: string. | — | `--owner <value>` — New schedule owner locator: employee/person ID, exact display name, or email. Type: string. Choose one input alternative: { owner } \| { title } \| { description } \| { meetingDuration } \| { meetingInterval } \| { availability } \| { timeZone } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--title <value>` — New schedule title. Type: string. Choose one input alternative: { owner } \| { title } \| { description } \| { meetingDuration } \| { meetingInterval } \| { availability } \| { timeZone } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--description <value>` — New schedule description. Pass null to clear; empty string is also accepted. Type: string. Choose one input alternative: { owner } \| { title } \| { description } \| { meetingDuration } \| { meetingInterval } \| { availability } \| { timeZone } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--meeting-duration <value>` — New default meeting duration in minutes. Type: integer. Choose one input alternative: { owner } \| { title } \| { description } \| { meetingDuration } \| { meetingInterval } \| { availability } \| { timeZone } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--meeting-interval <value>` — New minimum interval between meetings in minutes. Type: integer. Choose one input alternative: { owner } \| { title } \| { description } \| { meetingDuration } \| { meetingInterval } \| { availability } \| { timeZone } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--availability <value>` — New weekly schedule availability. Type: object. Choose one input alternative: { owner } \| { title } \| { description } \| { meetingDuration } \| { meetingInterval } \| { availability } \| { timeZone } \| { calendarId } \| { calendarName }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--time-zone <value>` — New IANA time zone for this schedule. Type: string. Choose one input alternative: { owner } \| { title } \| { description } \| { meetingDuration } \| { meetingInterval } \| { availability } \| { timeZone } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--calendar-id <value>` — Move schedule booking target to this calendar ID. Do not provide with calendarName. Type: string. Choose one input alternative: { owner } \| { title } \| { description } \| { meetingDuration } \| { meetingInterval } \| { availability } \| { timeZone } \| { calendarId } \| { calendarName }. Pass null to clear the field.<br>`--calendar-name <value>` — Move schedule booking target to this calendar name. Do not provide with calendarId. Type: string. Choose one input alternative: { owner } \| { title } \| { description } \| { meetingDuration } \| { meetingInterval } \| { availability } \| { timeZone } \| { calendarId } \| { calendarName }. Pass null to clear the field. |
| `huly cards comments add <cardSpace> <card> <body>` | Add Card Comment | `<cardSpace>` — Card space name or ID Type: string.<br>`<card>` — Card title or ID Type: string.<br>`<body>` — Comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--body-file <path>` — read body as text |
| `huly cards comments delete <cardSpace> <card> <commentId>` | Delete Card Comment Requires `--yes`. | `<cardSpace>` — Card space name or ID Type: string.<br>`<card>` — Card title or ID Type: string.<br>`<commentId>` — Card comment ID Type: string. | — | — |
| `huly cards comments list <cardSpace> <card>` | List Card Comments | `<cardSpace>` — Card space name or ID Type: string.<br>`<card>` — Card title or ID Type: string. | — | `--limit <value>` — Maximum number of comments to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly cards comments update <cardSpace> <card> <commentId> <body>` | Update Card Comment | `<cardSpace>` — Card space name or ID Type: string.<br>`<card>` — Card title or ID Type: string.<br>`<commentId>` — Card comment ID Type: string.<br>`<body>` — Comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--body-file <path>` — read body as text |
| `huly cards create <cardSpace> <type> <title>` | Create Card | `<cardSpace>` — Card space name or ID. Type: string.<br>`<type>` — Master tag card type name or ID. Type: string.<br>`<title>` — Card title. Type: string. | — | `--content <value>` — Card content in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--parent <value>` — Parent card title or ID. Type: string. Pass null to clear the field.<br>`--content-file <path>` — read content as text |
| `huly cards delete <cardSpace> <card>` | Delete Card Requires `--yes`. | `<cardSpace>` — Card space name or ID. Type: string.<br>`<card>` — Card title or ID. Type: string. | — | — |
| `huly cards get <cardSpace> <card>` | Get Card | `<cardSpace>` — Card space name or ID. Type: string.<br>`<card>` — Card title or ID. Type: string. | — | — |
| `huly cards list <cardSpace>` | List Cards | `<cardSpace>` — Card space name or ID. Type: string. | — | `--type <value>` — Master tag card type name or ID. Type: string. Pass null to clear the field.<br>`--title-search <value>` — Case-insensitive card title search. Type: string. Pass null to clear the field.<br>`--title-regex <value>` — Regular expression card title search. Type: string. Pass null to clear the field.<br>`--content-search <value>` — Full-text card content search. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of rows to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly cards master-tags list <cardSpace>` | List Master Tags | `<cardSpace>` — Card space name or ID. Type: string. | — | — |
| `huly cards spaces list` | List Card Spaces | — | — | `--include-archived` / `--no-include-archived` — Whether to include archived card spaces. Type: boolean. Pass null to clear the field.<br>`--limit <value>` — Maximum number of rows to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly cards update <cardSpace> <card>` | Update Card | `<cardSpace>` — Card space name or ID. Type: string.<br>`<card>` — Card title or ID. Type: string. | — | `--title <value>` — New card title. Type: string. Choose one input alternative: { title } \| { content }. Pass null to clear the field.<br>`--content <value>` — New card content in markdown; null clears it. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Choose one input alternative: { title } \| { content }. Pass null to clear the field.<br>`--content-file <path>` — read content as text |
| `huly cards versions list <cardSpace> <card>` | List Card Versions | `<cardSpace>` — Exact card-space name or ID. Type: string.<br>`<card>` — Card version ID or exact title. Type: string. | — | `--limit <value>` — Maximum versions in this page. Type: integer. |
| `huly channels access request <channel>` | Request Channel Access Requires `--yes`. | `<channel>` — Private channel name or ID for which access would be requested. Type: string. | — | — |
| `huly channels archive <channel>` | Archive Channel | `<channel>` — Channel name or ID whose archive state should change. Type: string. | — | — |
| `huly channels conversations closed set <closed>` | Set Conversation Closed | `<closed>` — True to close/hide this conversation for the authenticated user, false to reopen it. Does not leave channels or remove members. Type: boolean. | — | `--channel <value>` — Channel name or ID. Provide exactly one of channel or dm. Type: string. Choose one input alternative: { channel } \| { dm }.<br>`--dm <value>` — Direct-message conversation ID, or a one-to-one participant display name. Provide exactly one of channel or dm. Type: string. Choose one input alternative: { channel } \| { dm }. |
| `huly channels conversations starred set <starred>` | Set Conversation Starred | `<starred>` — True to star/pin this conversation for the authenticated user, false to unstar it. Type: boolean. | — | `--channel <value>` — Channel name or ID. Provide exactly one of channel or dm. Type: string. Choose one input alternative: { channel } \| { dm }.<br>`--dm <value>` — Direct-message conversation ID, or a one-to-one participant display name. Provide exactly one of channel or dm. Type: string. Choose one input alternative: { channel } \| { dm }. |
| `huly channels create <name>` | Create Channel | `<name>` — Channel name Type: string. | — | `--topic <value>` — Channel topic/description Type: string. Pass null to clear the field.<br>`--private` / `--no-private` — Whether channel is private (default: false) Type: boolean. Pass null to clear the field. |
| `huly channels delete <channel>` | Delete Channel Requires `--yes`. | `<channel>` — Channel name or ID Type: string. | — | — |
| `huly channels direct-messages create <person>` | Create Direct Message | `<person>` — Participant email address or exact display name used to open a one-to-one direct message. Type: string. | — | — |
| `huly channels direct-messages list` | List Direct Messages | — | — | `--limit <value>` — Maximum number of DM conversations to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly channels direct-messages messages delete <dm> <messageId>` | Delete a direct-message message Requires `--yes`. | `<dm>` — Direct-message conversation ID or participant display name. A name resolves only to a one-to-one DM with the authenticated account. Type: string.<br>`<messageId>` — Message ID to delete. Type: string. | — | — |
| `huly channels direct-messages messages list <dm>` | List Dm Messages | `<dm>` — Direct-message conversation ID or participant display name. A name resolves only to a one-to-one DM with the authenticated account. Type: string. | — | `--limit <value>` — Maximum number of messages to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly channels direct-messages messages send <dm> <body>` | Send a direct message | `<dm>` — Direct-message conversation ID or participant display name. A name resolves only to a one-to-one DM with the authenticated account. Type: string.<br>`<body>` — Message body in Markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--body-file <path>` — read body as text |
| `huly channels direct-messages messages update <dm> <messageId> <body>` | Update a direct-message message | `<dm>` — Direct-message conversation ID or participant display name. A name resolves only to a one-to-one DM with the authenticated account. Type: string.<br>`<messageId>` — Message ID to update. Type: string.<br>`<body>` — New message body in Markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--body-file <path>` — read body as text |
| `huly channels external-messages list <provider> <channel>` | List External Channel Messages | `<provider>` — External provider to read or assess. Allowed values: "gmail" \| "telegram". Type: string.<br>`<channel>` — Provider channel locator: exact stored value or stable Huly contact-channel ID. Type: string. | — | `--limit <value>` — Requested maximum message count (default: 50, max: 200). Type: integer. Pass null to clear the field. |
| `huly channels get <channel>` | Get Channel | `<channel>` — Channel name or ID Type: string. | — | — |
| `huly channels group-direct-messages create <people>` | Create Group Direct Message | `<people>` — At least two other workspace members to include in a group DM. Each entry accepts an exact email address or exact person display name. The authenticated account is included automatically. Type: array. Pass arrays or objects as JSON. | — | — |
| `huly channels join <channel>` | Join Channel Requires `--yes`. | `<channel>` — Channel name or ID whose archive state should change. Type: string. | — | — |
| `huly channels leave <channel>` | Leave Channel Requires `--yes`. | `<channel>` — Channel name or ID whose archive state should change. Type: string. | — | — |
| `huly channels list` | List Channels | — | — | `--name-search <value>` — Search channels by name substring (case-insensitive). Mutually exclusive with nameRegex. Type: string. Pass null to clear the field.<br>`--name-regex <value>` — Filter channels by name using Huly $regex. On the supported Postgres backend this is SQL SIMILAR TO, not JavaScript RegExp; matching is case-sensitive and the pattern must match the whole name: use '%' for any string (e.g., '%dev%' contains, 'dev%' prefix). Mutually exclusive with nameSearch; use nameSearch for simple substring matching. Type: string. Pass null to clear the field.<br>`--topic-search <value>` — Search channels by topic substring (case-insensitive) Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of channels to return (default: 50) Type: integer. Pass null to clear the field.<br>`--include-archived` / `--no-include-archived` — Include archived channels in results (default: false) Type: boolean. Pass null to clear the field. |
| `huly channels members add <channel> <members>` | Add Channel Members | `<channel>` — Channel name or ID whose members should change. Type: string.<br>`<members>` — Members to add or remove. Each entry may be an account UUID, exact email address, or exact person display name. Type: array. Pass arrays or objects as JSON. | — | — |
| `huly channels members list <channel>` | List Channel Members | `<channel>` — Channel name or ID whose members should be listed. Type: string. | — | — |
| `huly channels members remove <channel> <members>` | Remove Channel Members Requires `--yes`. | `<channel>` — Channel name or ID whose members should change. Type: string.<br>`<members>` — Members to add or remove. Each entry may be an account UUID, exact email address, or exact person display name. Type: array. Pass arrays or objects as JSON. | — | — |
| `huly channels messages attachments add` | Attach a file to a channel, direct-message, or thread message; pass --target as JSON. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | — | `--target <value>` — Chat attachment target. Use channel_message for a channel message, dm_message for a direct-message message, or thread_reply for a thread reply. Type: object. JSON shape alternatives: { kind, channel, messageId } \| { kind, dm, messageId } \| { kind, channel, messageId, replyId }. Pass arrays or objects as JSON.<br>`--filename <value>` — Name of the file to attach to the chat message or thread reply. Type: string.<br>`--content-type <value>` — MIME type of the file, such as image/png or application/pdf. Type: string. | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Optional attachment description. Use null on update to clear it. Type: string. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether the attachment should be pinned. Type: boolean. Pass null to clear the field.<br>`--description-file <path>` — read description as text<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly channels messages attachments delete <target> <attachmentId>` | Delete Chat Message Attachment Requires `--yes`. | `<target>` — Chat attachment target. Use channel_message for a channel message, dm_message for a direct-message message, or thread_reply for a thread reply. Type: object. JSON shape alternatives: { kind, channel, messageId } \| { kind, dm, messageId } \| { kind, channel, messageId, replyId }. Pass arrays or objects as JSON.<br>`<attachmentId>` — Attachment ID. Must belong directly to the resolved chat message target. Type: string. | — | — |
| `huly channels messages attachments get <target> <attachmentId>` | Get Chat Message Attachment | `<target>` — Chat attachment target. Use channel_message for a channel message, dm_message for a direct-message message, or thread_reply for a thread reply. Type: object. JSON shape alternatives: { kind, channel, messageId } \| { kind, dm, messageId } \| { kind, channel, messageId, replyId }. Pass arrays or objects as JSON.<br>`<attachmentId>` — Attachment ID. Must belong directly to the resolved chat message target. Type: string. | — | — |
| `huly channels messages attachments list <target>` | List Chat Message Attachments | `<target>` — Chat attachment target. Use channel_message for a channel message, dm_message for a direct-message message, or thread_reply for a thread reply. Type: object. JSON shape alternatives: { kind, channel, messageId } \| { kind, dm, messageId } \| { kind, channel, messageId, replyId }. Pass arrays or objects as JSON. | — | `--limit <value>` — Maximum number of matching attachments to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly channels messages attachments update <target> <attachmentId>` | Update Chat Message Attachment | `<target>` — Chat attachment target. Use channel_message for a channel message, dm_message for a direct-message message, or thread_reply for a thread reply. Type: object. JSON shape alternatives: { kind, channel, messageId } \| { kind, dm, messageId } \| { kind, channel, messageId, replyId }. Pass arrays or objects as JSON.<br>`<attachmentId>` — Attachment ID. Must belong directly to the resolved chat message target. Type: string. | — | `--description <value>` — Optional attachment description. Use null on update to clear it. Type: string. Choose one input alternative: { description } \| { pinned }. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether the attachment should be pinned. Type: boolean. Choose one input alternative: { description } \| { pinned }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly channels messages delete <channel> <messageId>` | Delete a channel message Requires `--yes`. | `<channel>` — Channel name or ID Type: string.<br>`<messageId>` — Message ID to delete Type: string. | — | — |
| `huly channels messages list <channel>` | List Channel Messages | `<channel>` — Channel name or ID Type: string. | — | `--limit <value>` — Maximum number of messages to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly channels messages pinned list` | List Pinned Chat Messages | — | — | `--channel <value>` — Channel name or ID. Provide exactly one of channel or dm. Type: string. Choose one input alternative: { channel } \| { dm }.<br>`--dm <value>` — Direct-message conversation ID, or a one-to-one participant display name. Provide exactly one of channel or dm. Type: string. Choose one input alternative: { channel } \| { dm }.<br>`--limit <value>` — Maximum pinned messages to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly channels messages pinned set <messageId> <pinned>` | Set Chat Message Pinned | `<messageId>` — Message or thread-reply ID to pin or unpin. Type: string.<br>`<pinned>` — True to pin the message, false to unpin it. Type: boolean. | — | `--channel <value>` — Channel name or ID. Provide exactly one of channel or dm. Type: string. Choose one input alternative: { channel } \| { dm }.<br>`--dm <value>` — Direct-message conversation ID, or a one-to-one participant display name. Provide exactly one of channel or dm. Type: string. Choose one input alternative: { channel } \| { dm }. |
| `huly channels messages send <channel> <body>` | Send a message to a channel | `<channel>` — Channel name or ID Type: string.<br>`<body>` — Message body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--body-file <path>` — read body as text |
| `huly channels messages translate <messageId> <targetLanguage>` | Translate Chat Message Requires `--yes`. | `<messageId>` — Message or thread-reply ID to translate. Type: string.<br>`<targetLanguage>` — Requested non-empty translation language value, such as `French`, `fr`, or `fr-CA`. Type: string. | — | `--channel <value>` — Channel name or ID. Provide exactly one of channel or dm. Type: string. Choose one input alternative: { channel } \| { dm }.<br>`--dm <value>` — Direct-message conversation ID, or a one-to-one participant display name. Provide exactly one of channel or dm. Type: string. Choose one input alternative: { channel } \| { dm }. |
| `huly channels messages update <channel> <messageId> <body>` | Update a channel message | `<channel>` — Channel name or ID Type: string.<br>`<messageId>` — Message ID to update Type: string.<br>`<body>` — New message body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--body-file <path>` — read body as text |
| `huly channels threads replies add <channel> <messageId> <body>` | Reply to a channel message | `<channel>` — Channel name or ID Type: string.<br>`<messageId>` — Parent message ID to reply to Type: string.<br>`<body>` — Reply body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--body-file <path>` — read body as text |
| `huly channels threads replies delete <channel> <messageId> <replyId>` | Delete a thread reply Requires `--yes`. | `<channel>` — Channel name or ID Type: string.<br>`<messageId>` — Parent message ID Type: string.<br>`<replyId>` — Thread reply ID to delete Type: string. | — | — |
| `huly channels threads replies list <channel> <messageId>` | List Thread Replies | `<channel>` — Channel name or ID Type: string.<br>`<messageId>` — Parent message ID Type: string. | — | `--limit <value>` — Maximum number of replies to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly channels threads replies update <channel> <messageId> <replyId> <body>` | Update a thread reply | `<channel>` — Channel name or ID Type: string.<br>`<messageId>` — Parent message ID Type: string.<br>`<replyId>` — Thread reply ID to update Type: string.<br>`<body>` — New reply body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--body-file <path>` — read body as text |
| `huly channels unarchive <channel>` | Unarchive Channel Requires `--yes`. | `<channel>` — Channel name or ID whose archive state should change. Type: string. | — | — |
| `huly channels update <channel>` | Update Channel | `<channel>` — Channel name or ID Type: string. | — | `--name <value>` — New channel name Type: string. Choose one input alternative: { name } \| { topic }. Pass null to clear the field.<br>`--topic <value>` — New channel topic. Pass null to clear; empty string is also accepted. Type: string. Choose one input alternative: { name } \| { topic }. Pass null to clear the field. |
| `huly collaborators object list` | List Object Collaborators | — | — | `--object-id <value>` — Advanced: internal Huly object ID. Use with objectClass. Type: string. Pass null to clear the field.<br>`--object-class <value>` — Advanced: internal Huly object class for objectId. Type: string. Pass null to clear the field.<br>`--project <value>` — Project identifier for issue target, e.g. 'HULY'. Use with issueIdentifier. Type: string. Pass null to clear the field.<br>`--issue-identifier <value>` — Issue identifier, e.g. 'HULY-123' or '123'. Use with project. Type: string. Pass null to clear the field.<br>`--teamspace <value>` — Teamspace name or ID for document target. Use with document. Type: string. Pass null to clear the field.<br>`--document <value>` — Document title or ID for document target. Use with teamspace. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of collaborators to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly comments add` | Add an issue comment | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string.<br>`--issue-identifier <value>` — Issue identifier (e.g., 'HULY-123' or just '123') Type: string.<br>`--body <value>` — Comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | `--body-file <path>` — read body as text |
| `huly comments delete` | Delete an issue comment Requires `--yes`. | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string.<br>`--issue-identifier <value>` — Issue identifier (e.g., 'HULY-123' or just '123') Type: string.<br>`--comment-id <value>` — Comment ID to delete Type: string. | — |
| `huly comments list` | List issue comments | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string.<br>`--issue-identifier <value>` — Issue identifier (e.g., 'HULY-123' or just '123') Type: string. | `--limit <value>` — Maximum number of comments to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly comments update` | Update an issue comment | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string.<br>`--issue-identifier <value>` — Issue identifier (e.g., 'HULY-123' or just '123') Type: string.<br>`--comment-id <value>` — Comment ID to update Type: string.<br>`--body <value>` — New comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | `--body-file <path>` — read body as text |
| `huly components create <project> <label>` | Create Component | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<label>` — Component name/label Type: string. | — | `--description <value>` — Component description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--lead <value>` — Lead person email address or display name Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly components delete <project> <component>` | Delete Component Requires `--yes`. | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<component>` — Component ID or label Type: string. | — | — |
| `huly components get` | Get a component | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string.<br>`--component <value>` — Component ID or label Type: string. | — |
| `huly components list` | List components | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string. | `--limit <value>` — Maximum number of components to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly components update <project> <component>` | Update Component | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<component>` — Component ID or label Type: string. | — | `--label <value>` — New component name/label Type: string. Choose one input alternative: { label } \| { description } \| { lead }. Pass null to clear the field.<br>`--description <value>` — New component description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Type: string. Choose one input alternative: { label } \| { description } \| { lead }. Pass null to clear the field.<br>`--lead <value>` — New lead person email or display name (null to unassign) Type: string. Choose one input alternative: { label } \| { description } \| { lead }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly contacts channel-providers list` | List Contact Channel Providers | — | — | — |
| `huly contacts employees list` | List Employees | — | — | `--limit <value>` — Maximum number of employees to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly contacts organizations channels add <provider> <value> <organizationId>` | Add Organization Channel | `<provider>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber. Allowed values: "email" \| "phone" \| "linkedin" \| "twitter" \| "github" \| "facebook" \| "telegram" \| "homepage" \| "whatsapp" \| "skype" \| "profile" \| "viber". Type: string.<br>`<value>` — Contact channel value. Type: string.<br>`<organizationId>` — Organization ID or exact unique organization name. Type: string. | — | — |
| `huly contacts organizations channels list <organizationId>` | List Organization Channels | `<organizationId>` — Organization ID or exact unique organization name. Type: string. | — | — |
| `huly contacts organizations channels remove <organizationId>` | Remove Organization Channel Requires `--yes`. | `<organizationId>` — Organization ID or exact unique organization name. Type: string. | — | `--channel-id <value>` — Raw channel document ID returned by list/get/add channel tools. Type: string. Pass null to clear the field.<br>`--provider <value>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber. Allowed values: "email" \| "phone" \| "linkedin" \| "twitter" \| "github" \| "facebook" \| "telegram" \| "homepage" \| "whatsapp" \| "skype" \| "profile" \| "viber". Type: string. Pass null to clear the field.<br>`--value <value>` — Contact channel value. Type: string. Pass null to clear the field. |
| `huly contacts organizations channels update <organizationId>` | Update Organization Channel | `<organizationId>` — Organization ID or exact unique organization name. Type: string. | — | `--channel-id <value>` — Raw channel document ID returned by list/get/add channel tools. Type: string. Pass null to clear the field.<br>`--provider <value>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber. Allowed values: "email" \| "phone" \| "linkedin" \| "twitter" \| "github" \| "facebook" \| "telegram" \| "homepage" \| "whatsapp" \| "skype" \| "profile" \| "viber". Type: string. Pass null to clear the field.<br>`--value <value>` — Contact channel value. Type: string. Pass null to clear the field.<br>`--new-provider <value>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber. Allowed values: "email" \| "phone" \| "linkedin" \| "twitter" \| "github" \| "facebook" \| "telegram" \| "homepage" \| "whatsapp" \| "skype" \| "profile" \| "viber". Type: string. Choose one input alternative: { newProvider } \| { newValue }. Pass null to clear the field.<br>`--new-value <value>` — Replacement channel value. Type: string. Choose one input alternative: { newProvider } \| { newValue }. Pass null to clear the field. |
| `huly contacts organizations create <name>` | Create Organization | `<name>` — Organization name Type: string. | — | `--members <value>` — Member person IDs or emails Type: array. Pass arrays or objects as JSON. Pass null to clear the field. |
| `huly contacts organizations customer make <identifier>` | Make Organization Customer Requires `--yes`. | `<identifier>` — Organization ID or exact name Type: string. | — | — |
| `huly contacts organizations delete <identifier>` | Delete Organization Requires `--yes`. | `<identifier>` — Organization ID or exact name Type: string. | — | — |
| `huly contacts organizations get <identifier>` | Get Organization | `<identifier>` — Organization ID or exact name Type: string. | — | — |
| `huly contacts organizations list` | List Organizations | — | — | `--limit <value>` — Maximum number of organizations to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly contacts organizations members add <organizationId> <personIdentifier>` | Add Organization Member | `<organizationId>` — Organization ID or exact name Type: string.<br>`<personIdentifier>` — Person ID or email address Type: string. | — | — |
| `huly contacts organizations members list <organizationId>` | List Organization Members | `<organizationId>` — Organization ID or exact name Type: string. | — | — |
| `huly contacts organizations members remove <organizationId> <personIdentifier>` | Remove Organization Member Requires `--yes`. | `<organizationId>` — Organization ID or exact name Type: string.<br>`<personIdentifier>` — Person ID or email address Type: string. | — | — |
| `huly contacts organizations update <identifier>` | Update Organization | `<identifier>` — Organization ID or exact name Type: string. | — | `--name <value>` — Organization name Type: string. Choose one input alternative: { name } \| { city } \| { description }. Pass null to clear the field.<br>`--city <value>` — New city (null to clear) Type: string. Choose one input alternative: { name } \| { city } \| { description }. Pass null to clear the field.<br>`--description <value>` — New description/notes in markdown, or null to clear. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Choose one input alternative: { name } \| { city } \| { description }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly contacts persons channels add <provider> <value> <person>` | Add Person Channel | `<provider>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber. Allowed values: "email" \| "phone" \| "linkedin" \| "twitter" \| "github" \| "facebook" \| "telegram" \| "homepage" \| "whatsapp" \| "skype" \| "profile" \| "viber". Type: string.<br>`<value>` — Contact channel value. Type: string.<br>`<person>` — Person ID, exact email address, or exact Huly display name. Type: string. | — | — |
| `huly contacts persons channels list <person>` | List Person Channels | `<person>` — Person ID, exact email address, or exact Huly display name. Type: string. | — | — |
| `huly contacts persons channels remove <person>` | Remove Person Channel Requires `--yes`. | `<person>` — Person ID, exact email address, or exact Huly display name. Type: string. | — | `--channel-id <value>` — Raw channel document ID returned by list/get/add channel tools. Type: string. Pass null to clear the field.<br>`--provider <value>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber. Allowed values: "email" \| "phone" \| "linkedin" \| "twitter" \| "github" \| "facebook" \| "telegram" \| "homepage" \| "whatsapp" \| "skype" \| "profile" \| "viber". Type: string. Pass null to clear the field.<br>`--value <value>` — Contact channel value. Type: string. Pass null to clear the field. |
| `huly contacts persons channels update <person>` | Update Person Channel | `<person>` — Person ID, exact email address, or exact Huly display name. Type: string. | — | `--channel-id <value>` — Raw channel document ID returned by list/get/add channel tools. Type: string. Pass null to clear the field.<br>`--provider <value>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber. Allowed values: "email" \| "phone" \| "linkedin" \| "twitter" \| "github" \| "facebook" \| "telegram" \| "homepage" \| "whatsapp" \| "skype" \| "profile" \| "viber". Type: string. Pass null to clear the field.<br>`--value <value>` — Contact channel value. Type: string. Pass null to clear the field.<br>`--new-provider <value>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber. Allowed values: "email" \| "phone" \| "linkedin" \| "twitter" \| "github" \| "facebook" \| "telegram" \| "homepage" \| "whatsapp" \| "skype" \| "profile" \| "viber". Type: string. Choose one input alternative: { newProvider } \| { newValue }. Pass null to clear the field.<br>`--new-value <value>` — Replacement channel value. Type: string. Choose one input alternative: { newProvider } \| { newValue }. Pass null to clear the field. |
| `huly contacts persons create <firstName> <lastName>` | Create Person | `<firstName>` — First name Type: string.<br>`<lastName>` — Last name Type: string. | — | `--email <value>` — Email address Type: string. Pattern: ^[^@]+@[^@]+$. Pass null to clear the field.<br>`--city <value>` — City Type: string. Pass null to clear the field. |
| `huly contacts persons delete <personId>` | Delete Person Requires `--yes`. | `<personId>` — Person ID Type: string. | — | — |
| `huly contacts persons get` | Get Person | — | — | `--person-id <value>` — Person ID Type: string. Choose one input alternative: { personId } \| { email }.<br>`--email <value>` — Person email address Type: string. Pattern: ^[^@]+@[^@]+$. Choose one input alternative: { personId } \| { email }. |
| `huly contacts persons list` | List Persons | — | — | `--name-search <value>` — Search persons by name substring (case-insensitive). Mutually exclusive with nameRegex. Type: string. Pass null to clear the field.<br>`--name-regex <value>` — Filter persons by name using Huly $regex. Mutually exclusive with nameSearch. Type: string. Pass null to clear the field.<br>`--email-search <value>` — Search persons by email substring (case-insensitive) Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of persons to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly contacts persons organizations list` | List Person Organizations | — | — | `--person-id <value>` — Person ID Type: string. Choose one input alternative: { personId } \| { email }.<br>`--email <value>` — Person email address Type: string. Pattern: ^[^@]+@[^@]+$. Choose one input alternative: { personId } \| { email }. |
| `huly contacts persons update <personId>` | Update Person | `<personId>` — Person ID Type: string. | — | `--first-name <value>` — New first name Type: string. Choose one input alternative: { firstName } \| { lastName } \| { city }. Pass null to clear the field.<br>`--last-name <value>` — New last name Type: string. Choose one input alternative: { firstName } \| { lastName } \| { city }. Pass null to clear the field.<br>`--city <value>` — New city (null to clear) Type: string. Choose one input alternative: { firstName } \| { lastName } \| { city }. Pass null to clear the field. |
| `huly custom-fields list` | List Custom Fields | — | — | `--target-class <value>` — Filter by owner class or mixin ID. Returns fields defined directly on that class or mixin only. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of fields to return (default: 200). Type: integer. Pass null to clear the field. |
| `huly custom-fields values get <objectId> <objectClass>` | Get Custom Field Values | `<objectId>` — Document ID to read custom field values from. Type: string.<br>`<objectClass>` — Document class ID, such as tracker:class:Issue or card:class:Card. Type: string. | — | — |
| `huly deletion preview <entityType> <project>` | Preview Deletion | `<entityType>` — Type of entity: issue, project, component, milestone. Allowed values: "issue" \| "project" \| "component" \| "milestone". Type: string.<br>`<project>` — Project identifier. For entityType=project, this is the target project. Type: string. | — | `--identifier <value>` — Entity identifier within the project. Required for issues, components, and milestones; ignored for projects. Type: string. Pass null to clear the field. |
| `huly documents comments` | List document inline comments | — | `--teamspace <value>` — Teamspace name or ID Type: string.<br>`--document <value>` — Document title or ID Type: string. | `--include-replies` / `--no-include-replies` — Include thread reply messages for each inline comment (default: false) Type: boolean. Pass null to clear the field. |
| `huly documents create` | Create a document | — | `--teamspace <value>` — Teamspace name or ID Type: string.<br>`--title <value>` — Document title Type: string. | `--content <value>` — Document content in markdown. Use markdown links to current-workspace Huly browse URLs for native references; Huly browse links returned in get_document content round-trip as native references. The URL identifies the object; link text is display text; plain issue keys stay text. External URLs stay normal markdown links. Type: string. Pass null to clear the field.<br>`--parent <value>` — Parent document title or ID to nest this document under. If omitted, creates a top-level document. Type: string. Pass null to clear the field.<br>`--content-file <path>` — read content as text |
| `huly documents delete <teamspace> <document>` | Delete Document Requires `--yes`. | `<teamspace>` — Teamspace name or ID Type: string.<br>`<document>` — Document title or ID Type: string. | — | — |
| `huly documents edit` | Edit a document | — | `--teamspace <value>` — Type: string.<br>`--document <value>` — Type: string. | `--title <value>` — Type: string. Choose one input alternative: { title } \| { content } \| { old_text, new_text }. Pass null to clear the field.<br>`--content <value>` — Full replacement content in markdown. Mutually exclusive with old_text/new_text. Use markdown links to current-workspace Huly browse URLs for native references; Huly browse links returned in get_document content round-trip as native references. The URL identifies the object; link text is display text; plain issue keys stay text. External URLs stay normal markdown links. Type: string. Choose one input alternative: { title } \| { content } \| { old_text, new_text }. Pass null to clear the field.<br>`--old-text <value>` — Exact text to find in the document. Must be non-empty. Mutually exclusive with content. Type: string. Choose one input alternative: { title } \| { content } \| { old_text, new_text }. Pass null to clear the field.<br>`--new-text <value>` — Replacement text. Empty string deletes the matched text. Required when old_text is provided. Type: string. Choose one input alternative: { title } \| { content } \| { old_text, new_text }. Pass null to clear the field.<br>`--replace-all` / `--no-replace-all` — Replace all occurrences of old_text (default: false). Only used with old_text/new_text. Type: boolean. Pass null to clear the field.<br>`--content-file <path>` — read content as text<br>`--old-text-file <path>` — read old_text as text<br>`--new-text-file <path>` — read new_text as text |
| `huly documents get` | Get a document | — | `--teamspace <value>` — Teamspace name or ID Type: string.<br>`--document <value>` — Document title or ID Type: string. | — |
| `huly documents labels add <teamspace> <document> <label>` | Add Document Label | `<teamspace>` — Teamspace name or ID containing the document. Type: string.<br>`<document>` — Document title or ID within the teamspace. Type: string.<br>`<label>` — Label TagElement _id or exact title. A missing title creates the module label definition. Type: string. | — | `--color <value>` — Color for a newly created label definition from 0 through 23 (default: 0). Ignored for an existing label. Type: integer. Pass null to clear the field. |
| `huly documents labels definitions list` | List Document Label Definitions | — | — | `--title-search <value>` — Optional label title substring search. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of label definitions to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly documents labels list <teamspace> <document>` | List Document Labels | `<teamspace>` — Teamspace name or ID containing the document. Type: string.<br>`<document>` — Document title or ID within the teamspace. Type: string. | — | — |
| `huly documents labels remove <teamspace> <document> <label>` | Remove Document Label Requires `--yes`. | `<teamspace>` — Teamspace name or ID containing the document. Type: string.<br>`<document>` — Document title or ID within the teamspace. Type: string.<br>`<label>` — Label TagElement _id or exact title. Type: string. | — | — |
| `huly documents list` | List documents | — | `--teamspace <value>` — Teamspace name or ID Type: string. | `--title-search <value>` — Search documents by title substring (case-insensitive). Mutually exclusive with titleRegex. Type: string. Pass null to clear the field.<br>`--title-regex <value>` — Filter documents by title using Huly $regex. On the supported Postgres backend this is SQL SIMILAR TO, not JavaScript RegExp; matching is case-sensitive and the pattern must match the whole title: use '%' for any string (e.g., '%RFC%' contains, 'RFC%' prefix). Mutually exclusive with titleSearch; use titleSearch for simple substring matching. Type: string. Pass null to clear the field.<br>`--content-search <value>` — Search documents by content (fulltext search) Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of documents to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly documents snapshots get <teamspace> <document> <snapshot>` | Get Document Snapshot | `<teamspace>` — Document teamspace name or ID. Type: string.<br>`<document>` — Document title or ID within the teamspace. Type: string.<br>`<snapshot>` — Snapshot ID, exact snapshot title, or exact createdOn timestamp in milliseconds. Use list_document_snapshots first when unsure. Type: string. | — | — |
| `huly documents snapshots list <teamspace> <document>` | List Document Snapshots | `<teamspace>` — Document teamspace name or ID. Type: string.<br>`<document>` — Document title or ID within the teamspace. Type: string. | — | `--limit <value>` — Maximum number of snapshots to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly drawings create <parentId> <parentClass> <space>` | Create a drawing; pass opaque content with --content or --content-file | `<parentId>` — Internal Huly parent object ID. Type: string.<br>`<parentClass>` — Internal Huly parent object class. Type: string.<br>`<space>` — Space ID where the drawing should be created. Type: string. | — | `--content <value>` — Opaque drawing content payload stored by Huly. Type: string. Pass null to clear the field.<br>`--content-file <path>` — read content as text |
| `huly drawings delete <drawingId>` | Delete Drawing Requires `--yes`. | `<drawingId>` — Drawing ID to delete. Type: string. | — | — |
| `huly drawings get <drawingId>` | Get Drawing | `<drawingId>` — Drawing ID. Type: string. | — | — |
| `huly drawings list <parentId> <parentClass>` | List Drawings | `<parentId>` — Internal Huly parent object ID. Type: string.<br>`<parentClass>` — Internal Huly parent object class. Type: string. | — | `--limit <value>` — Maximum number of drawings to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly drawings update <drawingId>` | Update drawing content; pass null to --content to clear it | `<drawingId>` — Drawing ID. Type: string. | `--content <value>` — New drawing content payload. Use null to clear. Type: string. Pass null to clear the field. | `--content-file <path>` — read content as text |
| `huly drive create <name>` | Create Drive | `<name>` — Drive name. If an active Drive already has this name, it is returned unchanged. Type: string. | — | `--description <value>` — Plain-text Drive description. Type: string. Pass null to clear the field.<br>`--private` / `--no-private` — Whether the Drive is private. Defaults to false. Type: boolean. Pass null to clear the field.<br>`--auto-join` / `--no-auto-join` — Whether workspace members should auto-join the Drive. Defaults to false. Type: boolean. Pass null to clear the field.<br>`--members <value>` — Initial Drive members. Each entry may be an account UUID, exact email address, or exact person name. When omitted, the caller is added. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--owners <value>` — Initial Drive owners. Each entry may be an account UUID, exact email address, or exact person name. When omitted, the caller is the owner. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly drive delete <drive>` | Delete Drive Requires `--yes`. | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string. | — | — |
| `huly drive files activity list <drive>` | List Drive File Activity | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string. | — | `--file-path <value>` — Exact Drive file path, such as '/Specs/API.md'. Mutually exclusive with fileId. Type: string. Choose one input alternative: { filePath } \| { fileId }. Pass null to clear the field.<br>`--file-id <value>` — Exact Drive file id. Mutually exclusive with filePath. Type: string. Choose one input alternative: { filePath } \| { fileId }. Pass null to clear the field.<br>`--limit <value>` — Maximum number of activity messages to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly drive files comments add <drive> <body>` | Add Drive File Comment | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string.<br>`<body>` — Comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--file-path <value>` — Exact Drive file path, such as '/Specs/API.md'. Mutually exclusive with fileId. Type: string. Choose one input alternative: { filePath } \| { fileId }. Pass null to clear the field.<br>`--file-id <value>` — Exact Drive file id. Mutually exclusive with filePath. Type: string. Choose one input alternative: { filePath } \| { fileId }. Pass null to clear the field.<br>`--body-file <path>` — read body as text |
| `huly drive files comments delete <drive> <commentId>` | Delete Drive File Comment Requires `--yes`. | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string.<br>`<commentId>` — Drive file comment id to permanently delete. Type: string. | — | `--file-path <value>` — Exact Drive file path, such as '/Specs/API.md'. Mutually exclusive with fileId. Type: string. Choose one input alternative: { filePath } \| { fileId }. Pass null to clear the field.<br>`--file-id <value>` — Exact Drive file id. Mutually exclusive with filePath. Type: string. Choose one input alternative: { filePath } \| { fileId }. Pass null to clear the field. |
| `huly drive files comments list <drive>` | List Drive File Comments | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string. | — | `--file-path <value>` — Exact Drive file path, such as '/Specs/API.md'. Mutually exclusive with fileId. Type: string. Choose one input alternative: { filePath } \| { fileId }. Pass null to clear the field.<br>`--file-id <value>` — Exact Drive file id. Mutually exclusive with filePath. Type: string. Choose one input alternative: { filePath } \| { fileId }. Pass null to clear the field.<br>`--limit <value>` — Maximum number of comments to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly drive files comments update <drive> <commentId> <body>` | Update Drive File Comment | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string.<br>`<commentId>` — Drive file comment id to update. Type: string.<br>`<body>` — New comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--file-path <value>` — Exact Drive file path, such as '/Specs/API.md'. Mutually exclusive with fileId. Type: string. Choose one input alternative: { filePath } \| { fileId }. Pass null to clear the field.<br>`--file-id <value>` — Exact Drive file id. Mutually exclusive with filePath. Type: string. Choose one input alternative: { filePath } \| { fileId }. Pass null to clear the field.<br>`--body-file <path>` — read body as text |
| `huly drive files upload <drive> <path> <contentType>` | Upload a drive file. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string.<br>`<path>` — Full Drive file path including filename, for example '/Specs/API.md'. Type: string.<br>`<contentType>` — MIME type of the file, for example 'text/plain' or 'application/pdf'. Type: string. | — | `--create-parents` / `--no-create-parents` — Create missing parent folders automatically. Defaults to true. Type: boolean. Pass null to clear the field.<br>`--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly drive files versions list <drive> <file>` | List Drive File Versions | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string.<br>`<file>` — Drive file id or Drive file path. Type: string. | — | — |
| `huly drive files versions restore <drive> <file> <version>` | Restore Drive File Version | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string.<br>`<file>` — Drive file id or Drive file path. Type: string.<br>`<version>` — Drive file version id or numeric version string such as '1'. Type: string. | — | — |
| `huly drive files versions upload <drive> <file> <contentType>` | Upload a new drive-file version. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string.<br>`<file>` — Drive file id or Drive file path. Type: string.<br>`<contentType>` — MIME type of the new version, for example 'text/plain' or 'application/pdf'. Type: string. | — | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly drive folders create <drive> <path>` | Create Drive Folder | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string.<br>`<path>` — Folder path to create. Missing parents are created, like mkdir -p. Type: string. | — | — |
| `huly drive get <drive>` | Get Drive | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string. | — | — |
| `huly drive items delete <drive>` | Delete Drive Item Requires `--yes`. | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string. | — | `--path <value>` — POSIX-like Drive path. Absolute paths such as '/Specs/API.md' are preferred; relative paths are normalized under '/'. Type: string. Choose one input alternative: { path } \| { itemId }. Pass null to clear the field.<br>`--item-id <value>` — Exact Drive folder or file id. Mutually exclusive with path. Type: string. Choose one input alternative: { path } \| { itemId }. Pass null to clear the field. |
| `huly drive items get <drive>` | Get Drive Item | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string. | — | `--path <value>` — POSIX-like Drive path. Absolute paths such as '/Specs/API.md' are preferred; relative paths are normalized under '/'. Type: string. Choose one input alternative: { path } \| { itemId }. Pass null to clear the field.<br>`--item-id <value>` — Exact Drive folder or file id. Mutually exclusive with path. Type: string. Choose one input alternative: { path } \| { itemId }. Pass null to clear the field. |
| `huly drive items list <drive>` | List Drive Items | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string. | — | `--path <value>` — Folder path to list. Defaults to /. Type: string. Pass null to clear the field.<br>`--kind <value>` — Filter returned children by kind. Defaults to any. Allowed values: "any" \| "folder" \| "file". Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum drive items to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly drive items move <drive> <targetFolderPath>` | Move Drive Item | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string.<br>`<targetFolderPath>` — Existing destination folder path in the same Drive. The item keeps its current title. Type: string. | — | `--path <value>` — POSIX-like Drive path. Absolute paths such as '/Specs/API.md' are preferred; relative paths are normalized under '/'. Type: string. Choose one input alternative: { path } \| { itemId }. Pass null to clear the field.<br>`--item-id <value>` — Exact Drive folder or file id. Mutually exclusive with path. Type: string. Choose one input alternative: { path } \| { itemId }. Pass null to clear the field. |
| `huly drive items rename <drive> <title>` | Rename Drive Item | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string.<br>`<title>` — New file or folder title. Do not include path separators; use move_drive_item to change folders. Type: string. | — | `--path <value>` — POSIX-like Drive path. Absolute paths such as '/Specs/API.md' are preferred; relative paths are normalized under '/'. Type: string. Choose one input alternative: { path } \| { itemId }. Pass null to clear the field.<br>`--item-id <value>` — Exact Drive folder or file id. Mutually exclusive with path. Type: string. Choose one input alternative: { path } \| { itemId }. Pass null to clear the field. |
| `huly drive list` | List Drives | — | — | `--query <value>` — Case-insensitive substring to filter Drive names after listing. Type: string. Pass null to clear the field.<br>`--include-archived` / `--no-include-archived` — Include archived Drives. Defaults to false. Type: boolean. Pass null to clear the field.<br>`--limit <value>` — Maximum drives to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly drive members add <drive> <members>` | Add drive members; members is a JSON array Requires `--yes`. | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string.<br>`<members>` — Members to add or remove. Each entry may be an account UUID, exact email address, or exact person name. Type: array. Pass arrays or objects as JSON. | — | — |
| `huly drive members remove <drive> <members>` | Remove drive members; members is a JSON array Requires `--yes`. | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string.<br>`<members>` — Members to add or remove. Each entry may be an account UUID, exact email address, or exact person name. Type: array. Pass arrays or objects as JSON. | — | — |
| `huly drive owners set <drive> <owners>` | Replace drive owners; owners is a JSON array Requires `--yes`. | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string.<br>`<owners>` — Replacement Drive owner list. Each entry may be an account UUID, exact email address, or exact person name. Pass [] to clear owners. Type: array. Pass arrays or objects as JSON. | — | `--ensure-members` / `--no-ensure-members` — Also add each owner to Drive members. Defaults to true. Type: boolean. Pass null to clear the field. |
| `huly drive update <drive>` | Update Drive | `<drive>` — Exact Drive id or exact Drive name. Use list_drives first when unsure. Type: string. | — | `--name <value>` — New Drive name. Type: string. Choose one input alternative: { name } \| { description } \| { private } \| { archived } \| { autoJoin }. Pass null to clear the field.<br>`--description <value>` — New plain-text Drive description. Pass null to clear; empty string is also accepted. Type: string. Choose one input alternative: { name } \| { description } \| { private } \| { archived } \| { autoJoin }. Pass null to clear the field.<br>`--private` / `--no-private` — Whether the Drive is private. Type: boolean. Choose one input alternative: { name } \| { description } \| { private } \| { archived } \| { autoJoin }. Pass null to clear the field.<br>`--archived` / `--no-archived` — Whether the Drive is archived. Type: boolean. Choose one input alternative: { name } \| { description } \| { private } \| { archived } \| { autoJoin }. Pass null to clear the field.<br>`--auto-join` / `--no-auto-join` — Whether workspace members should auto-join the Drive. Type: boolean. Choose one input alternative: { name } \| { description } \| { private } \| { archived } \| { autoJoin }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly inventory categories list` | List Inventory Categories | — | — | `--query <value>` — Case-insensitive substring filter for category names. Type: string. Pass null to clear the field.<br>`--parent-category <value>` — Optional parent category scope. Use a category ID, exact category name, 'root', or 'inventory:global:Category'. Omit to search all categories. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of categories to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly inventory category create <name>` | Create Inventory Category | `<name>` — New category name. Type: string. | — | `--parent-category <value>` — Parent category ID or exact name. Defaults to the Inventory root category. Type: string. Pass null to clear the field. |
| `huly inventory category delete <category>` | Delete Inventory Category Requires `--yes`. | `<category>` — Category ID or exact category name. Name lookup must be unambiguous; pass parentCategory when duplicate names may exist. Type: string. | — | `--parent-category <value>` — Optional exact parent scope for category name lookup. Type: string. Pass null to clear the field. |
| `huly inventory category get <category>` | Get Inventory Category | `<category>` — Category ID or exact category name. Name lookup must be unambiguous; pass parentCategory when duplicate names may exist. Type: string. | — | `--parent-category <value>` — Optional exact parent scope for category name lookup. Type: string. Pass null to clear the field. |
| `huly inventory category update <category>` | Update Inventory Category | `<category>` — Category ID or exact name to update. Name lookup must be unambiguous; pass parentCategory when needed. Type: string. | — | `--parent-category <value>` — Optional current parent scope for category name lookup. Type: string. Pass null to clear the field.<br>`--name <value>` — New category name. Type: string. Choose one input alternative: { name } \| { newParentCategory }. Pass null to clear the field.<br>`--new-parent-category <value>` — New parent category ID or exact name; use 'root' to move to the Inventory root. Type: string. Choose one input alternative: { name } \| { newParentCategory }. Pass null to clear the field. |
| `huly inventory product activity list <product>` | List Inventory Product Activity | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of matching rows to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly inventory product attachment delete <product> <attachmentId>` | Delete Inventory Product Attachment Requires `--yes`. | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string.<br>`<attachmentId>` — Product attachment ID. Must belong directly to the resolved inventory product. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field. |
| `huly inventory product attachment get <product> <attachmentId>` | Get Inventory Product Attachment | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string.<br>`<attachmentId>` — Product attachment ID. Must belong directly to the resolved inventory product. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field. |
| `huly inventory product attachment update <product> <attachmentId>` | Update Inventory Product Attachment | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string.<br>`<attachmentId>` — Product attachment ID. Must belong directly to the resolved inventory product. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field.<br>`--description <value>` — Optional media description. Use null on update to clear it. Type: string. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether the media item should be pinned. Type: boolean. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly inventory product attachments list <product>` | List Inventory Product Attachments | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of matching rows to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly inventory product comment add <product> <body>` | Add Inventory Product Comment | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string.<br>`<body>` — Comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field.<br>`--body-file <path>` — read body as text |
| `huly inventory product comment delete <product> <commentId>` | Delete Inventory Product Comment Requires `--yes`. | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string.<br>`<commentId>` — Product comment ID. Must belong directly to the resolved inventory product. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field. |
| `huly inventory product comment update <product> <commentId> <body>` | Update Inventory Product Comment | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string.<br>`<commentId>` — Product comment ID. Must belong directly to the resolved inventory product. Type: string.<br>`<body>` — Comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field.<br>`--body-file <path>` — read body as text |
| `huly inventory product comments list <product>` | List Inventory Product Comments | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of matching rows to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly inventory product create <name> <category>` | Create Inventory Product | `<name>` — New product name. Type: string.<br>`<category>` — Category ID or exact category name where the product will be created. Type: string. | — | — |
| `huly inventory product delete <product>` | Delete Inventory Product Requires `--yes`. | `<product>` — Product ID or exact product name. Name lookup must be unambiguous; pass category when needed. Type: string. | — | `--category <value>` — Optional exact category scope for product name lookup. Type: string. Pass null to clear the field. |
| `huly inventory product get <product>` | Get Inventory Product | `<product>` — Product ID or exact product name. Name lookup must be unambiguous; pass category when needed. Type: string. | — | `--category <value>` — Optional exact category scope for product name lookup. Type: string. Pass null to clear the field. |
| `huly inventory product photo delete <product> <photoId>` | Delete Inventory Product Photo Requires `--yes`. | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string.<br>`<photoId>` — Product photo ID. Must belong directly to the resolved inventory product. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field. |
| `huly inventory product photo get <product> <photoId>` | Get Inventory Product Photo | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string.<br>`<photoId>` — Product photo ID. Must belong directly to the resolved inventory product. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field. |
| `huly inventory product photo update <product> <photoId>` | Update Inventory Product Photo | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string.<br>`<photoId>` — Product photo ID. Must belong directly to the resolved inventory product. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field.<br>`--description <value>` — Optional media description. Use null on update to clear it. Type: string. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether the media item should be pinned. Type: boolean. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly inventory product photos list <product>` | List Inventory Product Photos | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of matching rows to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly inventory product update <product>` | Update Inventory Product | `<product>` — Product ID or exact product name to update. Name lookup must be unambiguous; pass category when needed. Type: string. | — | `--category <value>` — Optional current category scope for product name lookup. Type: string. Pass null to clear the field.<br>`--name <value>` — New product name. Type: string. Choose one input alternative: { name } \| { newCategory }. Pass null to clear the field.<br>`--new-category <value>` — New category ID or exact category name. Type: string. Choose one input alternative: { name } \| { newCategory }. Pass null to clear the field. |
| `huly inventory products attachments add <product> <filename> <contentType>` | Add a product attachment. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string.<br>`<filename>` — Name of the file to attach to the inventory product. Type: string.<br>`<contentType>` — MIME type of the file, such as image/png or application/pdf. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field.<br>`--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Optional media description. Use null on update to clear it. Type: string. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether the media item should be pinned. Type: boolean. Pass null to clear the field.<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly inventory products list` | List Inventory Products | — | — | `--query <value>` — Case-insensitive substring filter for product names. Type: string. Pass null to clear the field.<br>`--category <value>` — Optional category scope by ID or exact category name. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of products to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly inventory products photos add <product> <filename> <contentType>` | Add a product photo. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. Type: string.<br>`<filename>` — Name of the file to attach to the inventory product. Type: string.<br>`<contentType>` — MIME type of the file, such as image/png or application/pdf. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. Type: string. Pass null to clear the field.<br>`--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Optional media description. Use null on update to clear it. Type: string. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether the media item should be pinned. Type: boolean. Pass null to clear the field.<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly inventory variant create <product> <name> <sku>` | Create Inventory Variant | `<product>` — Product ID or exact product name where the variant will be created. Type: string.<br>`<name>` — New variant name. Type: string.<br>`<sku>` — New exact SKU. Type: string. | — | `--category <value>` — Optional category scope used to resolve product names. Type: string. Pass null to clear the field. |
| `huly inventory variant delete <variant>` | Delete Inventory Variant Requires `--yes`. | `<variant>` — Variant ID, exact variant name, or exact SKU. Name/SKU lookup must be unambiguous; pass product when needed. Type: string. | — | `--product <value>` — Optional exact product scope for variant name/SKU lookup. Type: string. Pass null to clear the field.<br>`--category <value>` — Optional category scope used to resolve product names. Type: string. Pass null to clear the field. |
| `huly inventory variant get <variant>` | Get Inventory Variant | `<variant>` — Variant ID, exact variant name, or exact SKU. Name/SKU lookup must be unambiguous; pass product when needed. Type: string. | — | `--product <value>` — Optional exact product scope for variant name/SKU lookup. Type: string. Pass null to clear the field.<br>`--category <value>` — Optional category scope used to resolve product names. Type: string. Pass null to clear the field. |
| `huly inventory variant update <variant>` | Update Inventory Variant | `<variant>` — Variant ID, exact variant name, or exact SKU to update. Pass product when needed. Type: string. | — | `--product <value>` — Optional exact product scope for variant name/SKU lookup. Type: string. Pass null to clear the field.<br>`--category <value>` — Optional category scope used to resolve product names. Type: string. Pass null to clear the field.<br>`--name <value>` — New variant name. Type: string. Choose one input alternative: { name } \| { sku }. Pass null to clear the field.<br>`--sku <value>` — New SKU. Type: string. Choose one input alternative: { name } \| { sku }. Pass null to clear the field. |
| `huly inventory variants list` | List Inventory Variants | — | — | `--query <value>` — Case-insensitive substring filter for variant names or SKUs. Type: string. Pass null to clear the field.<br>`--product <value>` — Optional product scope by ID or exact product name. Type: string. Pass null to clear the field.<br>`--category <value>` — Optional category scope used to resolve product names. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of variants to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly issue-statuses create <name> <category>` | Create Issue Status | `<name>` — Display name for the workflow status to add. Type: string.<br>`<category>` — Huly SDK task.statusCategory key: UnStarted, ToDo, Active, Won, Lost. Allowed values: "UnStarted" \| "ToDo" \| "Active" \| "Won" \| "Lost". Type: string. | — | `--project-type <value>` — Project type ID or display name. If omitted, uses the unambiguous Classic tracker project type. Type: string. Pass null to clear the field.<br>`--task-type <value>` — Optional task type ID or display name to scope the status to. If omitted, the status is added to every task type in the project type. Type: string. Pass null to clear the field. |
| `huly issues component set` | Set or clear an issue component | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string.<br>`--identifier <value>` — Issue identifier (e.g., 'HULY-123') Type: string.<br>`--component <value>` — Component ID or label (null to clear) Type: string. Pass null to clear the field. | — |
| `huly issues create` | Create an issue | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string.<br>`--title <value>` — Issue title Type: string. | `--description <value>` — Issue description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references. External URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--priority <value>` — Issue priority (urgent, high, medium, low, no-priority) Allowed values: "urgent" \| "high" \| "medium" \| "low" \| "no-priority". Type: string. Pass null to clear the field.<br>`--assignee <value>` — Assignee email, Person display name, or exact agent UserProfile title Type: string. Pass null to clear the field.<br>`--status <value>` — Initial status (uses project default if not specified) Type: string. Pass null to clear the field.<br>`--task-type <value>` — Issue/task type ID or display name. Resolved within the target project's project type; use list_task_types or get_project_type to discover valid values. If omitted, creates the default Issue type. Type: string. Pass null to clear the field.<br>`--parent-issue <value>` — Parent issue identifier (e.g., 'HULY-42') to create as a sub-issue. Omit to create a native top-level issue. Type: string. Pass null to clear the field.<br>`--due-date <value>` — Due date as Unix timestamp in milliseconds (e.g., 1719792000000 for 2024-07-01), or null to clear Type: integer. Pass null to clear the field.<br>`--estimation <value>` — Time estimation in hours (Huly native unit). Examples: 0.25 = 15 minutes; 8 = one work day. Allowed values: "Infinity" \| "-Infinity" \| "NaN". Type: number \| string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly issues delete <project> <identifier>` | Delete Issue Requires `--yes`. | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<identifier>` — Issue identifier (e.g., 'HULY-123') Type: string. | — | — |
| `huly issues documents link` | Link a document to an issue Requires `--yes`. | — | `--project <value>` — Project identifier of the issue (e.g., 'HULY') Type: string.<br>`--issue-identifier <value>` — Issue identifier (e.g., 'HULY-123') Type: string.<br>`--teamspace <value>` — Teamspace containing the document (name or ID) Type: string.<br>`--document <value>` — Document to link (title or ID) Type: string. | — |
| `huly issues documents unlink` | Unlink a document from an issue Requires `--yes`. | — | `--project <value>` — Project identifier of the issue (e.g., 'HULY') Type: string.<br>`--issue-identifier <value>` — Issue identifier (e.g., 'HULY-123') Type: string.<br>`--teamspace <value>` — Teamspace containing the document (name or ID) Type: string.<br>`--document <value>` — Document to link (title or ID) Type: string. | — |
| `huly issues from-template create <project> <template>` | Create Issue From Template | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<template>` — Template ID or title Type: string. | — | `--title <value>` — Override title (uses template title if not specified) Type: string. Pass null to clear the field.<br>`--description <value>` — Override description in markdown (uses template description if not specified). Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--priority <value>` — Override priority Allowed values: "urgent" \| "high" \| "medium" \| "low" \| "no-priority". Type: string. Pass null to clear the field.<br>`--assignee <value>` — Override assignee email or display name Type: string. Pass null to clear the field.<br>`--status <value>` — Initial status (uses project default if not specified) Type: string. Pass null to clear the field.<br>`--include-children` / `--no-include-children` — Whether to create sub-issues from template children (default: true) Type: boolean. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly issues get <project> <identifier>` | Get an issue | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<identifier>` — Issue identifier (e.g., 'HULY-123') Type: string. | — | — |
| `huly issues labels add` | Add an issue label | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string.<br>`--identifier <value>` — Issue identifier (e.g., 'HULY-123') Type: string.<br>`--label <value>` — Label name to add Type: string. | `--color <value>` — Huly platform color palette index from 0 through 23 (default: 0) Type: integer. Pass null to clear the field. |
| `huly issues labels remove` | Remove an issue label | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string.<br>`--identifier <value>` — Issue identifier (e.g., 'HULY-123') Type: string.<br>`--label <value>` — Label name to remove Type: string. | — |
| `huly issues list` | List issues | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string. | `--status <value>` — Filter by exact workflow status name. Does not accept category aliases. Type: string. Pass null to clear the field.<br>`--status-category <value>` — Filter by Huly SDK task.statusCategory key: UnStarted, ToDo, Active, Won, Lost. Use status for exact project-specific status names. Type: string. Pass null to clear the field.<br>`--assignee <value>` — Filter by assignee email, Person display name, or exact agent UserProfile title Type: string. Pass null to clear the field.<br>`--creator <value>` — Filter by creator using a raw Person ID, exact email address, or exact display name. Ambiguous exact display names are rejected; unknown people return an empty list. Type: string. Pass null to clear the field.<br>`--parent-issue <value>` — Filter to children of this parent issue (e.g., 'HULY-42') Type: string. Pass null to clear the field.<br>`--title-search <value>` — Search issues by title substring (case-insensitive). Mutually exclusive with titleRegex. Type: string. Pass null to clear the field.<br>`--title-regex <value>` — Filter issues by title using Huly $regex. On the supported Postgres backend this is SQL SIMILAR TO, not JavaScript RegExp; matching is case-sensitive and the pattern must match the whole title: use '%' for any string (e.g., '%BUG%' contains, 'BUG%' prefix). Mutually exclusive with titleSearch; use titleSearch for simple substring matching. Type: string. Pass null to clear the field.<br>`--description-search <value>` — Search issues by description content (fulltext search) Type: string. Pass null to clear the field.<br>`--component <value>` — Filter by component ID or label Type: string. Pass null to clear the field.<br>`--label <value>` — Filter by an attached human-readable label title. Matching is exact after trimming and case-insensitive; duplicate attachments do not duplicate issues. Type: string. Pass null to clear the field.<br>`--milestone <value>` — Milestone ID or exact project-scoped label after trimming and case-insensitive matching. Mutually exclusive with hasMilestone. Type: string. Pass null to clear the field.<br>`--has-milestone` / `--no-has-milestone` — Filter by milestone presence. true = only scheduled issues, false = only issues without a milestone. Type: boolean. Pass null to clear the field.<br>`--has-assignee` / `--no-has-assignee` — Filter by assignee presence. true = only assigned issues, false = only unassigned issues. Type: boolean. Pass null to clear the field.<br>`--has-due-date` / `--no-has-due-date` — Filter by due date presence. true = only issues with a due date, false = only issues without. Type: boolean. Pass null to clear the field.<br>`--has-component` / `--no-has-component` — Filter by component presence. true = only issues with a component, false = only issues without. Type: boolean. Pass null to clear the field.<br>`--is-top-level` / `--no-is-top-level` — When true, only return top-level issues (not sub-issues). false or omitted returns all issues. Type: boolean. Pass null to clear the field.<br>`--limit <value>` — Maximum number of issues to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly issues milestone set` | Set or clear an issue milestone | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string.<br>`--identifier <value>` — Issue identifier (e.g., 'HULY-123') Type: string.<br>`--milestone <value>` — Milestone ID or label (null to clear) Type: string. Pass null to clear the field. | — |
| `huly issues move <project> <identifier>` | Move an issue | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<identifier>` — Issue to move (e.g., 'HULY-123') Type: string. | `--new-parent <value>` — New parent issue identifier, or null to restore the native top-level issue shape Type: string. Pass null to clear the field. | — |
| `huly issues related-targets delete <space>` | Delete Related Issue Space Target Requires `--yes`. | `<space>` — Source space name or ID whose spaceRule default related-issue destination project should be deleted. Type: string. | — | — |
| `huly issues related-targets list` | List Related Issue Targets | — | — | `--space <value>` — Optional source space name or ID. When provided, returns only the spaceRule that sets that space's default related-issue destination project. Type: string. Pass null to clear the field.<br>`--object-class <value>` — Optional exact Huly object class ID. When provided, returns only the classRule that sets that class's default related-issue destination project. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of related issue targets to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly issues related-targets set <targetProject>` | Set Related Issue Target | `<targetProject>` — Default destination project identifier for related issues from the selected space or object class, or null to clear the default destination project. Type: string. Pass null to clear the field. | — | `--space <value>` — Source space name or ID for a spaceRule. Space rules set the default destination project for related issues from that space and can be created or updated by this tool. Type: string. Choose one input alternative: { space } \| { objectClass }. Pass null to clear the field.<br>`--object-class <value>` — Exact Huly object class ID for an existing classRule. Class rules set the default destination project for related issues from that object class and can only have their targetProject updated. Type: string. Choose one input alternative: { space } \| { objectClass }. Pass null to clear the field. |
| `huly issues relations add` | Add an issue relation | — | `--project <value>` — Project identifier of the source issue. Type: string.<br>`--issue-identifier <value>` — Source issue identifier. Type: string.<br>`--target-issue <value>` — Target issue identifier; cross-project identifiers are accepted. Type: string.<br>`--relation-type <value>` — Relation type: 'blocks' (source blocks target), 'is-blocked-by' (source is blocked by target), 'relates-to' (bidirectional link). Allowed values: "blocks" \| "is-blocked-by" \| "relates-to". Type: string. | — |
| `huly issues relations list` | List issue relations | — | `--project <value>` — Project identifier of the source issue. Type: string.<br>`--issue-identifier <value>` — Source issue identifier. Type: string. | — |
| `huly issues relations remove` | Remove an issue relation | — | `--project <value>` — Project identifier of the source issue. Type: string.<br>`--issue-identifier <value>` — Source issue identifier. Type: string.<br>`--target-issue <value>` — Target issue identifier; cross-project identifiers are accepted. Type: string.<br>`--relation-type <value>` — Relation type: 'blocks' (source blocks target), 'is-blocked-by' (source is blocked by target), 'relates-to' (bidirectional link). Allowed values: "blocks" \| "is-blocked-by" \| "relates-to". Type: string. | — |
| `huly issues templates children add <title> <project> <template>` | Add Template Child | `<title>` — Child template title Type: string.<br>`<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<template>` — Template ID or title Type: string. | — | `--description <value>` — Child template description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--priority <value>` — Child default priority Allowed values: "urgent" \| "high" \| "medium" \| "low" \| "no-priority". Type: string. Pass null to clear the field.<br>`--assignee <value>` — Child default assignee email or display name Type: string. Pass null to clear the field.<br>`--component <value>` — Child default component ID or label Type: string. Pass null to clear the field.<br>`--estimation <value>` — Child default estimation in hours (Huly native unit). Examples: 0.25 = 15 minutes; 8 = one work day. Allowed values: "Infinity" \| "-Infinity" \| "NaN". Type: number \| string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly issues templates children remove <project> <template> <childId>` | Remove Template Child Requires `--yes`. | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<template>` — Template ID or title Type: string.<br>`<childId>` — ID of the child template to remove Type: string. | — | — |
| `huly issues templates create <project> <title>` | Create Issue Template | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<title>` — Template title Type: string. | — | `--description <value>` — Template description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--priority <value>` — Default priority for issues created from this template Allowed values: "urgent" \| "high" \| "medium" \| "low" \| "no-priority". Type: string. Pass null to clear the field.<br>`--assignee <value>` — Default assignee email address or display name Type: string. Pass null to clear the field.<br>`--component <value>` — Default component ID or label Type: string. Pass null to clear the field.<br>`--estimation <value>` — Default estimation in hours (Huly native unit). Examples: 0.25 = 15 minutes; 8 = one work day. Allowed values: "Infinity" \| "-Infinity" \| "NaN". Type: number \| string. Pass null to clear the field.<br>`--children <value>` — Child (sub-task) templates to include Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly issues templates delete <project> <template>` | Delete Issue Template Requires `--yes`. | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<template>` — Template ID or title Type: string. | — | — |
| `huly issues templates get <project> <template>` | Get Issue Template | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<template>` — Template ID or title Type: string. | — | — |
| `huly issues templates list <project>` | List Issue Templates | `<project>` — Project identifier (e.g., 'HULY') Type: string. | — | `--limit <value>` — Maximum number of templates to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly issues templates update <project> <template>` | Update Issue Template | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<template>` — Template ID or title Type: string. | — | `--title <value>` — New template title Type: string. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { component } \| { estimation }. Pass null to clear the field.<br>`--description <value>` — New template description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Type: string. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { component } \| { estimation }. Pass null to clear the field.<br>`--priority <value>` — New default priority Allowed values: "urgent" \| "high" \| "medium" \| "low" \| "no-priority". Type: string. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { component } \| { estimation }. Pass null to clear the field.<br>`--assignee <value>` — New default assignee email or display name (null to unassign) Type: string. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { component } \| { estimation }. Pass null to clear the field.<br>`--component <value>` — New default component ID or label (null to clear) Type: string. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { component } \| { estimation }. Pass null to clear the field.<br>`--estimation <value>` — New default estimation in hours (Huly native unit). Examples: 0.25 = 15 minutes; 8 = one work day. Use null to clear. Allowed values: "Infinity" \| "-Infinity" \| "NaN". Type: number \| string. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { component } \| { estimation }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly issues update <project> <identifier>` | Update an issue | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<identifier>` — Issue identifier (e.g., 'HULY-123') Type: string. | — | `--title <value>` — New issue title Type: string. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { status } \| { taskType } \| { dueDate } \| { estimation }. Pass null to clear the field.<br>`--description <value>` — New issue description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references. External URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Type: string. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { status } \| { taskType } \| { dueDate } \| { estimation }. Pass null to clear the field.<br>`--priority <value>` — New issue priority Allowed values: "urgent" \| "high" \| "medium" \| "low" \| "no-priority". Type: string. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { status } \| { taskType } \| { dueDate } \| { estimation }. Pass null to clear the field.<br>`--assignee <value>` — New assignee email, Person display name, or exact agent UserProfile title (null to unassign) Type: string. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { status } \| { taskType } \| { dueDate } \| { estimation }. Pass null to clear the field.<br>`--status <value>` — New status Type: string. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { status } \| { taskType } \| { dueDate } \| { estimation }. Pass null to clear the field.<br>`--task-type <value>` — New issue/task type ID or display name. Resolved within the target project's project type; status is preserved only if valid for that task type. Use list_task_types or get_project_type to discover valid values. Type: string. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { status } \| { taskType } \| { dueDate } \| { estimation }. Pass null to clear the field.<br>`--due-date <value>` — Due date as Unix timestamp in milliseconds (e.g., 1719792000000 for 2024-07-01), or null to clear Type: integer. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { status } \| { taskType } \| { dueDate } \| { estimation }. Pass null to clear the field.<br>`--estimation <value>` — Time estimation in hours (Huly native unit). Examples: 0.25 = 15 minutes; 8 = one work day. Use null to clear. Allowed values: "Infinity" \| "-Infinity" \| "NaN". Type: number \| string. Choose one input alternative: { title } \| { description } \| { priority } \| { assignee } \| { status } \| { taskType } \| { dueDate } \| { estimation }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly labels create <title>` | Create Label | `<title>` — Label name Type: string. | — | `--color <value>` — Huly platform color palette index from 0 through 23 (default: 0) Type: integer. Pass null to clear the field.<br>`--description <value>` — Label description Type: string. Pass null to clear the field.<br>`--category <value>` — Category ID or label name. Falls back to tracker default category ('Other') if not specified. Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly labels delete <label>` | Delete Label Requires `--yes`. | `<label>` — Label ID or title to delete Type: string. | — | — |
| `huly labels list` | List labels | — | — | `--category <value>` — Filter by category ID or label name Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of labels to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly labels update <label>` | Update Label | `<label>` — Label ID or title to update Type: string. | — | `--title <value>` — New label name Type: string. Choose one input alternative: { title } \| { color } \| { description }. Pass null to clear the field.<br>`--color <value>` — New Huly platform color palette index from 0 through 23 Type: integer. Choose one input alternative: { title } \| { color } \| { description }. Pass null to clear the field.<br>`--description <value>` — New label description. Pass null to clear; empty string is also accepted. Type: string. Choose one input alternative: { title } \| { color } \| { description }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly leads create <funnel> <customer> <title>` | Create Lead | `<funnel>` — Active funnel ID returned by list_funnels, or exact funnel name. Type: string.<br>`<customer>` — Existing person or organization customer locator. Type: object. JSON shape alternatives: { kind, identifier }. Pass arrays or objects as JSON.<br>`<title>` — Non-empty lead title. Type: string. | — | `--description <value>` — Optional Markdown description. Type: string. Pass null to clear the field.<br>`--assignee <value>` — Optional employee assignee by ID, exact email, or exact display name. Type: string. Pass null to clear the field.<br>`--status <value>` — Optional exact workflow status name. Type: string. Pass null to clear the field.<br>`--task-type <value>` — Optional native Lead task type ID or exact display name. Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly leads funnels list` | List Funnels | — | — | `--include-archived` / `--no-include-archived` — Include archived funnels (default: false). Type: boolean. Pass null to clear the field.<br>`--limit <value>` — Maximum number of funnels to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly leads get <funnel> <identifier>` | Get Lead | `<funnel>` — Funnel ID returned by list_funnels, or exact funnel name. Type: string.<br>`<identifier>` — Lead identifier, such as LEAD-1. Type: string. | — | — |
| `huly leads list <funnel>` | List Leads | `<funnel>` — Funnel ID returned by list_funnels, or exact funnel name. Type: string. | — | `--status <value>` — Filter by exact status name. Type: string. Pass null to clear the field.<br>`--assignee <value>` — Filter by assignee email or display name. Type: string. Pass null to clear the field.<br>`--title-search <value>` — Search leads by title substring (case-insensitive). Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of leads to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly mail threads list` | List Mail Threads | — | — | `--space <value>` — Filter by a Huly space name or stable space ID. Names must resolve unambiguously. Type: string. Pass null to clear the field.<br>`--channel-title-search <value>` — Case-insensitive substring search over the outer Mail channel title. The title may be a replication-recipient email; it does not prove a correspondent or configured mailbox. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of Mail thread channels to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly milestones create <project> <label> <targetDate>` | Create Milestone | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<label>` — Milestone name/label Type: string.<br>`<targetDate>` — Target date as Unix timestamp in milliseconds Type: integer. | — | `--description <value>` — Milestone description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly milestones delete <project> <milestone>` | Delete Milestone Requires `--yes`. | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<milestone>` — Milestone ID or label Type: string. | — | — |
| `huly milestones get` | Get a milestone | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string.<br>`--milestone <value>` — Milestone ID or label Type: string. | — |
| `huly milestones list` | List milestones | — | `--project <value>` — Project identifier (e.g., 'HULY') Type: string. | `--limit <value>` — Maximum number of milestones to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly milestones update <project> <milestone>` | Update Milestone | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<milestone>` — Milestone ID or label Type: string. | — | `--label <value>` — New milestone name/label Type: string. Choose one input alternative: { label } \| { description } \| { targetDate } \| { status }. Pass null to clear the field.<br>`--description <value>` — New milestone description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Type: string. Choose one input alternative: { label } \| { description } \| { targetDate } \| { status }. Pass null to clear the field.<br>`--target-date <value>` — New target date as Unix timestamp in milliseconds Type: integer. Choose one input alternative: { label } \| { description } \| { targetDate } \| { status }. Pass null to clear the field.<br>`--status <value>` — New milestone status Allowed values: "planned" \| "in-progress" \| "completed" \| "canceled". Type: string. Choose one input alternative: { label } \| { description } \| { targetDate } \| { status }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly model attributes create <class> <name> <label>` | Create Huly Model Attribute | `<class>` — Owning class or mixin ID, exact name, or exact label. Type: string.<br>`<name>` — Stable property key used on Huly documents. Type: string.<br>`<label>` — Human-readable property label. Type: string. | `--type <value>` — Attribute type definition, including referenced enum or class when required. Type: object. JSON shape alternatives: { kind } \| { kind, enum } \| { kind, class }. Pass arrays or objects as JSON.<br>`--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes Huly workspace model metadata. Allowed values: true. Type: boolean. | `--index <value>` — Optional Huly attribute index mode. Allowed values: "fulltext" \| "indexed" \| "indexedDescending". Type: string. Pass null to clear the field.<br>`--automation-only` / `--no-automation-only` — Whether the attribute is restricted to automation-owned behavior. Type: boolean. Pass null to clear the field.<br>`--hidden` / `--no-hidden` — Whether the attribute is hidden from ordinary Huly UI surfaces. Type: boolean. Pass null to clear the field. |
| `huly model attributes delete <attribute>` | Delete Huly Model Attribute Requires `--yes`. | `<attribute>` — Attribute ID or exact attribute name. Type: string. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes Huly workspace model metadata. Allowed values: true. Type: boolean. | `--class <value>` — Owning class ID or exact name used to disambiguate attribute names. Type: string. Pass null to clear the field. |
| `huly model attributes list` | List Huly model attributes | — | — | `--class <value>` — Only return attributes declared directly on this class, interface, or mixin ID. Type: string. Pass null to clear the field.<br>`--query <value>` — Case-insensitive substring match against attribute ID, name, label, owner class ID, or type target. Type: string. Pass null to clear the field.<br>`--custom-only` / `--no-custom-only` — Only return attributes marked as custom fields. Defaults to false. Type: boolean. Pass null to clear the field.<br>`--limit <value>` — Maximum number of attributes to return after filtering (default: 100, max: 200) Type: integer. Pass null to clear the field. |
| `huly model attributes update <attribute>` | Update Huly Model Attribute | `<attribute>` — Attribute ID or exact attribute name. Type: string. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes Huly workspace model metadata. Allowed values: true. Type: boolean. | `--class <value>` — Owning class ID or exact name used to disambiguate attribute names. Type: string. Pass null to clear the field.<br>`--label <value>` — New human-readable property label. Type: string. Choose one input alternative: { label } \| { index } \| { automationOnly } \| { hidden }. Pass null to clear the field.<br>`--index <value>` — New index mode; null removes the current index. Allowed values: "fulltext" \| "indexed" \| "indexedDescending". Type: string. Choose one input alternative: { label } \| { index } \| { automationOnly } \| { hidden }. Pass null to clear the field.<br>`--automation-only` / `--no-automation-only` — New automation-only flag. Type: boolean. Choose one input alternative: { label } \| { index } \| { automationOnly } \| { hidden }. Pass null to clear the field.<br>`--hidden` / `--no-hidden` — New hidden flag. Type: boolean. Choose one input alternative: { label } \| { index } \| { automationOnly } \| { hidden }. Pass null to clear the field. |
| `huly model classes get <class>` | Get a Huly model class and its attributes | `<class>` — Exact Huly class, interface, or mixin ID returned by list_huly_classes. Type: string. | — | `--include-inherited-attributes` / `--no-include-inherited-attributes` — Include attributes declared on parent classes. Defaults to true. Type: boolean. Pass null to clear the field. |
| `huly model classes list` | List Huly model classes, interfaces, and mixins | — | — | `--query <value>` — Case-insensitive substring match against class ID or label. Type: string. Pass null to clear the field.<br>`--kind <value>` — Filter by class, interface, or mixin. Allowed values: "class" \| "interface" \| "mixin" \| "unknown". Type: string. Pass null to clear the field.<br>`--domain <value>` — Filter by Huly storage domain, such as tracker, document, card, contact, or model. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of classes to return after filtering (default: 100, max: 200) Type: integer. Pass null to clear the field. |
| `huly model collaborators delete <class>` | Delete Class Collaborator Metadata Requires `--yes`. | `<class>` — Class ID, tail name, or label. Type: string. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. Allowed values: true. Type: boolean. | — |
| `huly model collaborators get <class>` | Get Class Collaborator Metadata | `<class>` — Class ID, tail name, or label. Type: string. | — | — |
| `huly model collaborators set <class> <fieldSelection>` | Set Class Collaborator Metadata Requires `--yes`. | `<class>` — Class ID, tail name, or label. Type: string.<br>`<fieldSelection>` — Collaborator field selection: all, none, or an explicit non-empty fields list. Type: object. JSON shape alternatives: { mode } \| { mode, fields }. Pass arrays or objects as JSON. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. Allowed values: true. Type: boolean. | `--provide-security` / `--no-provide-security` — Propagate security through collaborator fields; defaults to false. Type: boolean.<br>`--provide-attached-security` / `--no-provide-attached-security` — Propagate security through attached collaborator documents; defaults to false. Type: boolean. |
| `huly model domain-indexes list` | List Huly domain index configurations | — | — | — |
| `huly model enums create <name> <values>` | Create Huly Model Enum | `<name>` — Unique human-readable enum name. Type: string.<br>`<values>` — Ordered, case-insensitively unique enum option values. Type: array. Pass arrays or objects as JSON. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes Huly workspace model metadata. Allowed values: true. Type: boolean. | — |
| `huly model enums delete <enum>` | Delete Huly Model Enum Requires `--yes`. | `<enum>` — Enum ID or exact enum name. Type: string. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes Huly workspace model metadata. Allowed values: true. Type: boolean. | — |
| `huly model enums list` | List Huly model enums | — | — | `--enum <value>` — Exact enum document ID. Type: string. Pass null to clear the field.<br>`--query <value>` — Case-insensitive substring match against enum ID, name, or option values. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of enums to return after filtering (default: 100, max: 200) Type: integer. Pass null to clear the field. |
| `huly model enums update <enum>` | Update Huly Model Enum | `<enum>` — Enum ID or exact enum name. Type: string. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes Huly workspace model metadata. Allowed values: true. Type: boolean. | `--name <value>` — New unique enum name. Type: string. Choose one input alternative: { name } \| { values }. Pass null to clear the field.<br>`--values <value>` — Replacement ordered, case-insensitively unique enum option values. Type: array. Choose one input alternative: { name } \| { values }. Pass arrays or objects as JSON. Pass null to clear the field. |
| `huly model permissions create <label> <scope>` | Create Huly Permission Requires `--yes`. | `<label>` — Clear human-readable permission label. Type: string.<br>`<scope>` — Permission scope: space or workspace. Allowed values: "space" \| "workspace". Type: string. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. Allowed values: true. Type: boolean. | `--object-class <value>` — Optional object class ID, tail name, or label constrained by this permission. Type: string.<br>`--transaction <value>` — Optional constrained transaction kind: create, update, remove, or mixin. Allowed values: "create" \| "update" \| "remove" \| "mixin". Type: string.<br>`--forbid` / `--no-forbid` — Whether the permission forbids rather than grants the selected action. Type: boolean.<br>`--description <value>` — Optional human-readable permission description. Type: string. |
| `huly model permissions delete <permission>` | Delete Huly Permission Requires `--yes`. | `<permission>` — Permission ID, exact label, or exact final label segment. Type: string. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. Allowed values: true. Type: boolean. | — |
| `huly model permissions update <permission>` | Update Huly Permission Requires `--yes`. | `<permission>` — Permission ID, exact label, or exact final label segment. Type: string. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. Allowed values: true. Type: boolean. | `--label <value>` — New human-readable permission label. Type: string. Choose one input alternative: { label } \| { scope } \| { objectClass } \| { transaction } \| { forbid } \| { description }.<br>`--scope <value>` — New permission scope: space or workspace. Allowed values: "space" \| "workspace". Type: string. Choose one input alternative: { label } \| { scope } \| { objectClass } \| { transaction } \| { forbid } \| { description }.<br>`--object-class <value>` — New constrained object class; null clears the constraint. Type: string. Choose one input alternative: { label } \| { scope } \| { objectClass } \| { transaction } \| { forbid } \| { description }. Pass null to clear the field.<br>`--transaction <value>` — New constrained transaction kind; null clears the constraint. Allowed values: "create" \| "update" \| "remove" \| "mixin". Type: string. Choose one input alternative: { label } \| { scope } \| { objectClass } \| { transaction } \| { forbid } \| { description }. Pass null to clear the field.<br>`--forbid` / `--no-forbid` — New forbid flag. Type: boolean. Choose one input alternative: { label } \| { scope } \| { objectClass } \| { transaction } \| { forbid } \| { description }.<br>`--description <value>` — New permission description; null clears it. Type: string. Choose one input alternative: { label } \| { scope } \| { objectClass } \| { transaction } \| { forbid } \| { description }. Pass null to clear the field. |
| `huly model plugins list` | List Huly plugin configurations | — | — | — |
| `huly model sequences create <class> <kind>` | Create Huly Sequence | `<class>` — Attached class ID, exact class name, or exact class label. Type: string.<br>`<kind>` — Sequence kind: standard or custom. Allowed values: "standard" \| "custom". Type: string. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes a Huly sequence definition. Allowed values: true. Type: boolean. | `--prefix <value>` — Prefix added to identifiers generated by a custom sequence. Type: string. |
| `huly model sequences delete <sequence> <expectedCurrentValue>` | Delete Huly Sequence Requires `--yes`. | `<sequence>` — Exact sequence ID or attached class ID or name. Type: string. Pattern: ^\S[\s\S]*\S$\|^\S$\|^$.<br>`<expectedCurrentValue>` — Must be 0; only a never-used sequence can be deleted. Allowed values: 0. Type: number. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes a Huly sequence definition. Allowed values: true. Type: boolean. | — |
| `huly model sequences list` | List Huly model sequences | — | — | — |
| `huly model sequences update <sequence> <prefix>` | Update Huly Custom Sequence | `<sequence>` — Exact sequence ID or attached class ID or name. Type: string. Pattern: ^\S[\s\S]*\S$\|^\S$\|^$.<br>`<prefix>` — New prefix added to identifiers generated by the custom sequence. Type: string. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes a Huly sequence definition. Allowed values: true. Type: boolean. | — |
| `huly model space-types capabilities describe <spaceType>` | Describe a Huly space type's model capabilities | `<spaceType>` — SpaceType ID or exact SpaceType name whose capabilities should be described. Type: string. | — | — |
| `huly notifications all archive` | Archive all notifications for the current account Requires `--yes`. | — | — | — |
| `huly notifications all read` | Mark all notifications read for the current account Requires `--yes`. | — | — | — |
| `huly notifications archive <notificationId>` | Archive Notification | `<notificationId>` — Notification ID to archive Type: string. | — | — |
| `huly notifications contexts archive <contextId>` | Archive Notification Context | `<contextId>` — Notification context ID whose inbox notifications should be archived. Type: string. | — | — |
| `huly notifications contexts get <objectId> <objectClass>` | Get Notification Context | `<objectId>` — Object ID to get notification context for Type: string.<br>`<objectClass>` — Object class name (e.g., 'tracker.class.Issue') Type: string. | — | — |
| `huly notifications contexts hide <contextId> <hidden>` | Hide Notification Context Requires `--yes`. | `<contextId>` — Notification context ID to hide/unhide Type: string.<br>`<hidden>` — Whether to hide (true) or unhide (false) the context Type: boolean. | — | — |
| `huly notifications contexts list` | List Notification Contexts | — | — | `--limit <value>` — Maximum number of contexts to return (default: 50) Type: integer. Pass null to clear the field.<br>`--pinned-only` / `--no-pinned-only` — Return only pinned contexts (default: false) Type: boolean. Pass null to clear the field.<br>`--include-hidden` / `--no-include-hidden` — Include hidden notification contexts in results (default: false) Type: boolean. Pass null to clear the field. |
| `huly notifications contexts pin <contextId> <pinned>` | Pin Notification Context | `<contextId>` — Notification context ID to pin/unpin Type: string.<br>`<pinned>` — Whether to pin (true) or unpin (false) the context Type: boolean. | — | — |
| `huly notifications contexts unarchive <contextId>` | Unarchive Notification Context Requires `--yes`. | `<contextId>` — Notification context ID whose inbox notifications should be archived. Type: string. | — | — |
| `huly notifications delete <notificationId>` | Delete Notification Requires `--yes`. | `<notificationId>` — Notification ID to delete Type: string. | — | — |
| `huly notifications get <notificationId>` | Get Notification | `<notificationId>` — Notification ID Type: string. | — | — |
| `huly notifications list` | List Notifications | — | — | `--limit <value>` — Maximum number of notifications to return (default: 50) Type: integer. Pass null to clear the field.<br>`--include-archived` / `--no-include-archived` — Include archived notifications in results (default: false) Type: boolean. Pass null to clear the field.<br>`--unread-only` / `--no-unread-only` — Return only unread notifications (default: false) Type: boolean. Pass null to clear the field. |
| `huly notifications providers list` | List Notification Providers | — | — | `--limit <value>` — Maximum number of providers to return (default: 50) Type: integer. Pass null to clear the field.<br>`--include-unavailable` / `--no-include-unavailable` — Include providers that the workspace may not currently expose as configurable settings. Type: boolean. Pass null to clear the field. |
| `huly notifications providers settings update <providerId> <enabled>` | Update Notification Provider Setting | `<providerId>` — Notification provider ID Type: string.<br>`<enabled>` — Whether to enable or disable the provider Type: boolean. | — | — |
| `huly notifications read mark <notificationId>` | Mark Notification Read | `<notificationId>` — Notification ID to mark as read Type: string. | — | — |
| `huly notifications settings list` | List Notification Settings | — | — | `--limit <value>` — Maximum number of settings to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly notifications types list` | List Notification Types | — | — | `--limit <value>` — Maximum number of notification types to return (default: 50) Type: integer. Pass null to clear the field.<br>`--include-hidden` / `--no-include-hidden` — Include hidden/internal notification types. Type: boolean. Pass null to clear the field.<br>`--object-class <value>` — Filter to notification types for this Huly object class. Type: string. Pass null to clear the field. |
| `huly notifications types settings update <providerId> <typeId> <enabled>` | Update Notification Type Setting | `<providerId>` — Notification provider ID, such as notification:providers:InboxNotificationProvider. Type: string.<br>`<typeId>` — Notification type ID to configure. Type: string.<br>`<enabled>` — Whether to enable or disable this notification type for the provider. Type: boolean. | — | — |
| `huly notifications unarchive <notificationId>` | Unarchive Notification Requires `--yes`. | `<notificationId>` — Notification ID to unarchive Type: string. | — | — |
| `huly notifications unread mark <notificationId>` | Mark Notification Unread | `<notificationId>` — Notification ID to mark as unread Type: string. | — | — |
| `huly notifications unread-count get` | Get Unread Notification Count | — | — | — |
| `huly objects collaborators add <member>` | Add a collaborator using one raw, issue, or document object locator Requires `--yes`. | `<member>` — Workspace account UUID, or an exact person display name or email. Type: string. | — | `--object-id <value>` — Advanced: internal Huly object ID. Use with objectClass. Type: string. Pass null to clear the field.<br>`--object-class <value>` — Advanced: internal Huly object class for objectId. Type: string. Pass null to clear the field.<br>`--project <value>` — Project identifier for issue target, e.g. 'HULY'. Use with issueIdentifier. Type: string. Pass null to clear the field.<br>`--issue-identifier <value>` — Issue identifier, e.g. 'HULY-123' or '123'. Use with project. Type: string. Pass null to clear the field.<br>`--teamspace <value>` — Teamspace name or ID for document target. Use with document. Type: string. Pass null to clear the field.<br>`--document <value>` — Document title or ID for document target. Use with teamspace. Type: string. Pass null to clear the field. |
| `huly objects collaborators remove <member>` | Remove a collaborator using one raw, issue, or document object locator Requires `--yes`. | `<member>` — Workspace account UUID, or an exact person display name or email. Type: string. | — | `--object-id <value>` — Advanced: internal Huly object ID. Use with objectClass. Type: string. Pass null to clear the field.<br>`--object-class <value>` — Advanced: internal Huly object class for objectId. Type: string. Pass null to clear the field.<br>`--project <value>` — Project identifier for issue target, e.g. 'HULY'. Use with issueIdentifier. Type: string. Pass null to clear the field.<br>`--issue-identifier <value>` — Issue identifier, e.g. 'HULY-123' or '123'. Use with project. Type: string. Pass null to clear the field.<br>`--teamspace <value>` — Teamspace name or ID for document target. Use with document. Type: string. Pass null to clear the field.<br>`--document <value>` — Document title or ID for document target. Use with teamspace. Type: string. Pass null to clear the field. |
| `huly objects custom-fields set <objectId> <objectClass> <fieldId> <value>` | Set a custom field on a raw Huly object | `<objectId>` — Document ID to set the custom field value on. Type: string.<br>`<objectClass>` — Document class ID, such as tracker:class:Issue or card:class:Card. Type: string.<br>`<fieldId>` — Custom field attribute ID returned by list_custom_fields. Type: string.<br>`<value>` — Value to set using the field's documented wire format. Dates accept YYYY-MM-DD or canonical non-negative epoch milliseconds. Type: string. | — | — |
| `huly objects notifications subscribe <objectId> <objectClass>` | Subscribe the current account to raw-object notifications Requires `--yes`. | `<objectId>` — Internal Huly object ID to subscribe/unsubscribe the authenticated account to. Type: string.<br>`<objectClass>` — Internal Huly object class for objectId. Type: string. | — | `--space <value>` — Optional object space ID. If omitted, the operation reads the object to determine the space. Type: string. Pass null to clear the field. |
| `huly objects notifications unsubscribe <objectId> <objectClass>` | Unsubscribe the current account from raw-object notifications Requires `--yes`. | `<objectId>` — Internal Huly object ID to subscribe/unsubscribe the authenticated account to. Type: string.<br>`<objectClass>` — Internal Huly object class for objectId. Type: string. | — | `--space <value>` — Optional object space ID. If omitted, the operation reads the object to determine the space. Type: string. Pass null to clear the field. |
| `huly objects tags attach <targetClass> <tag>` | Attach a tag; pass the raw target locator with --object as JSON Requires `--yes`. | `<targetClass>` — Huly class or mixin this tag definition applies to, such as tracker:class:Issue or recruit:mixin:Candidate. Type: string.<br>`<tag>` — Tag ID or exact title; a missing title creates the tag definition before attaching it. Type: string. | `--object <value>` — Raw Huly object and collection that will own the tag reference. Type: object. JSON shape alternatives: { objectId, objectClass, space, collection }. Pass arrays or objects as JSON. | `--color <value>` — Color palette index for a newly created tag (default: 0); ignored for existing tags. Type: integer. Pass null to clear the field.<br>`--category <value>` — Category for a newly created tag; ignored for existing tags. Type: string. Pass null to clear the field.<br>`--weight <value>` — Optional weight or knowledge level stored on the tag reference. Allowed values: 0 \| 1 \| 2 \| 3 \| 4 \| 5 \| 6 \| 7 \| 8. Type: number. Pass null to clear the field. |
| `huly objects tags detach <targetClass> <tag>` | Detach a tag; pass the raw target locator with --object as JSON Requires `--yes`. | `<targetClass>` — Huly class or mixin this tag definition applies to, such as tracker:class:Issue or recruit:mixin:Candidate. Type: string.<br>`<tag>` — Tag ID or exact title within targetClass. Type: string. | `--object <value>` — Raw Huly object and collection from which to detach the tag reference. Type: object. JSON shape alternatives: { objectId, objectClass, space, collection }. Pass arrays or objects as JSON. | — |
| `huly office defaults list` | List Office Defaults | — | — | — |
| `huly office devices preferences list` | List Device Preferences | — | — | — |
| `huly office floors get <floorId>` | Get Office Floor | `<floorId>` — Virtual office floor ID. Type: string. | — | — |
| `huly office floors list` | List Office Floors | — | — | `--limit <value>` — Maximum number of floors to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly office meeting-minutes get <meetingMinutesId>` | Get Meeting Minutes | `<meetingMinutesId>` — Meeting notes/transcript record ID (meeting minutes ID). Type: string. | — | — |
| `huly office meeting-minutes list` | List Meeting Minutes | — | — | `--attached-to-id <value>` — Optional room or meeting document ID that the meeting notes/transcript record (minutes) is attached to. Type: string. Pass null to clear the field.<br>`--from <value>` — Created-on lower bound timestamp. Type: integer. Pass null to clear the field.<br>`--to <value>` — Created-on upper bound timestamp. Type: integer. Pass null to clear the field.<br>`--limit <value>` — Maximum number of meeting notes/transcript records (minutes) to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly office offices get <roomId>` | Get Office | `<roomId>` — Virtual office room ID. Type: string. | — | — |
| `huly office offices list` | List Offices | — | — | `--floor-id <value>` — Optional floor ID filter. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of rooms to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly office rooms active-info list` | List Active Room Info | — | — | — |
| `huly office rooms get <roomId>` | Get Office Room | `<roomId>` — Virtual office room ID. Type: string. | — | — |
| `huly office rooms list` | List Office Rooms | — | — | `--floor-id <value>` — Optional floor ID filter. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of rooms to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly office rooms participants list` | List Active Room Participants | — | — | `--room-id <value>` — Optional room ID filter. Type: string. Pass null to clear the field. |
| `huly planner todos complete <locator>` | Complete Todo Requires `--yes`. | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Type: object. JSON shape alternatives: { todoId } \| { issue } \| { title }. Pass arrays or objects as JSON. | — | `--done-on <value>` — Completion timestamp. If omitted, uses the current time. Type: integer. Pass null to clear the field. |
| `huly planner todos create <title>` | Create Todo | `<title>` — ToDo title. Type: string. | — | `--description <value>` — ToDo description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--owner <value>` — Owner exact email or display name. If omitted, uses the authenticated user. Type: string. Pass null to clear the field.<br>`--due-date <value>` — Due date as Unix timestamp in milliseconds. Type: integer. Pass null to clear the field.<br>`--priority <value>` — Priority. Default: no-priority. Allowed values: "no-priority" \| "low" \| "medium" \| "high" \| "urgent". Type: string. Pass null to clear the field.<br>`--visibility <value>` — Visibility. Default: private for personal ToDos, public for issue ToDos. Allowed values: "public" \| "freeBusy" \| "private". Type: string. Pass null to clear the field.<br>`--attached-to <value>` — Attachment target. If omitted, creates a personal ToDo. Type: object. JSON shape alternatives: { type } \| { type, project, identifier }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly planner todos delete <locator>` | Delete Todo Requires `--yes`. | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Type: object. JSON shape alternatives: { todoId } \| { issue } \| { title }. Pass arrays or objects as JSON. | — | — |
| `huly planner todos get <locator>` | Get Todo | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Type: object. JSON shape alternatives: { todoId } \| { issue } \| { title }. Pass arrays or objects as JSON. | — | — |
| `huly planner todos labels add <locator> <label>` | Add Todo Label | `<locator>` — Planner ToDo raw ID or human-oriented locator. Type: object. JSON shape alternatives: { todoId } \| { issue } \| { title }. Pass arrays or objects as JSON.<br>`<label>` — Label TagElement _id or exact title. A missing title creates the module label definition. Type: string. | — | `--color <value>` — Color for a newly created label definition from 0 through 23 (default: 0). Ignored for an existing label. Type: integer. Pass null to clear the field. |
| `huly planner todos labels definitions list` | List Todo Label Definitions | — | — | `--title-search <value>` — Optional label title substring search. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of label definitions to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly planner todos labels list <locator>` | List Todo Labels | `<locator>` — Planner ToDo raw ID or human-oriented locator. Type: object. JSON shape alternatives: { todoId } \| { issue } \| { title }. Pass arrays or objects as JSON. | — | — |
| `huly planner todos labels remove <locator> <label>` | Remove Todo Label Requires `--yes`. | `<locator>` — Planner ToDo raw ID or human-oriented locator. Type: object. JSON shape alternatives: { todoId } \| { issue } \| { title }. Pass arrays or objects as JSON.<br>`<label>` — Label TagElement _id or exact title. Type: string. | — | — |
| `huly planner todos list` | List Todos | — | — | `--owner <value>` — Filter by owner exact email, exact display name, or raw person/employee ID. Type: string. Pass null to clear the field.<br>`--issue <value>` — Filter ToDos attached to one issue. Type: object. JSON shape alternatives: { project, identifier }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--title <value>` — Exact ToDo title filter. Type: string. Pass null to clear the field.<br>`--title-search <value>` — Case-insensitive title substring filter. Type: string. Pass null to clear the field.<br>`--due-from <value>` — Only ToDos due at or after this timestamp. Type: integer. Pass null to clear the field.<br>`--due-to <value>` — Only ToDos due at or before this timestamp. Type: integer. Pass null to clear the field.<br>`--completion-state <value>` — Completion filter. Default: all. Allowed values: "open" \| "completed" \| "all". Type: string. Pass null to clear the field.<br>`--priority <value>` — Planner ToDo priority. Allowed values: no-priority, low, medium, high, urgent. Allowed values: "no-priority" \| "low" \| "medium" \| "high" \| "urgent". Type: string. Pass null to clear the field.<br>`--visibility <value>` — Planner ToDo visibility. Allowed values: public, freeBusy, private. Allowed values: "public" \| "freeBusy" \| "private". Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of ToDos to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly planner todos reopen <locator>` | Reopen Todo Requires `--yes`. | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Type: object. JSON shape alternatives: { todoId } \| { issue } \| { title }. Pass arrays or objects as JSON. | — | — |
| `huly planner todos schedule <locator> <date> <dueDate>` | Schedule Todo Requires `--yes`. | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Type: object. JSON shape alternatives: { todoId } \| { issue } \| { title }. Pass arrays or objects as JSON.<br>`<date>` — Work slot start timestamp. Type: integer.<br>`<dueDate>` — Work slot end timestamp. Type: integer. | — | — |
| `huly planner todos unschedule` | Unschedule Todo Requires `--yes`. | — | — | `--work-slot-id <value>` — Specific work slot ID to remove. Type: string. Choose one input alternative: { workSlotId } \| { locator, scope } \| { locator, scope }.<br>`--locator <value>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Type: object. JSON shape alternatives: { todoId } \| { issue } \| { title }. Choose one input alternative: { workSlotId } \| { locator, scope } \| { locator, scope }. Pass arrays or objects as JSON.<br>`--scope <value>` — Remove all work slots for the located ToDo. Allowed values: "all" \| "future". Type: string. Choose one input alternative: { workSlotId } \| { locator, scope } \| { locator, scope }.<br>`--from <value>` — Reference timestamp for future work slots. If omitted, uses current time. Type: integer. Pass null to clear the field. |
| `huly planner todos update <locator>` | Update Todo | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Type: object. JSON shape alternatives: { todoId } \| { issue } \| { title }. Pass arrays or objects as JSON. | — | `--title <value>` — New ToDo title. Type: string. Choose one input alternative: { title } \| { description } \| { owner } \| { dueDate } \| { priority } \| { visibility }. Pass null to clear the field.<br>`--description <value>` — New ToDo description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Type: string. Choose one input alternative: { title } \| { description } \| { owner } \| { dueDate } \| { priority } \| { visibility }. Pass null to clear the field.<br>`--owner <value>` — New owner exact email or display name. Type: string. Choose one input alternative: { title } \| { description } \| { owner } \| { dueDate } \| { priority } \| { visibility }. Pass null to clear the field.<br>`--due-date <value>` — New due date timestamp, or null to clear. Type: integer. Choose one input alternative: { title } \| { description } \| { owner } \| { dueDate } \| { priority } \| { visibility }. Pass null to clear the field.<br>`--priority <value>` — Planner ToDo priority. Allowed values: no-priority, low, medium, high, urgent. Allowed values: "no-priority" \| "low" \| "medium" \| "high" \| "urgent". Type: string. Choose one input alternative: { title } \| { description } \| { owner } \| { dueDate } \| { priority } \| { visibility }. Pass null to clear the field.<br>`--visibility <value>` — Planner ToDo visibility. Allowed values: public, freeBusy, private. Allowed values: "public" \| "freeBusy" \| "private". Type: string. Choose one input alternative: { title } \| { description } \| { owner } \| { dueDate } \| { priority } \| { visibility }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly platform associations list` | List Associations | — | — | `--association <value>` — Association ID, source or target role name, or stable association name. Type: string. Pass null to clear the field.<br>`--source-class <value>` — Only return associations whose source class matches this Huly class ID. Type: string. Pass null to clear the field.<br>`--target-class <value>` — Only return associations whose target class matches this Huly class ID. Type: string. Pass null to clear the field.<br>`--writable-only` / `--no-writable-only` — Only return associations whose relation write path is validated and allowlisted. Type: boolean. Pass null to clear the field.<br>`--include-system` / `--no-include-system` — Include internal system associations. Defaults to false. Type: boolean. Pass null to clear the field.<br>`--limit <value>` — Maximum number of associations to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly platform relations list` | List Relations | — | — | `--association <value>` — Association ID or name. Omit only when a source or target filter is provided. Type: string. Choose one input alternative: { association } \| { source } \| { target }. Pass null to clear the field.<br>`--source <value>` — Optional source endpoint filter. Type: object. JSON shape alternatives: { kind, id } \| { kind, issue } \| { kind, document } \| { kind, card }. Choose one input alternative: { association } \| { source } \| { target }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--target <value>` — Optional target endpoint filter. Type: object. JSON shape alternatives: { kind, id } \| { kind, issue } \| { kind, document } \| { kind, card }. Choose one input alternative: { association } \| { source } \| { target }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--direction <value>` — Relation traversal direction: source-to-target, target-to-source, either. Defaults to source-to-target. Allowed values: "source-to-target" \| "target-to-source" \| "either". Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of relations to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly preferences spaces get <space>` | Get Space Preference | `<space>` — Space ID or exact space name. Resolution tries ID first, then exact name. Type: string. | — | `--include-archived` / `--no-include-archived` — Allow exact-name matching against archived spaces. ID lookup may return archived spaces. Type: boolean. Pass null to clear the field.<br>`--class <value>` — Raw Huly space class ID used to disambiguate exact-name lookup. Type: string. Pass null to clear the field.<br>`--type <value>` — Raw Huly SpaceType ID used to disambiguate exact-name lookup. Type: string. Pass null to clear the field. |
| `huly preferences spaces list` | List Space Preferences | — | — | `--space <value>` — Space ID or exact space name. Resolution tries ID first, then exact name. Type: string. Pass null to clear the field.<br>`--include-archived` / `--no-include-archived` — Allow exact-name matching against archived spaces. ID lookup may return archived spaces. Type: boolean. Pass null to clear the field.<br>`--class <value>` — Raw Huly space class ID used to disambiguate exact-name lookup. Type: string. Pass null to clear the field.<br>`--type <value>` — Raw Huly SpaceType ID used to disambiguate exact-name lookup. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of preferences to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly processes executions cancel <execution>` | Cancel an active process execution Requires `--yes`. | `<execution>` — Process execution ID to cancel. Already-cancelled executions return cancelled=false; completed executions fail without mutation. Type: string. | — | — |
| `huly processes executions list` | List Process Executions | — | — | `--process <value>` — Optional process/workflow ID or exact display name. Ambiguous names fail with candidate IDs instead of guessing. Type: string. Pass null to clear the field.<br>`--card <value>` — Optional card/document ID or exact title. If a title matches multiple cards, the call fails with candidates. Type: string. Pass null to clear the field.<br>`--status <value>` — Optional execution status filter: active, done, or cancelled. Allowed values: "active" \| "done" \| "cancelled". Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of executions to return (default: 50, maximum: 200). Type: integer. Pass null to clear the field. |
| `huly processes get <process>` | Get Process | `<process>` — Process/workflow ID or exact display name. Ambiguous names fail with candidate IDs instead of guessing. Type: string. | — | — |
| `huly processes list` | List Processes | — | — | `--master-tag <value>` — Optional master tag/card type ID or display label. Use this when you only want workflows attached to one Huly card/document type. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of process definitions to return (default: 50, maximum: 200). Type: integer. Pass null to clear the field. |
| `huly processes start <process> <card>` | Start a process on a card or document Requires `--yes`. | `<process>` — Process/workflow ID or exact display name. Ambiguous names fail with candidate IDs instead of guessing. Type: string.<br>`<card>` — Card/document ID or exact title to attach the new execution to. If a title matches multiple cards, the call fails with candidates. Type: string. | — | — |
| `huly project-types get` | Get Project Type | — | — | `--project-type <value>` — Project type ID or display name. If omitted, uses the unambiguous Classic tracker project type. Type: string. Pass null to clear the field. |
| `huly project-types list` | List Project Types | — | — | — |
| `huly projects create <name> <identifier>` | Create Project | `<name>` — Project name Type: string.<br>`<identifier>` — Unique project identifier, 1-5 uppercase alphanumeric chars starting with letter (e.g., 'HULY', 'QA') Type: string. Pattern: ^[A-Z][A-Z0-9_]{0,4}$. | — | `--description <value>` — Project description Type: string. Pass null to clear the field.<br>`--private` / `--no-private` — Whether project is private (default: false) Type: boolean. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly projects delete <project>` | Delete Project Requires `--yes`. | `<project>` — Project identifier to delete Type: string. | — | — |
| `huly projects get <project>` | Get a project | `<project>` — Project identifier (e.g., 'HULY') Type: string. | — | — |
| `huly projects list` | List projects | — | — | `--include-archived` / `--no-include-archived` — Include archived projects in results (default: false, showing only active) Type: boolean. Pass null to clear the field.<br>`--limit <value>` — Maximum number of projects to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly projects statuses <project>` | List project issue statuses | `<project>` — Project identifier (e.g., 'HULY') Type: string. | — | — |
| `huly projects target-preferences list` | List Project Target Preferences | — | — | `--project <value>` — Optional project identifier. Omit to list recent preferences across projects. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of preferences to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly projects target-preferences upsert <project>` | Create or update a project target preference; pass --props as a JSON array Requires `--yes`. | `<project>` — Project identifier whose target preference record should be created or updated. Type: string. | — | `--props <value>` — SDK-open target preference properties to merge by key while preserving other existing properties. Type: array. Pass arrays or objects as JSON. Pass null to clear the field. |
| `huly projects update <project>` | Update Project | `<project>` — Project identifier to update Type: string. | — | `--name <value>` — New project name Type: string. Choose one input alternative: { name } \| { description }. Pass null to clear the field.<br>`--description <value>` — New description (null to clear) Type: string. Choose one input alternative: { name } \| { description }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly recruiting activity list <target>` | List Recruiting Activity | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Type: object. JSON shape alternatives: { kind, vacancy } \| { kind, candidate } \| { kind, applicant } \| { kind, review }. Pass arrays or objects as JSON. | — | `--limit <value>` — Maximum number of activity messages to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly recruiting applicant create <vacancy> <candidate>` | Create Recruiting Applicant | `<vacancy>` — Vacancy locator. Type: string.<br>`<candidate>` — Candidate locator. Type: string. | — | `--status <value>` — Exact Recruiting status name. Type: string. Pass null to clear the field.<br>`--assignee <value>` — Employee assignee locator. Type: string. Pass null to clear the field.<br>`--start-date <value>` — Applicant start timestamp. Type: integer. Pass null to clear the field.<br>`--due-date <value>` — Applicant due timestamp. Type: integer. Pass null to clear the field. |
| `huly recruiting applicant delete <applicant>` | Delete Recruiting Applicant Requires `--yes`. | `<applicant>` — Applicant locator. Type: string. | — | `--vacancy <value>` — Vacancy locator. Type: string. Pass null to clear the field.<br>`--candidate <value>` — Candidate locator. Type: string. Pass null to clear the field. |
| `huly recruiting applicant get <applicant>` | Get Recruiting Applicant | `<applicant>` — Applicant locator. Type: string. | — | `--vacancy <value>` — Vacancy locator. Type: string. Pass null to clear the field.<br>`--candidate <value>` — Candidate locator. Type: string. Pass null to clear the field. |
| `huly recruiting applicant match get <match>` | Get Recruiting Applicant Match | `<match>` — Applicant-match locator. Type: string. | — | — |
| `huly recruiting applicant matches list` | List Recruiting Applicant Matches | — | — | `--candidate <value>` — Candidate locator. Type: string. Pass null to clear the field.<br>`--complete` / `--no-complete` — Filter by completion state. Type: boolean. Pass null to clear the field.<br>`--query <value>` — Case-insensitive search text. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of records to return. Type: integer. Pass null to clear the field. |
| `huly recruiting applicant update <applicant>` | Update Recruiting Applicant | `<applicant>` — Applicant locator. Type: string. | — | `--vacancy <value>` — Vacancy locator. Type: string. Pass null to clear the field.<br>`--candidate <value>` — Candidate locator. Type: string. Pass null to clear the field.<br>`--status <value>` — Exact Recruiting status name. Type: string. Choose one input alternative: { status } \| { assignee } \| { startDate } \| { dueDate }. Pass null to clear the field.<br>`--assignee <value>` — Employee assignee locator. Type: string. Choose one input alternative: { status } \| { assignee } \| { startDate } \| { dueDate }. Pass null to clear the field.<br>`--start-date <value>` — Applicant start timestamp. Type: integer. Choose one input alternative: { status } \| { assignee } \| { startDate } \| { dueDate }. Pass null to clear the field.<br>`--due-date <value>` — Applicant due timestamp. Type: integer. Choose one input alternative: { status } \| { assignee } \| { startDate } \| { dueDate }. Pass null to clear the field. |
| `huly recruiting applicants list` | List Recruiting Applicants | — | — | `--vacancy <value>` — Vacancy locator. Type: string. Pass null to clear the field.<br>`--candidate <value>` — Candidate locator. Type: string. Pass null to clear the field.<br>`--status <value>` — Exact Recruiting status name. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of records to return. Type: integer. Pass null to clear the field. |
| `huly recruiting attachment delete <target> <attachmentId>` | Delete Recruiting Attachment Requires `--yes`. | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Type: object. JSON shape alternatives: { kind, vacancy } \| { kind, candidate } \| { kind, applicant } \| { kind, opinion }. Pass arrays or objects as JSON.<br>`<attachmentId>` — Attachment ID. Must belong directly to the resolved Recruiting target. Type: string. | — | — |
| `huly recruiting attachment get <target> <attachmentId>` | Get Recruiting Attachment | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Type: object. JSON shape alternatives: { kind, vacancy } \| { kind, candidate } \| { kind, applicant } \| { kind, opinion }. Pass arrays or objects as JSON.<br>`<attachmentId>` — Attachment ID. Must belong directly to the resolved Recruiting target. Type: string. | — | — |
| `huly recruiting attachment update <target> <attachmentId>` | Update Recruiting Attachment | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Type: object. JSON shape alternatives: { kind, vacancy } \| { kind, candidate } \| { kind, applicant } \| { kind, opinion }. Pass arrays or objects as JSON.<br>`<attachmentId>` — Attachment ID. Must belong directly to the resolved Recruiting target. Type: string. | — | `--description <value>` — New attachment description; null clears it. Type: string. Choose one input alternative: { description } \| { pinned }. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Pin or unpin the attachment. Type: boolean. Choose one input alternative: { description } \| { pinned }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly recruiting attachments add <target> <filename> <contentType>` | Add a recruiting attachment. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Type: object. JSON shape alternatives: { kind, vacancy } \| { kind, candidate } \| { kind, applicant } \| { kind, opinion }. Pass arrays or objects as JSON.<br>`<filename>` — Name of the file to attach to the Recruiting object. Type: string.<br>`<contentType>` — MIME type of the file, such as image/png or application/pdf. Type: string. | — | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Optional attachment description. Type: string. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether the attachment should be pinned. Type: boolean. Pass null to clear the field.<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly recruiting attachments list <target>` | List Recruiting Attachments | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Type: object. JSON shape alternatives: { kind, vacancy } \| { kind, candidate } \| { kind, applicant } \| { kind, opinion }. Pass arrays or objects as JSON. | — | `--limit <value>` — Maximum number of attachments to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly recruiting candidate get <candidate>` | Get Recruiting Candidate | `<candidate>` — Candidate locator. Type: string. | — | — |
| `huly recruiting candidate profile set <candidate>` | Set Recruiting Candidate Profile | `<candidate>` — Candidate locator. Type: string. | — | `--title <value>` — Recruiting title or search text. Type: string. Choose one input alternative: { title } \| { source } \| { onsite } \| { remote }. Pass null to clear the field.<br>`--source <value>` — Candidate source text. Type: string. Choose one input alternative: { title } \| { source } \| { onsite } \| { remote }. Pass null to clear the field.<br>`--onsite` / `--no-onsite` — Whether the candidate accepts onsite work. Type: boolean. Choose one input alternative: { title } \| { source } \| { onsite } \| { remote }. Pass null to clear the field.<br>`--remote` / `--no-remote` — Whether the candidate accepts remote work. Type: boolean. Choose one input alternative: { title } \| { source } \| { onsite } \| { remote }. Pass null to clear the field. |
| `huly recruiting candidate skill add <candidate> <skill>` | Add Recruiting Candidate Skill | `<candidate>` — Candidate locator. Type: string.<br>`<skill>` — Skill locator. Type: string. | — | `--category <value>` — Skill category. Type: string. Pass null to clear the field.<br>`--color <value>` — Skill color. Type: integer. Pass null to clear the field.<br>`--weight <value>` — Skill weight. Allowed values: 0 \| 1 \| 2 \| 3 \| 4 \| 5 \| 6 \| 7 \| 8. Type: number. Pass null to clear the field. |
| `huly recruiting candidate skill remove <candidate> <skill>` | Remove Recruiting Candidate Skill Requires `--yes`. | `<candidate>` — Candidate locator. Type: string.<br>`<skill>` — Skill locator. Type: string. | — | — |
| `huly recruiting candidate skills list <candidate>` | List Recruiting Candidate Skills | `<candidate>` — Candidate locator. Type: string. | — | — |
| `huly recruiting candidates list` | List Recruiting Candidates | — | — | `--query <value>` — Case-insensitive search text. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of records to return. Type: integer. Pass null to clear the field. |
| `huly recruiting comment add <target> <body>` | Add Recruiting Comment | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Type: object. JSON shape alternatives: { kind, vacancy } \| { kind, candidate } \| { kind, applicant } \| { kind, review } \| { kind, opinion }. Pass arrays or objects as JSON.<br>`<body>` — Comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--body-file <path>` — read body as text |
| `huly recruiting comment delete <target> <commentId>` | Delete Recruiting Comment Requires `--yes`. | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Type: object. JSON shape alternatives: { kind, vacancy } \| { kind, candidate } \| { kind, applicant } \| { kind, review } \| { kind, opinion }. Pass arrays or objects as JSON.<br>`<commentId>` — Comment ID. Must belong directly to the resolved Recruiting target. Type: string. | — | — |
| `huly recruiting comment update <target> <commentId> <body>` | Update Recruiting Comment | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Type: object. JSON shape alternatives: { kind, vacancy } \| { kind, candidate } \| { kind, applicant } \| { kind, review } \| { kind, opinion }. Pass arrays or objects as JSON.<br>`<commentId>` — Comment ID. Must belong directly to the resolved Recruiting target. Type: string.<br>`<body>` — New comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. | — | `--body-file <path>` — read body as text |
| `huly recruiting comments list <target>` | List Recruiting Comments | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Type: object. JSON shape alternatives: { kind, vacancy } \| { kind, candidate } \| { kind, applicant } \| { kind, review } \| { kind, opinion }. Pass arrays or objects as JSON. | — | `--limit <value>` — Maximum number of comments to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly recruiting opinion create <review> <value>` | Create Recruiting Opinion | `<review>` — Review locator. Type: string.<br>`<value>` — Opinion value. Type: string. | — | `--description <value>` — Markdown description. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly recruiting opinion delete <opinion>` | Delete Recruiting Opinion Requires `--yes`. | `<opinion>` — Opinion locator. Type: string. | — | `--review <value>` — Review locator. Type: string. Pass null to clear the field. |
| `huly recruiting opinion get <opinion>` | Get Recruiting Opinion | `<opinion>` — Opinion locator. Type: string. | — | `--review <value>` — Review locator. Type: string. Pass null to clear the field. |
| `huly recruiting opinion update <opinion>` | Update Recruiting Opinion | `<opinion>` — Opinion locator. Type: string. | — | `--review <value>` — Review locator. Type: string. Pass null to clear the field.<br>`--value <value>` — Opinion value. Type: string. Choose one input alternative: { value } \| { description }. Pass null to clear the field.<br>`--description <value>` — Markdown description. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Choose one input alternative: { value } \| { description }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly recruiting opinions list <review>` | List Recruiting Opinions | `<review>` — Review locator. Type: string. | — | `--limit <value>` — Maximum number of records to return. Type: integer. Pass null to clear the field. |
| `huly recruiting related issue add <target> <issue>` | Add Recruiting Related Issue | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Type: object. JSON shape alternatives: { kind, vacancy } \| { kind, candidate } \| { kind, applicant }. Pass arrays or objects as JSON.<br>`<issue>` — Issue identifier, such as HULY-123, or a numeric issue number when project is also provided. Type: string. | — | `--project <value>` — Project identifier. Optional when issue already includes a project prefix like HULY-123. Type: string. Pass null to clear the field. |
| `huly recruiting related issue remove <target> <issue>` | Remove Recruiting Related Issue Requires `--yes`. | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Type: object. JSON shape alternatives: { kind, vacancy } \| { kind, candidate } \| { kind, applicant }. Pass arrays or objects as JSON.<br>`<issue>` — Issue identifier, such as HULY-123, or a numeric issue number when project is also provided. Type: string. | — | `--project <value>` — Project identifier. Optional when issue already includes a project prefix like HULY-123. Type: string. Pass null to clear the field. |
| `huly recruiting related issues list <target>` | List Recruiting Related Issues | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Type: object. JSON shape alternatives: { kind, vacancy } \| { kind, candidate } \| { kind, applicant }. Pass arrays or objects as JSON. | — | `--limit <value>` — Maximum number of related issues to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly recruiting review create <candidate> <title> <date>` | Create Recruiting Review | `<candidate>` — Candidate locator. Type: string.<br>`<title>` — Review title. Type: string.<br>`<date>` — Review date timestamp. Type: integer. | — | `--due-date <value>` — Review due timestamp. Type: integer. Pass null to clear the field.<br>`--description <value>` — Markdown description. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--verdict <value>` — Review verdict. Type: string. Pass null to clear the field.<br>`--application <value>` — Applicant/application locator. Type: string. Pass null to clear the field.<br>`--company <value>` — Company organization locator. Type: string. Pass null to clear the field.<br>`--location <value>` — Review location. Type: string. Pass null to clear the field.<br>`--participants <value>` — Calendar participant locators. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly recruiting review delete <review>` | Delete Recruiting Review Requires `--yes`. | `<review>` — Review locator. Type: string. | — | `--candidate <value>` — Candidate locator. Type: string. Pass null to clear the field.<br>`--application <value>` — Applicant/application locator. Type: string. Pass null to clear the field. |
| `huly recruiting review get <review>` | Get Recruiting Review | `<review>` — Review locator. Type: string. | — | `--candidate <value>` — Candidate locator. Type: string. Pass null to clear the field.<br>`--application <value>` — Applicant/application locator. Type: string. Pass null to clear the field. |
| `huly recruiting review update <review>` | Update Recruiting Review | `<review>` — Review locator. Type: string. | — | `--candidate <value>` — Candidate locator. Type: string. Pass null to clear the field.<br>`--application-context <value>` — Applicant/application context locator. Type: string. Pass null to clear the field.<br>`--title <value>` — Review title. Type: string. Choose one input alternative: { title } \| { description } \| { verdict } \| { date } \| { dueDate } \| { application } \| { company } \| { location } \| { participants }. Pass null to clear the field.<br>`--description <value>` — Markdown description. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Choose one input alternative: { title } \| { description } \| { verdict } \| { date } \| { dueDate } \| { application } \| { company } \| { location } \| { participants }. Pass null to clear the field.<br>`--verdict <value>` — Review verdict. Type: string. Choose one input alternative: { title } \| { description } \| { verdict } \| { date } \| { dueDate } \| { application } \| { company } \| { location } \| { participants }. Pass null to clear the field.<br>`--date <value>` — Review date timestamp. Type: integer. Choose one input alternative: { title } \| { description } \| { verdict } \| { date } \| { dueDate } \| { application } \| { company } \| { location } \| { participants }. Pass null to clear the field.<br>`--due-date <value>` — Review due timestamp. Type: integer. Choose one input alternative: { title } \| { description } \| { verdict } \| { date } \| { dueDate } \| { application } \| { company } \| { location } \| { participants }. Pass null to clear the field.<br>`--application <value>` — Applicant/application locator. Type: string. Choose one input alternative: { title } \| { description } \| { verdict } \| { date } \| { dueDate } \| { application } \| { company } \| { location } \| { participants }. Pass null to clear the field.<br>`--company <value>` — Company organization locator. Type: string. Choose one input alternative: { title } \| { description } \| { verdict } \| { date } \| { dueDate } \| { application } \| { company } \| { location } \| { participants }. Pass null to clear the field.<br>`--location <value>` — Review location. Type: string. Choose one input alternative: { title } \| { description } \| { verdict } \| { date } \| { dueDate } \| { application } \| { company } \| { location } \| { participants }. Pass null to clear the field.<br>`--participants <value>` — Calendar participant locators. Type: array. Choose one input alternative: { title } \| { description } \| { verdict } \| { date } \| { dueDate } \| { application } \| { company } \| { location } \| { participants }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly recruiting reviews list` | List Recruiting Reviews | — | — | `--candidate <value>` — Candidate locator. Type: string. Pass null to clear the field.<br>`--application <value>` — Applicant/application locator. Type: string. Pass null to clear the field.<br>`--query <value>` — Case-insensitive search text. Type: string. Pass null to clear the field.<br>`--from <value>` — Inclusive lower timestamp bound. Type: integer. Pass null to clear the field.<br>`--to <value>` — Inclusive upper timestamp bound. Type: integer. Pass null to clear the field.<br>`--limit <value>` — Maximum number of records to return. Type: integer. Pass null to clear the field. |
| `huly recruiting skills list` | List Recruiting Skills | — | — | `--title-search <value>` — Case-insensitive skill title search text. Type: string. Pass null to clear the field.<br>`--category <value>` — Skill category. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of records to return. Type: integer. Pass null to clear the field. |
| `huly recruiting vacancies list` | List Recruiting Vacancies | — | — | `--include-archived` / `--no-include-archived` — Include archived records. Type: boolean. Pass null to clear the field.<br>`--query <value>` — Case-insensitive search text. Type: string. Pass null to clear the field.<br>`--type <value>` — Vacancy type locator. Type: string. Pass null to clear the field.<br>`--company <value>` — Company organization locator. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of records to return. Type: integer. Pass null to clear the field. |
| `huly recruiting vacancy archive <vacancy>` | Archive Recruiting Vacancy | `<vacancy>` — Vacancy locator. Type: string. | — | — |
| `huly recruiting vacancy create <name>` | Create Recruiting Vacancy | `<name>` — Vacancy name. Type: string. | — | `--short-description <value>` — Short vacancy description. Type: string. Pass null to clear the field.<br>`--full-description <value>` — Full vacancy description in Markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--type <value>` — Vacancy type locator. Type: string. Pass null to clear the field.<br>`--company <value>` — Company organization locator. Type: string. Pass null to clear the field.<br>`--location <value>` — Vacancy location. Type: string. Pass null to clear the field.<br>`--due-to <value>` — Vacancy due timestamp. Type: integer. Pass null to clear the field.<br>`--private` / `--no-private` — Whether the vacancy is private. Type: boolean. Pass null to clear the field.<br>`--short-description-file <path>` — read shortDescription as text<br>`--full-description-file <path>` — read fullDescription as text |
| `huly recruiting vacancy get <vacancy>` | Get Recruiting Vacancy | `<vacancy>` — Vacancy locator. Type: string. | — | — |
| `huly recruiting vacancy statuses list <vacancy>` | List Recruiting Vacancy Statuses | `<vacancy>` — Vacancy locator. Type: string. | — | — |
| `huly recruiting vacancy types list` | List Recruiting Vacancy Types | — | — | `--include-archived` / `--no-include-archived` — Include archived records. Type: boolean. Pass null to clear the field.<br>`--limit <value>` — Maximum number of records to return. Type: integer. Pass null to clear the field. |
| `huly recruiting vacancy unarchive <vacancy>` | Unarchive Recruiting Vacancy Requires `--yes`. | `<vacancy>` — Vacancy locator. Type: string. | — | — |
| `huly recruiting vacancy update <vacancy>` | Update Recruiting Vacancy | `<vacancy>` — Vacancy locator. Type: string. | — | `--name <value>` — Vacancy name. Type: string. Choose one input alternative: { name } \| { shortDescription } \| { fullDescription } \| { type } \| { company } \| { location } \| { dueTo } \| { private }. Pass null to clear the field.<br>`--short-description <value>` — Short vacancy description. Type: string. Choose one input alternative: { name } \| { shortDescription } \| { fullDescription } \| { type } \| { company } \| { location } \| { dueTo } \| { private }. Pass null to clear the field.<br>`--full-description <value>` — Full vacancy description in Markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Choose one input alternative: { name } \| { shortDescription } \| { fullDescription } \| { type } \| { company } \| { location } \| { dueTo } \| { private }. Pass null to clear the field.<br>`--type <value>` — Vacancy type locator. Type: string. Choose one input alternative: { name } \| { shortDescription } \| { fullDescription } \| { type } \| { company } \| { location } \| { dueTo } \| { private }. Pass null to clear the field.<br>`--company <value>` — Company organization locator. Type: string. Choose one input alternative: { name } \| { shortDescription } \| { fullDescription } \| { type } \| { company } \| { location } \| { dueTo } \| { private }. Pass null to clear the field.<br>`--location <value>` — Vacancy location. Type: string. Choose one input alternative: { name } \| { shortDescription } \| { fullDescription } \| { type } \| { company } \| { location } \| { dueTo } \| { private }. Pass null to clear the field.<br>`--due-to <value>` — Vacancy due timestamp. Type: integer. Choose one input alternative: { name } \| { shortDescription } \| { fullDescription } \| { type } \| { company } \| { location } \| { dueTo } \| { private }. Pass null to clear the field.<br>`--private` / `--no-private` — Whether the vacancy is private. Type: boolean. Choose one input alternative: { name } \| { shortDescription } \| { fullDescription } \| { type } \| { company } \| { location } \| { dueTo } \| { private }. Pass null to clear the field.<br>`--short-description-file <path>` — read shortDescription as text<br>`--full-description-file <path>` — read fullDescription as text |
| `huly relations create <association>` | Create a relation; pass --source and --target as JSON | `<association>` — Association ID or unambiguous name returned by list_associations. Type: string. | `--source <value>` — Source endpoint document. Type: object. JSON shape alternatives: { kind, id } \| { kind, issue } \| { kind, document } \| { kind, card }. Pass arrays or objects as JSON.<br>`--target <value>` — Target endpoint document. Type: object. JSON shape alternatives: { kind, id } \| { kind, issue } \| { kind, document } \| { kind, card }. Pass arrays or objects as JSON. | `--direction <value>` — Relation traversal direction: source-to-target, target-to-source, either. Defaults to source-to-target. Allowed values: "source-to-target" \| "target-to-source" \| "either". Type: string. Pass null to clear the field.<br>`--if-exists <value>` — return_existing returns an existing relation; fail reports it as an error. Allowed values: "return_existing" \| "fail". Type: string. Pass null to clear the field. |
| `huly relations delete` | Delete a relation by ID or exact structured endpoints Requires `--yes`. | — | — | `--relation <value>` — Concrete relation ID to delete. Type: string. Choose one input alternative: { relation } \| { association, source, target }.<br>`--association <value>` — Association ID or unambiguous name. Type: string. Choose one input alternative: { relation } \| { association, source, target }.<br>`--source <value>` — Source endpoint document. Type: object. JSON shape alternatives: { kind, id } \| { kind, issue } \| { kind, document } \| { kind, card }. Choose one input alternative: { relation } \| { association, source, target }. Pass arrays or objects as JSON.<br>`--target <value>` — Target endpoint document. Type: object. JSON shape alternatives: { kind, id } \| { kind, issue } \| { kind, document } \| { kind, card }. Choose one input alternative: { relation } \| { association, source, target }. Pass arrays or objects as JSON.<br>`--direction <value>` — Relation traversal direction: source-to-target, target-to-source, either. Defaults to source-to-target. Allowed values: "source-to-target" \| "target-to-source" \| "either". Type: string. Pass null to clear the field. |
| `huly search <query>` | Search Huly | `<query>` — Non-empty full-text search query. Type: string. | — | `--limit <value>` — Maximum number of results to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly spaces admins get` | Get Global Space Admins | — | — | — |
| `huly spaces admins set` | Set Global Space Admins Requires `--yes`. | — | `--admins <value>` — Complete replacement global space-admin list. Accepts account UUIDs, exact emails, or exact person display names; pass [] to clear the role. Type: array. Pass arrays or objects as JSON. | — |
| `huly spaces create <spaceType> <name>` | Create Generic Typed Space | `<spaceType>` — SpaceType _id or exact name. Generic creation is accepted only when SDK descriptor metadata proves the type uses core:class:TypedSpace directly. Type: string.<br>`<name>` — New space display name. Type: string. | — | `--description <value>` — Plain-text space description. Type: string. Pass null to clear the field.<br>`--private` / `--no-private` — Whether the space is private. Type: boolean. Pass null to clear the field.<br>`--auto-join` / `--no-auto-join` — Whether workspace members auto-join the space. Type: boolean. Pass null to clear the field.<br>`--restricted` / `--no-restricted` — Whether transactions require an assigned space-type permission. Type: boolean. Pass null to clear the field.<br>`--members <value>` — Initial members. SpaceType default members, owners, and role-assigned members are always included as well. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--owners <value>` — Initial owners. Defaults to the calling Huly account; every owner is also made a member. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--role-assignments <value>` — Optional role assignments created in the same MCP call after the space document is created. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly spaces get <space>` | Get Space | `<space>` — Space _id or exact space name. Resolution tries _id first, then exact name. Duplicate names require class and/or type narrowing. Type: string. | — | `--include-archived` / `--no-include-archived` — Allow matching archived spaces by exact name. Defaults to false for name lookup. ID lookup can return archived spaces. Type: boolean. Pass null to clear the field.<br>`--class <value>` — Optional raw Huly space class ID used to disambiguate exact-name lookup. Type: string. Pass null to clear the field.<br>`--type <value>` — Optional raw Huly SpaceType _id used to disambiguate exact-name lookup. Type: string. Pass null to clear the field. |
| `huly spaces list` | List Spaces | — | — | `--include-archived` / `--no-include-archived` — Include archived spaces in results. Defaults to false, so only active/non-archived spaces are returned. Type: boolean. Pass null to clear the field.<br>`--class <value>` — Optional raw Huly space class ID to filter results, for example 'tracker:class:Project' or 'document:class:Teamspace'. Type: string. Pass null to clear the field.<br>`--type <value>` — Optional raw Huly SpaceType _id to filter typed spaces. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum spaces to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly spaces members add <space> <members>` | Add people to a space; members is a JSON array Requires `--yes`. | `<space>` — Space _id or exact space name whose members should change. Use class/type narrowing if the name is shared by multiple spaces. Type: string.<br>`<members>` — Members to add or remove. Each entry may be an account UUID, exact email address, or exact person name. Type: array. Pass arrays or objects as JSON. | — | `--class <value>` — Optional raw Huly space class ID used to disambiguate exact-name lookup. Type: string. Pass null to clear the field.<br>`--type <value>` — Optional raw Huly SpaceType _id used to disambiguate exact-name lookup. Type: string. Pass null to clear the field. |
| `huly spaces members remove <space> <members>` | Remove people from a space; members is a JSON array Requires `--yes`. | `<space>` — Space _id or exact space name whose members should change. Use class/type narrowing if the name is shared by multiple spaces. Type: string.<br>`<members>` — Members to add or remove. Each entry may be an account UUID, exact email address, or exact person name. Type: array. Pass arrays or objects as JSON. | — | `--class <value>` — Optional raw Huly space class ID used to disambiguate exact-name lookup. Type: string. Pass null to clear the field.<br>`--type <value>` — Optional raw Huly SpaceType _id used to disambiguate exact-name lookup. Type: string. Pass null to clear the field. |
| `huly spaces owners set <space> <owners>` | Replace space owners; owners is a JSON array Requires `--yes`. | `<space>` — Space _id or exact space name whose owners should be replaced. Use class/type narrowing if the name is shared by multiple spaces. Type: string.<br>`<owners>` — Replacement owner list. Each entry may be an account UUID, exact email address, or exact person name. Pass [] to clear owners. Type: array. Pass arrays or objects as JSON. | — | `--class <value>` — Optional raw Huly space class ID used to disambiguate exact-name lookup. Type: string. Pass null to clear the field.<br>`--type <value>` — Optional raw Huly SpaceType _id used to disambiguate exact-name lookup. Type: string. Pass null to clear the field.<br>`--ensure-members` / `--no-ensure-members` — Also add each owner to members. Defaults to true. Type: boolean. Pass null to clear the field. |
| `huly spaces permissions list` | List Space Permissions | — | — | `--scope <value>` — Filter permissions by Huly scope: space or workspace. Allowed values: "space" \| "workspace". Type: string. Pass null to clear the field.<br>`--object-class <value>` — Filter permissions bound to this raw Huly object class ID. Type: string. Pass null to clear the field.<br>`--search <value>` — Case-insensitive substring search across permission id, label, and description. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum permissions to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly spaces roles create <spaceType> <name> <permissions>` | Create Space Role Requires `--yes`. | `<spaceType>` — SpaceType ID or exact SpaceType name. Type: string.<br>`<name>` — Unique role name within the selected SpaceType. Type: string.<br>`<permissions>` — Permission IDs or exact labels; duplicates are rejected case-insensitively. Type: array. Pass arrays or objects as JSON. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. Allowed values: true. Type: boolean. | — |
| `huly spaces roles members add <space> <role> <members>` | Add space-role members; members is a JSON array Requires `--yes`. | `<space>` — Typed space _id or exact space name whose role assignment should change. The space must have a SpaceType. Type: string.<br>`<role>` — Role to resolve within the space's SpaceType. Accepts a raw Huly role _id or an exact role name from get_space_type. Type: string.<br>`<members>` — Members to add or remove from this role. Each entry may be an account UUID, exact email address, or exact person name. Type: array. Pass arrays or objects as JSON. | — | `--class <value>` — Optional raw Huly space class ID used to disambiguate exact-name lookup. Type: string. Pass null to clear the field.<br>`--type <value>` — Optional raw Huly SpaceType _id used to disambiguate exact-name lookup. Type: string. Pass null to clear the field. |
| `huly spaces roles members remove <space> <role> <members>` | Remove space-role members; members is a JSON array Requires `--yes`. | `<space>` — Typed space _id or exact space name whose role assignment should change. The space must have a SpaceType. Type: string.<br>`<role>` — Role to resolve within the space's SpaceType. Accepts a raw Huly role _id or an exact role name from get_space_type. Type: string.<br>`<members>` — Members to add or remove from this role. Each entry may be an account UUID, exact email address, or exact person name. Type: array. Pass arrays or objects as JSON. | — | `--class <value>` — Optional raw Huly space class ID used to disambiguate exact-name lookup. Type: string. Pass null to clear the field.<br>`--type <value>` — Optional raw Huly SpaceType _id used to disambiguate exact-name lookup. Type: string. Pass null to clear the field. |
| `huly spaces roles members set <space> <role> <members>` | Replace space-role members; members is a JSON array Requires `--yes`. | `<space>` — Typed space _id or exact space name whose role assignment should change. The space must have a SpaceType. Type: string.<br>`<role>` — Role to resolve within the space's SpaceType. Accepts a raw Huly role _id or an exact role name from get_space_type. Type: string.<br>`<members>` — Replacement member list for this role only. Each entry may be an account UUID, exact email address, or exact person name. Pass [] to clear this role. Type: array. Pass arrays or objects as JSON. | — | `--class <value>` — Optional raw Huly space class ID used to disambiguate exact-name lookup. Type: string. Pass null to clear the field.<br>`--type <value>` — Optional raw Huly SpaceType _id used to disambiguate exact-name lookup. Type: string. Pass null to clear the field. |
| `huly spaces roles permissions set <spaceType> <role> <permissions>` | Set Space Role Permissions Requires `--yes`. | `<spaceType>` — SpaceType ID or exact SpaceType name. Type: string.<br>`<role>` — Role ID or exact role name within the selected SpaceType. Type: string.<br>`<permissions>` — Replacement permission IDs or exact labels; duplicates are rejected case-insensitively. Type: array. Pass arrays or objects as JSON. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. Allowed values: true. Type: boolean. | — |
| `huly spaces types get <spaceType>` | Get Space Type | `<spaceType>` — SpaceType _id or exact name. Resolution tries _id first, then exact name. Type: string. | — | — |
| `huly spaces types list` | List Space Types | — | — | `--target-class <value>` — Optional raw Huly target space class ID to filter configured space types. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum space types to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly spaces update <space>` | Update a generic space | `<space>` — Space _id or exact space name to update. Use class/type narrowing if the name is shared by multiple spaces. Type: string. | — | `--class <value>` — Optional raw Huly space class ID used to disambiguate exact-name lookup. Type: string. Pass null to clear the field.<br>`--type <value>` — Optional raw Huly SpaceType _id used to disambiguate exact-name lookup. Type: string. Pass null to clear the field.<br>`--name <value>` — New space display name. Type: string. Choose one input alternative: { name } \| { description } \| { private } \| { archived } \| { autoJoin }. Pass null to clear the field.<br>`--description <value>` — New plain-text description. Pass null to clear; empty string is also accepted. Type: string. Choose one input alternative: { name } \| { description } \| { private } \| { archived } \| { autoJoin }. Pass null to clear the field.<br>`--private` / `--no-private` — Whether the space is private. Type: boolean. Choose one input alternative: { name } \| { description } \| { private } \| { archived } \| { autoJoin }. Pass null to clear the field.<br>`--archived` / `--no-archived` — Whether the space is archived. Type: boolean. Choose one input alternative: { name } \| { description } \| { private } \| { archived } \| { autoJoin }. Pass null to clear the field.<br>`--auto-join` / `--no-auto-join` — Whether workspace members should auto-join the space when supported. Type: boolean. Choose one input alternative: { name } \| { description } \| { private } \| { archived } \| { autoJoin }. Pass null to clear the field. |
| `huly status-categories create <ofAttribute> <label> <defaultStatus>` | Create Generic Status Category | `<ofAttribute>` — Status attribute ID or exact attribute name. Type: string.<br>`<label>` — Status-category label text or Huly internationalized-string identifier. Type: string.<br>`<defaultStatus>` — Status ID or exact name to use as the category default. It must belong to ofAttribute. Type: string. | — | `--icon <value>` — Exact Huly asset identifier for a status-category icon. Type: string. Pass null to clear the field.<br>`--color <value>` — Huly numeric color token or non-empty numeric gradient token array. Type: integer \| array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--order <value>` — Whole number. Type: integer. Pass null to clear the field. |
| `huly status-categories delete <category>` | Delete Generic Status Category Requires `--yes`. | `<category>` — Category ID or exact case-insensitive label. Type: string. | — | `--of-attribute <value>` — Status attribute ID or exact attribute name. Use this to filter results and disambiguate names shared across attributes. Type: string. Pass null to clear the field. |
| `huly status-categories get <category>` | Get Generic Status Category | `<category>` — Category ID or exact case-insensitive label. Type: string. | — | `--of-attribute <value>` — Status attribute ID or exact attribute name. Use this to filter results and disambiguate names shared across attributes. Type: string. Pass null to clear the field. |
| `huly status-categories list` | List Generic Status Categories | — | — | `--of-attribute <value>` — Status attribute ID or exact attribute name. Use this to filter results and disambiguate names shared across attributes. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum categories to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly status-categories update <category>` | Update Generic Status Category | `<category>` — Category ID or exact case-insensitive label. Type: string. | — | `--current-of-attribute <value>` — Status attribute ID or exact attribute name. Use this to filter results and disambiguate names shared across attributes. Type: string. Pass null to clear the field.<br>`--label <value>` — Status-category label text or Huly internationalized-string identifier. Type: string. Choose one input alternative: { label } \| { ofAttribute } \| { defaultStatus } \| { icon } \| { color } \| { order }. Pass null to clear the field.<br>`--of-attribute <value>` — Type: string. Choose one input alternative: { label } \| { ofAttribute } \| { defaultStatus } \| { icon } \| { color } \| { order }. Pass null to clear the field.<br>`--default-status <value>` — New default status ID or exact name. It must belong to the category's resulting attribute. Type: string. Choose one input alternative: { label } \| { ofAttribute } \| { defaultStatus } \| { icon } \| { color } \| { order }. Pass null to clear the field.<br>`--icon <value>` — Exact Huly asset identifier for a status-category icon. Type: string. Choose one input alternative: { label } \| { ofAttribute } \| { defaultStatus } \| { icon } \| { color } \| { order }. Pass null to clear the field.<br>`--color <value>` — Huly numeric color token or non-empty numeric gradient token array. Type: integer \| array. Choose one input alternative: { label } \| { ofAttribute } \| { defaultStatus } \| { icon } \| { color } \| { order }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--order <value>` — Whole number. Type: integer. Choose one input alternative: { label } \| { ofAttribute } \| { defaultStatus } \| { icon } \| { color } \| { order }. Pass null to clear the field. |
| `huly storage upload <filename> <contentType>` | Upload a file to Huly storage. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<filename>` — Name of the file (e.g., 'screenshot.png') Type: string.<br>`<contentType>` — MIME type of the file (e.g., 'image/png', 'application/pdf') Type: string. | — | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly support status get` | Get Support Status | — | — | — |
| `huly tags attached list` | List Attached Tags | — | `--object-id <value>` — Raw Huly object ID that owns the tag reference. Type: string.<br>`--object-class <value>` — Raw Huly class or mixin of the object receiving the tag reference. Type: string.<br>`--space <value>` — Huly space ID where the tag reference is stored. Type: string.<br>`--collection <value>` — Collection field on the object that stores tag references. Type: string. | — |
| `huly tags categories create <label>` | Create Tag Category | `<label>` — Category name. Type: string. | — | `--target-class <value>` — Target class for this category (default: tracker:class:Issue). Type: string. Pass null to clear the field.<br>`--default` / `--no-default` — Whether this is a default category (default: false). Type: boolean. Pass null to clear the field. |
| `huly tags categories delete <category>` | Delete Tag Category Requires `--yes`. | `<category>` — Category ID or exact label to delete. Type: string. | — | — |
| `huly tags categories list` | List Tag Categories | — | — | `--target-class <value>` — Filter by target class ID. Omit to include all classes. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of categories to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly tags categories update <category>` | Update Tag Category | `<category>` — Category ID or exact label to update. Type: string. | — | `--label <value>` — New category name. Type: string. Choose one input alternative: { label } \| { default }. Pass null to clear the field.<br>`--default` / `--no-default` — New default-category flag. Type: boolean. Choose one input alternative: { label } \| { default }. Pass null to clear the field. |
| `huly tags create <targetClass> <title>` | Create Tag | `<targetClass>` — Huly class or mixin this tag definition applies to, such as tracker:class:Issue or recruit:mixin:Candidate. Type: string.<br>`<title>` — Tag title. Type: string. | — | `--color <value>` — Huly color palette index from 0 through 23 (default: 0). Type: integer. Pass null to clear the field.<br>`--description <value>` — Tag description. Type: string. Pass null to clear the field.<br>`--category <value>` — Category ID or label within targetClass; omitting it uses the default or no-category bucket. Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tags delete <targetClass> <tag>` | Delete Tag Requires `--yes`. | `<targetClass>` — Huly class or mixin this tag definition applies to, such as tracker:class:Issue or recruit:mixin:Candidate. Type: string.<br>`<tag>` — Tag ID or exact title scoped to targetClass. Type: string. | — | — |
| `huly tags list <targetClass>` | List Tags | `<targetClass>` — Huly class or mixin this tag definition applies to, such as tracker:class:Issue or recruit:mixin:Candidate. Type: string. | — | `--category <value>` — Filter by tag category ID or label within targetClass. Type: string. Pass null to clear the field.<br>`--title-search <value>` — Case-insensitive tag-title substring search. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of tags to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly tags update <targetClass> <tag>` | Update Tag | `<targetClass>` — Huly class or mixin this tag definition applies to, such as tracker:class:Issue or recruit:mixin:Candidate. Type: string.<br>`<tag>` — Tag ID or exact title scoped to targetClass. Type: string. | — | `--title <value>` — New tag title. Type: string. Choose one input alternative: { title } \| { color } \| { description } \| { category }. Pass null to clear the field.<br>`--color <value>` — New Huly color palette index from 0 through 23. Type: integer. Choose one input alternative: { title } \| { color } \| { description } \| { category }. Pass null to clear the field.<br>`--description <value>` — New tag description; null clears it. Type: string. Choose one input alternative: { title } \| { color } \| { description } \| { category }. Pass null to clear the field.<br>`--category <value>` — New category ID or label within targetClass. Type: string. Choose one input alternative: { title } \| { color } \| { description } \| { category }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly task-types create <name>` | Create Task Type | `<name>` — Display name for the new issue/task type. Type: string. | — | `--project-type <value>` — Project type ID or display name. If omitted, uses the unambiguous Classic tracker project type. Type: string. Pass null to clear the field.<br>`--template-task-type <value>` — Existing task type ID or display name to copy required Huly configuration from. Omit to copy from the first task type on the project type. Type: string. Pass null to clear the field. |
| `huly task-types list` | List Task Types | — | — | `--project-type <value>` — Optional project type ID or display name. If omitted, returns task types from all project types. Type: string. Pass null to clear the field. |
| `huly teamspaces create <name>` | Create Teamspace | `<name>` — Teamspace name Type: string. | — | `--description <value>` — Teamspace description Type: string. Pass null to clear the field.<br>`--private` / `--no-private` — Whether the teamspace is private (default: false) Type: boolean. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly teamspaces delete <teamspace>` | Delete Teamspace Requires `--yes`. | `<teamspace>` — Teamspace name or ID Type: string. | — | — |
| `huly teamspaces get <teamspace>` | Get a teamspace | `<teamspace>` — Teamspace name or ID Type: string. | — | — |
| `huly teamspaces list` | List teamspaces | — | — | `--include-archived` / `--no-include-archived` — Include archived teamspaces in results (default: false, showing only active) Type: boolean. Pass null to clear the field.<br>`--limit <value>` — Maximum number of teamspaces to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly teamspaces update <teamspace>` | Update Teamspace | `<teamspace>` — Teamspace name or ID Type: string. | — | `--name <value>` — New teamspace name Type: string. Choose one input alternative: { name } \| { description } \| { archived }. Pass null to clear the field.<br>`--description <value>` — New description (null to clear) Type: string. Choose one input alternative: { name } \| { description } \| { archived }. Pass null to clear the field.<br>`--archived` / `--no-archived` — Set archived status Type: boolean. Choose one input alternative: { name } \| { description } \| { archived }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly templates categories list` | List Message Template Categories | — | — | `--limit <value>` — Maximum number of template categories to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly templates fields list` | List Message Template Fields | — | — | `--category <value>` — Optional template field category ID or exact raw label string. Type: string. Pass null to clear the field.<br>`--search <value>` — Optional case-insensitive substring search over raw template field labels. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of template fields to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly templates get <template>` | Get Message Template | `<template>` — Template ID or exact template title. If title is ambiguous, also provide category. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate template title lookup. Type: string. Pass null to clear the field. |
| `huly templates list` | List Message Templates | — | — | `--category <value>` — Optional category filter. Accepts a category ID or exact category name. Type: string. Pass null to clear the field.<br>`--search <value>` — Optional case-insensitive substring search over template titles. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of templates to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly templates render <template>` | Render a message template; pass --values as a JSON object | `<template>` — Template ID or exact template title. If title is ambiguous, also provide category. Type: string. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate template title lookup. Type: string. Pass null to clear the field.<br>`--values <value>` — Optional caller-provided placeholder values. Each field is a template field ID from placeholderFieldIds; duplicate field entries use the last value. Type: array. Pass arrays or objects as JSON. Pass null to clear the field. |
| `huly tests case create <project> <suite> <name>` | Create Test Case | `<project>` — Test project ID or name Type: string.<br>`<suite>` — Suite ID or name to attach the test case to Type: string.<br>`<name>` — Test case name Type: string. | — | `--description <value>` — Test case description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--type <value>` — Test case type (default: functional) Allowed values: "functional" \| "performance" \| "regression" \| "security" \| "smoke" \| "usability". Type: string. Pass null to clear the field.<br>`--priority <value>` — Test case priority (default: medium) Allowed values: "low" \| "medium" \| "high" \| "urgent". Type: string. Pass null to clear the field.<br>`--status <value>` — Test case status (default: draft) Allowed values: "draft" \| "ready-for-review" \| "fix-review-comments" \| "approved" \| "rejected". Type: string. Pass null to clear the field.<br>`--assignee <value>` — Assignee name or email Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests case delete <project> <testCase>` | Delete Test Case Requires `--yes`. | `<project>` — Test project ID or name Type: string.<br>`<testCase>` — Test case ID or name to delete Type: string. | — | — |
| `huly tests case get <project> <testCase>` | Get Test Case | `<project>` — Test project ID or name Type: string.<br>`<testCase>` — Test case ID or name Type: string. | — | — |
| `huly tests case update <project> <testCase>` | Update Test Case | `<project>` — Test project ID or name Type: string.<br>`<testCase>` — Test case ID or name to update Type: string. | — | `--name <value>` — New test case name Type: string. Choose one input alternative: { name } \| { description } \| { type } \| { priority } \| { status } \| { assignee }. Pass null to clear the field.<br>`--description <value>` — New test case description in markdown, or null to clear. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Choose one input alternative: { name } \| { description } \| { type } \| { priority } \| { status } \| { assignee }. Pass null to clear the field.<br>`--type <value>` — New test case type Allowed values: "functional" \| "performance" \| "regression" \| "security" \| "smoke" \| "usability". Type: string. Choose one input alternative: { name } \| { description } \| { type } \| { priority } \| { status } \| { assignee }. Pass null to clear the field.<br>`--priority <value>` — New priority Allowed values: "low" \| "medium" \| "high" \| "urgent". Type: string. Choose one input alternative: { name } \| { description } \| { type } \| { priority } \| { status } \| { assignee }. Pass null to clear the field.<br>`--status <value>` — New status Allowed values: "draft" \| "ready-for-review" \| "fix-review-comments" \| "approved" \| "rejected". Type: string. Choose one input alternative: { name } \| { description } \| { type } \| { priority } \| { status } \| { assignee }. Pass null to clear the field.<br>`--assignee <value>` — New assignee name or email (null to unassign) Type: string. Choose one input alternative: { name } \| { description } \| { type } \| { priority } \| { status } \| { assignee }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests cases list <project>` | List Test Cases | `<project>` — Test project ID or name Type: string. | — | `--suite <value>` — Filter by suite ID or name Type: string. Pass null to clear the field.<br>`--assignee <value>` — Filter by assignee name or email Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of test cases to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly tests plan create <project> <name>` | Create Test Plan | `<project>` — Test project ID or name Type: string.<br>`<name>` — Name Type: string. | — | `--description <value>` — Description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests plan delete <project> <plan>` | Delete Test Plan Requires `--yes`. | `<project>` — Test project ID or name Type: string.<br>`<plan>` — Test plan ID or name Type: string. | — | — |
| `huly tests plan get <project> <plan>` | Get Test Plan | `<project>` — Test project ID or name Type: string.<br>`<plan>` — Test plan ID or name Type: string. | — | — |
| `huly tests plan item add <project> <plan> <testCase>` | Add Test Plan Item | `<project>` — Test project ID or name Type: string.<br>`<plan>` — Test plan ID or name Type: string.<br>`<testCase>` — Test case ID or name to add Type: string. | — | `--assignee <value>` — Assignee email or name Type: string. Pass null to clear the field. |
| `huly tests plan item remove <project> <plan> <item>` | Remove Test Plan Item Requires `--yes`. | `<project>` — Test project ID or name Type: string.<br>`<plan>` — Test plan ID or name Type: string.<br>`<item>` — Test plan item ID to remove Type: string. | — | — |
| `huly tests plan run <project> <plan>` | Run Test Plan | `<project>` — Test project ID or name Type: string.<br>`<plan>` — Test plan ID or name Type: string. | — | `--run-name <value>` — Name for the created test run Type: string. Pass null to clear the field.<br>`--due-date <value>` — Due date as Unix timestamp in milliseconds Type: integer. Pass null to clear the field. |
| `huly tests plan update <project> <plan>` | Update Test Plan | `<project>` — Test project ID or name Type: string.<br>`<plan>` — Test plan ID or name Type: string. | — | `--name <value>` — Name Type: string. Choose one input alternative: { name } \| { description }. Pass null to clear the field.<br>`--description <value>` — Description in markdown, or null to clear. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Choose one input alternative: { name } \| { description }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests plans list <project>` | List Test Plans | `<project>` — Test project ID or name Type: string. | — | `--limit <value>` — Max items to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly tests projects list` | List Test Projects | — | — | `--limit <value>` — Maximum number of projects to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly tests result create <project> <run> <testCase>` | Create Test Result | `<project>` — Test project ID or name Type: string.<br>`<run>` — Test run ID or name Type: string.<br>`<testCase>` — Test case ID or name Type: string. | — | `--name <value>` — Result name. Omit to use the test case name. Type: string. Pass null to clear the field.<br>`--status <value>` — Status: untested, blocked, passed, failed Allowed values: "untested" \| "blocked" \| "passed" \| "failed". Type: string. Pass null to clear the field.<br>`--assignee <value>` — Assignee email or name Type: string. Pass null to clear the field. |
| `huly tests result delete <project> <result>` | Delete Test Result Requires `--yes`. | `<project>` — Test project ID or name Type: string.<br>`<result>` — Test result ID or name Type: string. | — | — |
| `huly tests result get <project> <result>` | Get Test Result | `<project>` — Test project ID or name Type: string.<br>`<result>` — Test result ID or name Type: string. | — | — |
| `huly tests result update <project> <result>` | Update Test Result | `<project>` — Test project ID or name Type: string.<br>`<result>` — Test result ID or name Type: string. | — | `--status <value>` — Status: untested, blocked, passed, failed Allowed values: "untested" \| "blocked" \| "passed" \| "failed". Type: string. Choose one input alternative: { status } \| { assignee } \| { description }. Pass null to clear the field.<br>`--assignee <value>` — Assignee email or name, or null to unassign Type: string. Choose one input alternative: { status } \| { assignee } \| { description }. Pass null to clear the field.<br>`--description <value>` — Description in markdown, or null to clear. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Choose one input alternative: { status } \| { assignee } \| { description }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests results list <project> <run>` | List Test Results | `<project>` — Test project ID or name Type: string.<br>`<run>` — Test run ID or name Type: string. | — | `--limit <value>` — Max items to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly tests run create <project> <name>` | Create Test Run | `<project>` — Test project ID or name Type: string.<br>`<name>` — Name Type: string. | — | `--description <value>` — Description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Pass null to clear the field.<br>`--due-date <value>` — Due date as Unix timestamp in milliseconds Type: integer. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests run delete <project> <run>` | Delete Test Run Requires `--yes`. | `<project>` — Test project ID or name Type: string.<br>`<run>` — Test run ID or name Type: string. | — | — |
| `huly tests run get <project> <run>` | Get Test Run | `<project>` — Test project ID or name Type: string.<br>`<run>` — Test run ID or name Type: string. | — | — |
| `huly tests run update <project> <run>` | Update Test Run | `<project>` — Test project ID or name Type: string.<br>`<run>` — Test run ID or name Type: string. | — | `--name <value>` — Name Type: string. Choose one input alternative: { name } \| { description } \| { dueDate }. Pass null to clear the field.<br>`--description <value>` — Description in markdown, or null to clear. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Type: string. Choose one input alternative: { name } \| { description } \| { dueDate }. Pass null to clear the field.<br>`--due-date <value>` — Due date (ms timestamp), or null to clear Type: integer. Choose one input alternative: { name } \| { description } \| { dueDate }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests runs list <project>` | List Test Runs | `<project>` — Test project ID or name Type: string. | — | `--limit <value>` — Max items to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly tests suite create <project> <name>` | Create Test Suite | `<project>` — Test project ID or name Type: string.<br>`<name>` — Suite name Type: string. | — | `--description <value>` — Suite description Type: string. Pass null to clear the field.<br>`--parent <value>` — Parent suite ID or name for nesting Type: string. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests suite delete <project> <suite>` | Delete Test Suite Requires `--yes`. | `<project>` — Test project ID or name Type: string.<br>`<suite>` — Test suite ID or name to delete Type: string. | — | — |
| `huly tests suite get <project> <suite>` | Get Test Suite | `<project>` — Test project ID or name Type: string.<br>`<suite>` — Test suite ID or name Type: string. | — | — |
| `huly tests suite update <project> <suite>` | Update Test Suite | `<project>` — Test project ID or name Type: string.<br>`<suite>` — Test suite ID or name to update Type: string. | — | `--name <value>` — New suite name Type: string. Choose one input alternative: { name } \| { description }. Pass null to clear the field.<br>`--description <value>` — New suite description (null to clear) Type: string. Choose one input alternative: { name } \| { description }. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests suites list <project>` | List Test Suites | `<project>` — Test project ID or name Type: string. | — | `--parent <value>` — Filter by parent suite ID or name. Only returns direct children of this suite. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of suites to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly time log <project> <identifier> <value>` | Log time in hours against an issue | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<identifier>` — Issue identifier (e.g., 'HULY-123' or just '123') Type: string.<br>`<value>` — Time spent in hours (Huly native unit). Examples: 0.25 = 15 minutes; 8 = one work day. Allowed values: "Infinity" \| "-Infinity" \| "NaN". Type: number \| string. | — | `--description <value>` — Description of work done Type: string. Pass null to clear the field. |
| `huly time reports detailed get <project>` | Get Detailed Time Report | `<project>` — Project identifier (e.g., 'HULY') Type: string. | — | `--from <value>` — Filter entries from this timestamp Type: integer. Pass null to clear the field.<br>`--to <value>` — Filter entries until this timestamp Type: integer. Pass null to clear the field. |
| `huly time reports get <project> <identifier>` | Get Time Report | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<identifier>` — Issue identifier (e.g., 'HULY-123' or just '123') Type: string. | — | — |
| `huly time reports list` | List Time Spend Reports | — | — | `--project <value>` — Filter by project identifier Type: string. Pass null to clear the field.<br>`--from <value>` — Filter entries from this timestamp Type: integer. Pass null to clear the field.<br>`--to <value>` — Filter entries until this timestamp Type: integer. Pass null to clear the field.<br>`--limit <value>` — Maximum number of entries to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly time timers start <project> <identifier>` | Start an issue timer | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<identifier>` — Issue identifier (e.g., 'HULY-123' or just '123') Type: string. | — | — |
| `huly time timers stop <project> <identifier>` | Stop an issue timer | `<project>` — Project identifier (e.g., 'HULY') Type: string.<br>`<identifier>` — Issue identifier (e.g., 'HULY-123' or just '123') Type: string. | — | — |
| `huly time work-slots list` | List Work Slots | — | — | `--employee-id <value>` — Filter by employee ID or email Type: string. Pass null to clear the field.<br>`--from <value>` — Filter slots from this timestamp Type: integer. Pass null to clear the field.<br>`--to <value>` — Filter slots until this timestamp Type: integer. Pass null to clear the field.<br>`--limit <value>` — Maximum number of slots to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly user-statuses list` | List User Statuses | — | — | `--online` / `--no-online` — Optional presence filter. Use true for currently connected users, false for offline records. Type: boolean. Pass null to clear the field.<br>`--user <value>` — Optional Huly account UUID filter. Pass the exact account UUID from a user status row. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of user status records to return (default: 50, maximum: 200). Type: integer. Pass null to clear the field. |
| `huly views filtered get <filteredView>` | Get Filtered View | `<filteredView>` — FilteredView _id or exact saved view name. Type: string. | — | `--attached-to <value>` — Optional raw Huly app/resource string such as board:app:Board to disambiguate exact-name matches. Type: string. Pass null to clear the field. |
| `huly views filtered list` | List Filtered Views | — | — | `--attached-to <value>` — Raw Huly app or resource string used to scope saved filtered views. Type: string. Pass null to clear the field.<br>`--visibility <value>` — Filter by whether the current account owns or shares the saved view. Allowed values: "own" \| "shared" \| "all". Type: string. Pass null to clear the field.<br>`--name-search <value>` — Optional saved-view name substring search. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of saved filtered views to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly views viewlets list` | List Viewlets | — | — | `--attach-to <value>` — Huly class ID rendered by the viewlet. Type: string. Pass null to clear the field.<br>`--viewlet <value>` — Viewlet ID, exact title, exact variant, or descriptor ID. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum number of viewlets to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly workbench applications list` | List Workbench Applications | — | — | `--alias <value>` — Exact application URL alias. Duplicate exact aliases are rejected as ambiguous. Type: string. Pass null to clear the field.<br>`--alias-search <value>` — Case-insensitive application alias substring. Mutually exclusive with alias. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum applications to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly workflow-statuses create <ofAttribute> <name>` | Create Generic Workflow Status | `<ofAttribute>` — Status attribute ID or exact attribute name. Type: string.<br>`<name>` — Workflow status display name. Type: string. | — | `--category <value>` — Category ID or exact label. Categories may be shared across status attributes; pass an ID when a label is ambiguous. Type: string. Pass null to clear the field.<br>`--color <value>` — Huly numeric color token or non-empty numeric gradient token array. Type: integer \| array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--description <value>` — Type: string. Pass null to clear the field. |
| `huly workflow-statuses delete <status>` | Delete Generic Workflow Status Requires `--yes`. | `<status>` — Status ID or exact case-insensitive name. Type: string. | — | `--of-attribute <value>` — Status attribute ID or exact attribute name. Use this to filter results and disambiguate names shared across attributes. Type: string. Pass null to clear the field. |
| `huly workflow-statuses get <status>` | Get Generic Workflow Status | `<status>` — Status ID or exact case-insensitive name. Type: string. | — | `--of-attribute <value>` — Status attribute ID or exact attribute name. Use this to filter results and disambiguate names shared across attributes. Type: string. Pass null to clear the field. |
| `huly workflow-statuses list` | List Generic Workflow Statuses | — | — | `--of-attribute <value>` — Status attribute ID or exact attribute name. Use this to filter results and disambiguate names shared across attributes. Type: string. Pass null to clear the field.<br>`--category <value>` — Category ID or exact label to filter by. Type: string. Pass null to clear the field.<br>`--limit <value>` — Maximum statuses to return (default: 50). Type: integer. Pass null to clear the field. |
| `huly workflow-statuses update <status>` | Update Generic Workflow Status | `<status>` — Status ID or exact case-insensitive name. Type: string. | — | `--current-of-attribute <value>` — Status attribute ID or exact attribute name. Use this to filter results and disambiguate names shared across attributes. Type: string. Pass null to clear the field.<br>`--name <value>` — New display name. Type: string. Choose one input alternative: { name } \| { ofAttribute } \| { category } \| { color } \| { description }. Pass null to clear the field.<br>`--of-attribute <value>` — New owning attribute. Moves are rejected while the status is referenced or when the target attribute requires a different concrete Status class. Type: string. Choose one input alternative: { name } \| { ofAttribute } \| { category } \| { color } \| { description }. Pass null to clear the field.<br>`--category <value>` — New category ID or exact label; pass null to remove the relationship. Categories may be shared across status attributes. Type: string. Choose one input alternative: { name } \| { ofAttribute } \| { category } \| { color } \| { description }. Pass null to clear the field.<br>`--color <value>` — New color; pass null to clear it. Type: integer \| array. Choose one input alternative: { name } \| { ofAttribute } \| { category } \| { color } \| { description }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--description <value>` — New description; pass null to clear it. Type: string. Choose one input alternative: { name } \| { ofAttribute } \| { category } \| { color } \| { description }. Pass null to clear the field. |
| `huly workspace access-links create` | Create a workspace access link Requires `--yes`. | — | — | `--role <value>` — Workspace role granted by the link. Defaults to GUEST. Allowed values: "READONLYGUEST" \| "DocGuest" \| "GUEST" \| "USER" \| "MAINTAINER" \| "OWNER" \| "ADMIN". Type: string. Pass null to clear the field.<br>`--first-name <value>` — Optional first name for personalized links. Type: string. Pass null to clear the field.<br>`--last-name <value>` — Optional last name for personalized links. Type: string. Pass null to clear the field.<br>`--navigate-url <value>` — Optional URL/path Huly should open after the link is used. Type: string. Pass null to clear the field.<br>`--spaces <value>` — Optional Huly space IDs this link should grant access to. Use list_teamspaces, list_card_spaces, or other list tools to discover space IDs. Type: array. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--not-before <value>` — Unix timestamp in seconds before which the link is invalid. Type: integer. Pass null to clear the field.<br>`--expiration <value>` — Unix timestamp in seconds after which the link expires. Type: integer. Pass null to clear the field.<br>`--personalized` / `--no-personalized` — Whether the link is bound to one person. Omit to use Huly's personalized-link behavior. Allowed values: true \| false. Type: boolean. Pass null to clear the field. |
| `huly workspace create <name>` | Create a workspace Requires `--yes`. | `<name>` — Name for the new workspace Type: string. | — | `--region <value>` — Region for the workspace (optional) Type: string. Pass null to clear the field. |
| `huly workspace delete` | Delete the configured workspace Requires `--yes`. | — | — | — |
| `huly workspace guest-settings update` | Update workspace guest access settings Requires `--yes`. | — | — | `--allow-read-only` / `--no-allow-read-only` — Allow read-only guests Type: boolean. Choose one input alternative: { allowReadOnly } \| { allowSignUp }. Pass null to clear the field.<br>`--allow-sign-up` / `--no-allow-sign-up` — Allow guest sign-up Type: boolean. Choose one input alternative: { allowReadOnly } \| { allowSignUp }. Pass null to clear the field. |
| `huly workspace info get` | Get Workspace Info | — | — | — |
| `huly workspace list` | List Workspaces | — | — | `--limit <value>` — Maximum number of workspaces to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly workspace members list` | List Workspace Members | — | — | `--limit <value>` — Maximum number of members to return (default: 50) Type: integer. Pass null to clear the field. |
| `huly workspace members role update <accountId> <role>` | Update a workspace member role Requires `--yes`. | `<accountId>` — Account UUID of the member Type: string.<br>`<role>` — New role for the member Allowed values: "READONLYGUEST" \| "DocGuest" \| "GUEST" \| "USER" \| "MAINTAINER" \| "OWNER" \| "ADMIN". Type: string. | — | — |
| `huly workspace profile get` | Get User Profile | — | — | — |
| `huly workspace profile update` | Update the current user's profile; pass null to clear nullable fields Requires `--yes`. | — | — | `--bio <value>` — Bio text (null to clear) Type: string. Choose one input alternative: { bio } \| { city } \| { country } \| { website } \| { socialLinks } \| { isPublic }. Pass null to clear the field.<br>`--city <value>` — City (null to clear) Type: string. Choose one input alternative: { bio } \| { city } \| { country } \| { website } \| { socialLinks } \| { isPublic }. Pass null to clear the field.<br>`--country <value>` — Country (null to clear) Type: string. Choose one input alternative: { bio } \| { city } \| { country } \| { website } \| { socialLinks } \| { isPublic }. Pass null to clear the field.<br>`--website <value>` — Website URL (null to clear) Type: string. Choose one input alternative: { bio } \| { city } \| { country } \| { website } \| { socialLinks } \| { isPublic }. Pass null to clear the field.<br>`--social-links <value>` — Social links as key-value pairs (null to clear) Type: object. Choose one input alternative: { bio } \| { city } \| { country } \| { website } \| { socialLinks } \| { isPublic }. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--is-public` / `--no-is-public` — Whether profile is public Type: boolean. Choose one input alternative: { bio } \| { city } \| { country } \| { website } \| { socialLinks } \| { isPublic }. Pass null to clear the field. |
| `huly workspace regions get` | Get Regions | — | — | — |
<!-- CLI_COMMAND_REFERENCE_END -->

</details>
