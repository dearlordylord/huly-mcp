# Huly SDK / Dev-Lib Feature Gap Matrix

Scope: features that are represented in the local Huly SDK/dev reference tree or installed `@hcengineering/*` packages, but are not exposed by this MCP server as first-class LLM-facing tools.

Audit guard: `plans/sdk-parity-ledger.json` classifies every installed `@hcengineering/*` plugin class or model-like export and every `.reference/platform/models/*` area discovered by `scripts/audit-sdk-parity.mjs` as `covered`, `gap`, `ignored`, or `not-mcp-facing`. `pnpm verify-sdk-parity` fails when a newly installed SDK model export or reference model area is not classified here.

Local evidence sources:

- Installed SDK packages: `node_modules/@hcengineering/*`
- Huly platform reference: `.reference/platform/plugins/*`, `.reference/platform/models/*`, `.reference/platform/server-plugins/*`
- Current MCP surface: `src/mcp/tools/*`, registered from `src/mcp/tools/index.ts`

Current high-level MCP categories:

`projects`, `issues`, `comments`, `milestones`, `documents`, `storage`, `attachments`, `contacts`, `channels`, `mail`, `support`, `workbench`, `calendar`, `time tracking`, `search`, `associations`, `activity`, `approvals`, `notifications`, `workspace`, `spaces`, `cards`, `boards`, `collaborators`, `custom-fields`, `drive`, `inventory`, `labels`, `leads`, `planner`, `processes`, `sdk-discovery`, `tags`, `tag-categories`, `templates`, `task-management`, `test-management`, `user-statuses`, `virtual-office`.

## Missing Or Partially Missing SDK Surfaces

| SDK / dev-lib area | Evidence in Huly libs | Current MCP coverage | Missing MCP feature surface |
|---|---|---|---|
| Controlled Documents / TraceX documents | `.reference/platform/models/controlled-documents/src/types.ts`: `DocumentSpace`, `DocumentSpaceType`, `OrgSpace`, `ExternalSpace`, `Project`, `Document`, `HierarchyDocument`, `DocumentTemplate`, `DocumentTraining`, `DocumentCategory`, `ControlledDocument`, `ChangeControl`, `DocumentSnapshot`, `ControlledDocumentSnapshot`, `DocumentComment`, `DocumentReviewRequest`, `DocumentApprovalRequest` | Basic non-controlled teamspaces/documents only via `documents` tools | Controlled document spaces/projects, controlled document CRUD, quality/technical docs, document templates, categories, review/approval requests, co-authors/reviewers/approvers, e-signature workflow, release/effective-date metadata, change control, snapshots/history, document training linkage, controlled-document comments/resolve |
| Products / product versions | `.reference/platform/models/products/src/index.ts`: `Product`, `ProductVersion`, `ProductTypeData`, `TypeProductVersionState` | No product tools; generic documents may not model product spaces | Product spaces, product members, product descriptions/attachments, product version CRUD, version state transitions, version change-control link |
| Trainings | `.reference/platform/models/training/src/types.ts`: `Training`, `TrainingRequest`, `TrainingAttempt`, state/attempt-state types | No training tools | Training CRUD, revisions, description/attachments, passing score, release, questions collection, assign requests to trainees/roles, due date/max attempts, cancel requests, attempts, submit/score attempts, results/reporting |
| Questions / assessments | `.reference/platform/models/questions/src/doc-types/*`: `Question`, `Assessment`, `Answer`, single-choice, multiple-choice, ordering types and answer data | No question-bank or assessment tools | Question CRUD, assessment CRUD, answer options, correct-answer/assessment data, ordering questions, answer submission/readback, reusable question model for trainings/surveys |
| Drive | `.reference/platform/models/drive/src/index.ts`: `Drive`, `Resource`, `Folder`, `File`, `FileVersion`, `TypeFileVersion` | First-class `drive` tools cover drive CRUD, member/owner administration, path traversal, folder creation, file upload and new versions, item move/rename/delete, version list/restore, and file comments/activity. | None for the installed Drive SDK surface; generic space administration supplies the shared permission foundation. |
| Human Resources | `.reference/platform/models/hr/src/index.ts`: `Department`, `Staff`, `RequestType`, `Request`, `PublicHoliday`, `TzDate` | Department hierarchy CRUD; server-derived department membership; managers/subscribers/team lead; Staff assignment; request-type discovery; request CRUD, comments, and attachments; public-holiday CRUD; complete schedule/table/summary projections | None for the installed `@hcengineering/hr@0.7.0` SDK surface |
| Recruiting | `.reference/platform/models/recruit/src/types.ts`: `Vacancy`, `Candidate`, `VacancyList`, `Applicant`, `ApplicantMatch`, `Review`, `Opinion`; statuses in `.reference/platform/models/recruit/src/spaceType.ts` | Vacancy, candidate, applicant, match, review, opinion, skill, comment, attachment, activity, and related-issue workflows are first-class and use Recruiting-aware locators. Existing people can be Candidate-enabled directly; new people use the documented `create_person` then `set_recruiting_candidate_profile` composition. | Recruiting-friendly custom-field read/write remains under #264. No separate Candidate entity or partial one-call creator will be added. |
| Surveys / polls | `.reference/platform/models/survey/src/types.ts`: `Survey`, `Poll` | No survey tools | Survey CRUD, poll creation/attachment, survey question data, poll completion/status, survey results |
| Generic approval requests | `.reference/platform/models/request/src/index.ts`: `Request`, `RequestDecisionComment`, request presenters; installed `@hcengineering/request@0.7.0` | `approvals` tools cover list/get/create, comments, approve/reject/cancel, statuses, requested/approved/rejected people, decision data, authorization, and opaque transaction payloads. | Friendly attached-target resolution beyond raw ids/classes is an LLM-first workflow enhancement; no installed `Request` model export remains uncovered. |
| Boards | Installed `@hcengineering/board`; `.reference/platform/models/board/src/index.ts`: `Board`, board `Card`, card cover, board preferences, menu pages | First-class `boards` tools cover board/card lifecycle, workflows, members/assignees, location, cover, dates, labels, menu pages, saved views, viewlets, and common board preference reads. The unrelated `cards` category covers the Huly Card module. | Provider integrations require separate provider contracts; hard board deletion remains intentionally excluded in favor of archive behavior. |
| Inventory | `.reference/platform/models/inventory/src/index.ts`: inventory `Category`, `Product`, `Variant`; installed `@hcengineering/inventory@0.7.0` | Category hierarchy CRUD, product CRUD, variant/SKU CRUD, and product-scoped photo, attachment, comment, and activity wrappers as first-class `inventory` tools, using exact write-side resolution and non-empty delete guards | Category/variant discussion wrappers remain out of the current product-focused slice |
| Leads write surface | `.reference/platform/models/lead/src/types.ts`: `Funnel`, `Lead`, `Customer`, `DefaultFunnelTypeData`, `LeadTypeData` | Funnel and Lead lifecycle, workflow/status changes, assignment, dates, customer descriptions, person-customer enablement, and lead comments/attachments/labels/relations are covered. | None for the stable modeled Lead/Funnel fields currently classified as LLM-facing. |
| Contact channels and identities | `.reference/platform/models/contact/src/index.ts`: `Channel`, `SocialIdentity`, `ChannelProvider`, `SocialIdentityProvider`, `Status`, `PersonSpace`, `WorkspaceMemberStatus`, `Employee` mixin | Person/organization/employee lifecycle, channels, provider and identity discovery/repair, notes, attachments, merge, invitations, deactivation, inactive employees, and stable status/profile fields are covered. | Full channel-provider administration, channel-item internals, PersonSpace, and UserProfile card administration remain separate lower-level model gaps. |
| Calendar schedules and integrations | Installed `@hcengineering/calendar`; `.reference/platform/models/calendar/src/index.ts`: `Calendar`, `ExternalCalendar`, `Event`, recurring events/instances, `Schedule`, `PrimaryCalendar` | Event/recurrence/Schedule workflows and writable-calendar discovery are covered. | #266 tracks caller-scoped Calendar settings, PrimaryCalendar preference, and local ExternalCalendar visibility. Provider identifiers/lifecycle and Calendar deletion are excluded; RSVP remains blocked until Huly publishes a provider-neutral contract. |
| Planner / ToDos | Installed `@hcengineering/time`; `.reference/platform/models/time/src/index.ts`: `ToDo`, `ProjectToDo`, `WorkSlot`, `TodoAutomationHelper`, todo priority | Work slots, issue time reporting, and Planner ToDo lifecycle with personal/issue attachment are covered. `TodoAutomationHelper` is server-only executable plumbing and is not MCP-facing. | #265 tracks native document-attached ToDos. |
| Team planner / schedule reporting | `.reference/platform/models/time`, `.reference/platform/models/calendar`, HR docs/models | Work slots, ToDos, calendar events, and project membership can be composed. | A privacy-safe team planner projection is deferred until private events and non-public ToDo details can be centrally redacted. Capacity is not defined by the current Huly model and is not planned. |
| Virtual office / meetings | Installed `@hcengineering/love@0.7.0`; `.reference/platform/models/love/src/index.ts`: `Room`, `Office`, `Floor`, `ParticipantInfo`, `RoomInfo`, `Meeting` mixin, `MeetingMinutes`, `MeetingSchedule`; settings in `setting.class.OfficeSettings` | Read-only durable and transient discovery is covered. | #267 tracks bounded durable Floor/Room administration; #268 tracks room-aware Meeting/MeetingSchedule composition. Transient writes, join/leave, LiveKit state, transcription, and recording are excluded or blocked. OfficeSettings awaits a compatible published declaration. |
| Chat channel membership/options | Installed `@hcengineering/chunter`; `.reference/platform/models/chunter/src/types.ts`: `Channel`, `DirectMessage`, `ChatMessage`, `ThreadMessage` | Channel CRUD, channel message writes, one-to-one direct-message create/list/message list/send/update/delete, thread replies, channel member list/add/remove, join/leave, archive/unarchive, conversation star/closed state, group direct-message create, message attachments, and locator-backed pinned-message list/set. Request-access and translation return explicit unsupported results with stable reason codes. | Request-access if Huly adds a stable request model, server-side translation if Huly adds a stable API, and chat viewlet/sync metadata/object chat panel internals |
| Communication applets and chat polls | `.reference/platform/models/communication/src/types.ts`: `MessageAction`, `Applet`, `PollAnswer`, `GuestCommunicationSettings` | No communication applet/poll tools | Chat applets, in-message poll creation/answers, custom activity presenters, guest communication settings |
| Inbox / notification gaps | Installed `@hcengineering/notification`; `.reference/platform/models/notification/src/index.ts`: `InboxNotification`, `DocNotifyContext`, provider/type settings, hidden context, collaborator mixin | Notifications list/get/read/unread/archive/unarchive/delete, contexts list/get/pin/hide/unhide/archive/unarchive, provider/type discovery, provider/type setting writes, current-account object subscribe/unsubscribe, and generic object collaborator list/add/remove | Browser/push subscription internals, provider default documents, and UI presenter/group metadata remain non-MCP-facing; rating-backed stars remain blocked by the unpublished rating package |
| Activity gaps | Installed `@hcengineering/activity`; `.reference/platform/models/activity/src/index.ts`: pinned messages, replies, references, filters, saved messages, reactions, mentions | List/get/pin activity, generic activity replies, activity filters, activity references, reactions, save/unsave, mentions, and update/reference/info message summaries | Presenter/viewlet/extension/control metadata remains non-MCP-facing |
| Attachments gaps | Installed `@hcengineering/attachment`; `.reference/platform/models/attachment/src/index.ts`: `Attachment`, `Embedding`, `Photo`, `SavedAttachments`, `Drawing` | Generic attachment CRUD/download/pin with attachment/embedding/photo class selection, saved attachments, drawing CRUD, metadata return, and issue/document convenience add | Attachment previews/preview UI metadata and object-specific friendly wrappers beyond issue/document |
| Tags/labels beyond tracker issues | Installed `@hcengineering/tags`; `.reference/platform/models/tags/src/index.ts`: `TagElement`, `TagReference`, `TagCategory` with arbitrary `targetClass` | SDK-level generic tags plus friendly issue, document, Planner ToDo, board, and Recruiting-skill wrappers are covered. | Controlled-document labels remain package-blocked; contact tags need a stable exposed collection before a friendly wrapper is justified. |
| Core classes, attributes, enums | Installed `@hcengineering/core`; `.reference/platform/models/core/src/core.ts`: `Class`, `Mixin`, `Enum`, `Attribute`, type classes, sequence/custom sequence | Class/mixin/interface discovery, attribute and enum discovery/guarded CRUD, sequence/custom-sequence discovery and guarded writes, plugin/index configuration discovery, and workflow status/category CRUD are covered. | Specialized permission/configuration internals and built-in model mutation remain guarded lower-level gaps. |
| Core spaces, permissions, roles | Installed `@hcengineering/core`; `.reference/platform/models/core/src/security.ts`: `Space`, `TypedSpace`, `SpaceType`, `Role`, `Permission`, permission groups | Generic space discovery/creation, safe metadata/member/owner/global-admin administration, typed-space roles, permission definitions, role permission replacement, and class/object collaborator metadata are covered. | Specialized `AttributePermission`/`ClassPermission` and raw system-space internals remain lower-level gaps; module-specific wrappers require separate workflow value. |
| Settings / integrations registry | `.reference/platform/models/setting/src/index.ts`: `Integration`, `IntegrationType`, settings categories, invite settings, role capability settings, office settings, workspace settings | Guest settings and access links, workspace info/profile | List integrations/integration types, connect/disconnect/configure integration records where backend supports it, invite settings, role capability settings, office settings, workspace setting icon/metadata |
| GitHub integration | Huly docs plus integration architecture in settings; PR task type appears through tracker/task type model when GitHub is enabled | No GitHub integration tools; issue tools may operate on synced issues after setup | Connect/list mapped repositories, map/unmap repo to project, list pull requests, issue GitHub sync controls, GitHub link metadata |
| Google Calendar integration | Settings integration architecture; external calendar model in calendar | No integration management | Connect/configure/disconnect Google Calendar, list synced calendars, enable/disable synced calendars, sync status/errors |
| Bitrix integration | `.reference/platform/models/bitrix/src/index.ts`: `BitrixSyncDoc`, `EntityMapping`, `FieldMapping` | No Bitrix tools | Integration connect/configure, entity mappings, field mappings, import/sync status, mapped docs |
| Gmail / email channel | Installed `@hcengineering/gmail@0.7.0`; `.reference/platform/models/gmail/src/index.ts`: `Message`, `NewMessage`, `SharedMessages` | Compatibility assessment returns explicit `supported=false`: the published package exposes legacy records, but Huly does not expose the live deployment-wide v1/v2 writer version needed to prove those records are current; contacts can store email values | Safe reads after an authoritative runtime check exists, compose/send queued email, reply-to, provider-specific attachments, shared email messages on contacts/channels, send status/error |
| Telegram channel | Installed exact `@hcengineering/telegram@0.7.0`; `.reference/platform/models/telegram/src/index.ts`: `Message`, `NewMessage`, `SharedMessages`; `docs/research/telegram-integration-spike.md` | `list_external_channel_messages` reads bounded, newest-first persisted Telegram messages by exact stored contact-channel value or stable ID, converts stored markup to Markdown, and reports explicit model/channel unavailability without implying provider freshness | Live provider health/sync freshness, compose/send/reply, shared-message records, attachment bytes, and durable failed/delivered/read status |
| Huly Mail / Mail plugin | Installed `@hcengineering/mail@0.7.0`; `docs/research/mail-integration-spike.md`; `.reference/platform/plugins/huly-mail`, `.reference/platform/plugins/mail`, `.reference/platform/models/huly-mail`, `.reference/platform/models/mail` | Read-only `list_mail_threads` discovers outer MailThread channel metadata with human-resolved spaces and bounded child Card subject summaries through the published Mail/Card contracts | Message bodies and attachments need a published communication/HulyLake read contract; send/reply, mailbox capability, and delivery status need authoritative mailbox and durable delivery acknowledgement APIs |
| Message templates | `.reference/platform/models/templates/src/index.ts`: `MessageTemplate`, `TemplateCategory`, `TemplateFieldCategory`, `TemplateField` | Read-only global template discovery and caller-provided `render_message_template` substitution are covered. Issue templates remain separate tracker tools. | Create/update/delete template categories/templates/fields and provider-backed execution/data semantics remain deferred. |
| AI assistant integration | `.reference/platform/models/ai-assistant/src/index.ts` registers `IntegrationType` | No AI-assistant integration tools | Connect/configure/disconnect AI assistant integration, inspect connection state. Actual assistant runtime needs deeper server-plugin review |
| AI bot | `.reference/platform/plugins/ai-bot`, `.reference/platform/server-plugins/ai-bot`; model has no first-class docs | No AI bot tools | Bot configuration/actions if server plugin exposes callable behavior. Needs deeper server-plugin review |
| Rating / reputation | `.reference/platform/models/rating/src/index.ts`: `DocReaction`, `PersonRating`; `npm view @hcengineering/rating version` returns 404 as of 2026-06-10 | Emoji reactions only through activity, not rating model | Star/rating reactions on docs, person rating/statistics, rating widget data blocked by #90 until a typed published SDK package is available |
| Support widget | Installed exact `@hcengineering/support@0.7.0`; `docs/research/support-integration-spike.md`; `.reference/platform_fork/models/support/src/index.ts`: `SupportConversation`, `SupportSystem` | `get_support_status` discovers zero/one/multiple model systems and returns only the authenticated account's private stored widget-status rows, clearly separated from live provider state | Message bodies, participants, attachments, transcripts, live unread counts, provider health, sends/replies, and widget controls have no published server-side contract |
| Billing / tiers | `.reference/platform/models/billing/src/index.ts`: `Tier`; billing plugin/resources | No billing tools | Billing tier/status discovery if relevant to workspace administration |
| Analytics collector / onboarding channel | `.reference/platform/models/analytics-collector/src/index.ts`: `OnboardingChannel` | No analytics/onboarding tools | Onboarding channels and analytics collector configuration; likely low priority for MCP |
| View / saved filtered views | `.reference/platform/models/view/src/index.ts`: `FilteredView`, `Viewlet`, actions, view preferences | Generic tools list/read filtered views and list viewlets with descriptor and preference metadata; Board tools cover Board-specific saved views. | Reusable action/filter/sort/group/aggregation configuration remains a lower-level gap. |
| Workbench tabs/widgets/apps | Installed exact `@hcengineering/workbench@0.7.0`; `docs/research/workbench-integration-spike.md`; `.reference/platform/models/workbench/src/index.ts`: `Application`, `ApplicationNavModel`, `HiddenApplication`, `Widget`, `WidgetPreference`, `WorkbenchTab` | `list_workbench_applications` discovers application and navigation model declarations by human URL alias and reports only the authenticated account's hidden preference boolean | Personal tabs and active selection are private/browser-local history; widgets and widget preferences lack a stable human-readable identifier and useful agent workflow; all Workbench mutations remain unsupported |
| Preference model | `.reference/platform/models/preference`; many modules store preferences | Read-only generic `SpacePreference` discovery through `list_space_preferences` and `get_space_preference` | Generic preference writes and module-specific preference payloads remain deferred unless the published SDK exposes safe typed fields |
| Document preferences and history | Installed `@hcengineering/document`; `node_modules/@hcengineering/document/types/types.d.ts` still exposes stale `SavedDocument extends Preference` plus `DocumentSnapshot`. Current platform source removed the `SavedDocument` model in hcengineering/platform#10124 and replaced document stars with rating-backed `DocReaction` / `ReactionKind.Star` | Document tools cover teamspaces, document CRUD, content edit, inline comments, relations, deletion, and read-only snapshot/history list/get. | Snapshot restore remains deferred until restore semantics are proven. Do not implement `SavedDocument` as SDK parity unless the platform model is restored; current document stars belong to the Rating / reputation row |

## Installed SDK Packages With Partial MCP Coverage

| Installed package | Current status | Notable missing first-class tools |
|---|---|---|
| `@hcengineering/activity` | Core model covered | UI presenter/viewlet/extension/control metadata is not MCP-facing. |
| `@hcengineering/attachment` | Core model covered | Preview UI metadata and more friendly object-specific wrappers remain workflow enhancements. |
| `@hcengineering/calendar` | Partial | #266 covers caller settings and PrimaryCalendar; provider lifecycle/deletion is excluded and RSVP awaits an upstream contract. |
| `@hcengineering/love` | Partial read-only | #267 and #268 cover durable Room/Floor and meeting-room composition; transient/provider behavior remains excluded or blocked. |
| `@hcengineering/board` | Installed model surface covered | Provider integrations require separate contracts; hard board deletion is intentionally excluded. |
| `@hcengineering/card` | Mostly basic CRUD | Card roles/permissions, favorites, export extensions, advanced master tag schema management |
| `@hcengineering/chunter` | Core chat covered | Request-access if modeled, server-side translation if exposed, and viewlet/sync/object-chat metadata |
| `@hcengineering/contact` | Main agent workflows covered | Full Channel/ChannelProvider administration, ChannelItem, PersonSpace, and UserProfile cards remain. |
| `@hcengineering/core` | Main administration workflows covered | Specialized permission/configuration and raw system-model internals remain. |
| `@hcengineering/document` | Partial | Snapshot restore, backlinks, notes, structured action items/tables, PDF/export, advanced document relationships |
| `@hcengineering/inventory` | Model CRUD plus product media/discussion wrappers covered; wrappers partial | Category/variant discussion wrappers if they prove user-visible |
| `@hcengineering/notification` | Installed agent-facing surface covered | Browser/push/provider-default and presenter/group metadata is not MCP-facing. |
| `@hcengineering/preference` | Read-only `SpacePreference` discovery covered | Generic preference writes and any module-specific preference payloads without published typed fields |
| `@hcengineering/request` | Installed Request surface covered | Friendly attached-target resolution remains a workflow enhancement. |
| `@hcengineering/tags` | SDK-level covered; module wrappers partial | Controlled-document labels are package-blocked; contact tags await a stable collection. |
| `@hcengineering/task` | Partial | Generic task/project abstractions beyond tracker, reusable task workflows |
| `@hcengineering/time` | Partial | #265 covers document-attached action items; team reporting is a deferred derived projection. |
| `@hcengineering/telegram` | Partial read-only | Live provider health/sync freshness, compose/send/reply, shared messages, attachment bytes, and durable delivery/read status |
| `@hcengineering/tracker` | Strong but partial | GitHub pull-request task type/sync details, PDF export, saved views beyond ProjectTargetPreference, tracker-local Document support type, and broader workflow automation behavior |
| `@hcengineering/view` | Filtered-view and viewlet discovery covered | Reusable action/filter/sort/group/aggregation configuration remains. |
| `@hcengineering/workbench` | Application/navigation declarations and caller hidden preference covered | Private/browser-local tabs, widgets without human-readable aliases, widget preferences, and all Workbench mutations |

## Highest-Value Additions For LLM Agents

1. **Common clear-field contract for update tools (implemented)**: Huly `DocumentUpdate<T>` supports `$unset`, but existing domain tools used a mix of field-specific clearing semantics: `null` for some optional relationships/dates/text, empty string for some descriptions/topics, and markup-specific empty references for rich descriptions. Update tools now use a shared LLM-facing contract where `null` means "clear/unset this field" for clearable fields, while backend operations map that intent to field-specific Huly-safe representations such as `""`, `null` markup refs, `$unset`, or `0`. Custom-field clearing remains separate because its safe backend representation depends on dynamic field metadata and storage owner.

   Required fields such as `update_milestone.targetDate` and required category refs such as `update_tag.category` are intentionally not clearable without a separate SDK-backed design.
2. **Recruiting residuals**: implement native-metadata Candidate custom fields under #264; keep new-person Candidate enablement as an explicit two-call composition.
3. **Planner residuals**: implement document-attached ToDos under #265. Team reporting remains a derived, privacy-blocked projection; `TodoAutomationHelper` is excluded server plumbing.
4. **Calendar and virtual office**: implement the narrow durable operations in #266-#268. Keep provider-owned, transient, transcript, recording, and RSVP behavior outside those tasks.

## Calendar, Schedule, And Virtual Office Omissions

- RSVP/calendar invite response state: omitted because installed `@hcengineering/calendar@0.7.0` exposes `Event`, `ReccuringEvent`, `ReccuringInstance`, and `Schedule` but no stable RSVP/response-status model. Unlock condition: a published SDK declaration with attendee response state or invite response records.
- Live meeting join/leave/start/stop writes: omitted from the first slice because they depend on transient room session and LiveKit behavior, not just durable model documents. Unlock condition: integration tests that can exercise local Huly live-session behavior deterministically.
- Meeting schedule room writes and raw love mixins: omitted as direct MCP inputs because callers should not need Huly mixin mechanics. Unlock condition: a room-aware calendar schedule write contract with integration coverage proving the backend accepts the mixin mutation.
- Transcripts and recordings as first-class artifacts: omitted because `MeetingMinutes` only exposes collection counts for transcription/messages/attachments in the current model declarations. Unlock condition: a stable read path for transcript/recording records and storage URLs.
- Latest SDK package upgrade: still blocked. `@hcengineering/love@0.7.0` is pinned because it publishes declarations that match the current SDK line; newer `0.7.423` tarballs were not adopted in this slice due missing/unsuitable declaration publication evidence captured in `plans/hcengineering-types-publication-evidence.md`.
5. **Controlled documents + trainings**: broad TraceX surface remains blocked until the owning SDK packages are published with usable declarations.

## Notes

- Some SDK/dev surfaces are UI/internal-heavy (`workbench`, `view`, `preference`, parts of `presentation`). They are included because models exist, but they are lower priority unless the MCP server aims to manage Huly UI state.
- Some integrations (`GitHub`, `Google Calendar`) are clearly product features, but their management APIs may live in account/server/plugin layers rather than simple document CRUD. They need implementation spikes before promising write tools.
- Existing generic tools (`list_activity`, `add_attachment`, `set_custom_field`, `create_relation`) can sometimes operate on raw Huly object IDs/classes. That is not counted as full coverage because it is not LLM-first: the caller must already know class IDs, collection names, and object resolution rules.
- Stale installed SDK declarations are not parity targets by themselves. `@hcengineering/document#SavedDocument` is deliberately ignored because current platform source removed its model registration and UI usage in hcengineering/platform#10124; current document stars are `@hcengineering/rating#DocReaction`.
